# Bushel Board - Current State

**Current implementation checkpoint (2026-06-23):** Normal farmer-facing `/thesis` is still Wheat-first, but the visible hero is now one Wheat Bull/Bear decision read instead of a Canada-vs-USA board. CA and US packets are evidence geographies inside the read, shown as compact source chips. The page leads with a large confidence-scaled Wheat score, bull/bear evidence cards, source-to-pressure-to-read mini-flow, Wheat-only local context, a latest USDA Crop Progress card for the 2026-06-22 release, and sorted pressure-lane bars. The source-lane swarm/reconciliation contract is now documented at `docs/plans/2026-06-23-wheat-bull-bear-thesis-swarm-redesign.md`: lane agents trace source rows to pressure cases, then a judge agent reconciles one consistent farmer-facing Wheat read. Track 54 write boundaries were preserved: no production Grok writes, no Supabase write-mode changes, no thesis-cache writes, and no retired Grok writer revival.

**Closeout checkpoint (2026-06-24):** Next Wheat session should start from `docs/plans/2026-06-24-wheat-thesis-goal-handoff.md`. The remaining product work is not another mockup pass; it is the deeper Wheat data-and-scoring loop: deepen the USDA Wheat source sweep, source-specific deciding-datum explanations, CFTC as timing/crowding context, and watch-only Hermes/Grok pulse unless tied back to official or admitted market data. The scorecard-backed headline, reconciliation judge, USDA Wheat source sweep, node-and-edge relationship spiderweb, Spring Wheat/HRW/SRW price-basket proof strip with packet-window plus 60-day historical price context, and bounded historical export context are wired into `/thesis`. The CGC Wheat export formula was repaired so Terminal Disposition export rows are not double-counted; cached week-45 Canada Wheat now shows 193.9 kt current-week exports, 19,663.9 kt crop-year exports, and 30.2% export/delivery ratio. Final closeout refreshed prices and thesis cache 12/12 with source watermark 2026-06-25T00:43:11Z; source-freshness watchdog reports 0 alerts. Current Wheat price context after that refresh: Spring Wheat/MWK26 7.11 USD/bu on 2026-06-25 (-0.594%), CBOT Wheat/ZW 5.8675 USD/bu on 2026-06-23 (-1.799%), and KC HRW/KE 6.1825 USD/bu on 2026-06-23 (-2.407%). Final session cleanup rechecked Hindsight memory and found no Wheat-specific documents, memories, tags, or mental models; repo docs remain operating truth. Skill cleanup added a shared `.agents/skills/_eval-core` so `agent-auditor` and `skill-auditor` no longer point at missing evaluator files, then tightened `grain-report`, `ai-pipeline-v2`, and `cftc-cot` so the next Wheat loop starts from the right source sweep and retired Grok writers stay archive-only.

**Last verified implementation checkpoint:** Bullish/Bearish Board V1 source-quality checkpoint is complete and pushed. `/thesis` and `/overview` are hard-gated to source-backed public V1 rows only (Corn, Soybeans, Wheat, Durum/Amber Durum, Canola, Barley, Oats). Spring Wheat and Winter Wheat are parked off public board surfaces until class-safe source mapping is admitted. Canada crop-progress parsing is hardened for Saskatchewan's newer narrative wording and Alberta's embedded historical rows, with regression tests. Canada crop progress is now surfaced in `v_source_freshness` and `grain_market_mappings` only for admitted V1 Canada lanes, with Saskatchewan crop-development group timing admitted live as low-confidence proxy evidence for mapped public grains only: Oilseeds -> Canola and Spring Cereals -> Wheat/Amber Durum/Barley/Oats. Corn/Soybeans do not inherit Saskatchewan development timing, and this does not unlock Spring/Winter Wheat or crop-specific condition/quality claims. Live source proof now includes 117 Saskatchewan rows from the 2026-05-28 crop report and a 12/12 thesis-packet-cache refresh after that import. Thesis cache V1 gating and `prediction_scorecard` public-read/service-role-write RLS remain live. Source-freshness watchdog now expects the hard-gated 12 thesis cache packets and selects the best same-period Prairie crop-progress package instead of letting a later province-only rerun downgrade a complete MB+SK+AB package; regression tests also lock that a newer partial period beats an older complete period. `/thesis` source-health now appears before the farmer read, Top Takeaway appears before the compact current snapshot, and both come before KPI summary cards so the market read is not buried on mobile. `/thesis` top takeaway now compares aggregate confidence-weighted reads against split-market country-side bull/bear pressure, labels one-country reads instead of implying full Canada-US confirmation, avoids singular/plural headline grammar traps for crops such as Soybeans and Oats, shows the lead evidence/source behind the named bull or bear read, and lists the first rows to inspect with reasons such as `country split` or `Canada-only read` instead of repeating `Open first`. Prominent evidence lines, driver cards, and freshness chips now use farmer-readable source names such as `CGC weekly grain stats` and `USDA Export Sales`; provenance rows retain raw source IDs in parentheses for audit/debugging. Quick scan and matrix rows now show action cues such as `Open first`, `Confirmed bull read`, `Confirmed bear read`, `One-country read`, or `Source gap`, while visible copy avoids prediction-market/advice phrasing such as `trade signal`, `pricing plan`, and `price-advice certainty`. Quick-scan legend copy is status-aware, so absent mapping gaps are not advertised while active one-country reads are explained. Stance bars in the scan and matrix are confidence-scaled, so low-confidence scores carry less visual weight. `/thesis` source-health distinguishes clean rendered source rows from partial Prairie crop-progress package status by surfacing partial package context only when the selected same-period package is actually partial; the top KPI now says `Watch Source Groups` and defines the count as stale/empty/lagged/broken freshness rows, not province completeness. Focused tests, lint, source-freshness watchdog, browser smoke, production build, broad TypeScript, and full all-files Vitest with a bounded two-worker runner pass.
**As of:** 2026-07-03

## Active task
Current focus is the **Wheat Bull/Bear Thesis decision board** on `/thesis`, with Track 54 daily/X evidence boundaries preserved. The farmer-facing route shows one active grain (Wheat), one visible read, and CA/US data only as supporting evidence geography while the shared multi-grain data/model harness remains available for later re-enable and for audit/operator proof.

**Strategic direction (2026-06-15): WHEAT-FIRST — depth before breadth.** Perfect and validate the full bull/bear pipeline on wheat (the most data-rich grain) before deepening the other 15; the weekly desk still publishes all 16. Two workstreams remain live alongside the Wheat board:
1. **Stance Model V2** (`docs/plans/2026-06-12-stance-model-v2-design.md`): deterministic relation-overlay rating, bounded international/FX/on-farm-stocks contexts, **GEE satellite crop-stress** lane (`gee_crop_stress`; US HRW + Russia winter/spring belts; validated vs NASS r=0.93/0.98; collects Fri 11am MT ahead of the desk swarm; **watch-only** until multi-season validation), and a stance-change ledger (debate Rules 20–21).
2. **Wheat-first site redesign** (`docs/plans/2026-06-15-wheat-first-data-viz-redesign.md`): data-rich visualizations + mapping + a dedicated data page (the GEE crop-stress map is the hero viz; start from that doc's §10 handoff).

**Collector/desk operational state (updated 2026-07-03):** the `npm run collect:*` wrapper migration from the 2026-06-15 handoff is complete — collectors registered and running (CGC, COT, export sales, crop progress, grain monitor, WASDE all green in `check:source-freshness`). The Friday desk swarms run headless via the service-role desk CLI (`npm run desk:cad|desk:us`, enabled 2026-07-02 with `DESK_WRITE_APPROVAL`). `collect:gee-crop-stress` was restored by merging `codex/data-layer-foundation-v1` (the collector had been stranded on that branch, which is why GEE writes stopped after 2026-06-15). Historical detail: `docs/plans/handoff/2026-06-15-session-handoff.md`.

---

Prior focus (still the operating pipeline): **Grok X Scout, Prices, and Daily Thesis Completion** from `docs/plans/2026-06-01-grok-x-price-daily-thesis-completion-plan.md`. This extends the source-backed Bullish/Bearish board with daily prices, a quarantined Grok/X evidence scout, deterministic X signal validation, bounded weekday soft updates, Friday accepted-signal bundles, and an `/thesis` X Pulse Watch panel.

Hard boundary: Grok can discover X evidence, but it cannot publish thesis rows, write `market_analysis` / `us_market_analysis`, write trajectory tables, or refresh `thesis_packet_cache`. The old Grok/xAI thesis-writing chain remains tombstoned. March Grok pipeline docs are retired history; `docs/plans/2026-05-27-x-signal-valuation-guardrail-framework.md` is conceptually accepted but superseded by the 2026-06-01 implementation plan.

Canonical Track 54 Bull/Bear data-flow map: `docs/reference/track54-daily-bull-bear-thesis-flow.md`.

The public V1 source-backed data/model scope remains Corn, Soybeans, Wheat, Durum, Canola, Barley, and Oats, but the normal farmer-facing display allowlist is currently Wheat only. Spring Wheat and Winter Wheat stay parked until class-safe source mapping is admitted.

Completed local work since the Kalshi parking update:

- 2026-06-24: Added the first USDA Wheat source-sweep panel to `/thesis`. The panel inventories Crop Progress, WASDE balance, Export Sales, and Quarterly Stocks as official U.S. source rows, showing source freshness, cadence, latest row date, weighted score effect, and whether the row is a deciding row, bear pressure, bull support, or cadence-limited context. It is a visual inventory/explanation layer over the existing scorecard; it does not add new score authority. Export Sales and Quarterly Stocks remain cadence-bound until USDA publishes fresher official rows.
- 2026-06-24: Deepened the USDA Wheat source-sweep explanation layer. `/thesis` now shows a condition-to-price relationship chain (`Condition signal`, `Balance anchor`, `Demand confirmation`, `Inventory check`, `Price confirmation`), adds `Decision role` blocks to each USDA source card, and gives the reconciliation judge source-specific copy so farmers can see why crop condition, WASDE/stocks, exports, and price confirmation do or do not overturn the current Wheat read. This adds no new score authority.
- 2026-06-24: Added packet-window price context to the Wheat board. `ThesisBoardItem` now carries normalized packet price rows, and `/thesis` shows `Packet trend context` under the Spring Wheat / HRW / SRW price-basket proof strip. The block compares earliest/latest packet settlements and labels sparse/provisional source legs such as Barchart Spring Wheat. It is explicitly packet-window context, not a full historical chart or new score authority.
- 2026-06-24: Added public-safe historical Wheat futures context. Migration `20260624192708_get_wheat_price_history` creates bounded RPC `get_wheat_price_history(integer)` for Spring Wheat, HRW, and SRW rows only; anon proof returned 103 rows across the three legs for the 60-day window. `/thesis` now renders `Historical price context` with three compact sparklines below the packet trend block. This remains price confirmation only, not a new score authority.
- 2026-06-24: Added public-safe historical Wheat export context and repaired the CGC export formula. Migration `20260625000843_get_wheat_export_history` plus performance/index follow-ups `20260625001442`, `20260625001726`, and `20260625001912` expose bounded `get_wheat_export_history(integer)` for Wheat-only demand visuals without raw `usda_export_sales` access. The Canada packet now uses Terminal Exports + Primary Shipment Distribution Export Destinations + Producer Cars Shipment Distribution Export; Terminal Disposition Export Destinations is treated as a cross-check. Cache refresh 12/12 updated Canada Wheat week 45 to 193.9 kt current-week exports, 19,663.9 kt crop-year exports, and 30.2% export/delivery ratio. `/thesis` now renders `Historical export context` below the price proof; this remains demand confirmation only, not new score authority.
- 2026-06-24: Parked an unfinished Wheat X-sentiment prototype found during cleanup at `docs/plans/2026-06-24-wheat-x-sentiment-ranking-plan.md`. Its executable TypeScript/test draft and leftover component draft were removed from the live tree because the prototype scored trusted prairie bearish signals as bullish; X pulse remains watch-only and must not move the official scorecard.
- 2026-06-24: Added the first farmer-facing Wheat reconciliation visual layer. `/thesis` now shows a `Reconciliation judge` card that chooses the largest weighted scorecard datum, compares it with the strongest bull/bear offsets, keeps CA/US as evidence geography, and states that daily overlays remain separate. It also adds a `Relationship spiderweb` ring view where distance bands and edge-width pills show score impact, plus a `Price basket proof` strip for Spring Wheat/MGEX, HRW/KCBT, and SRW/CBOT contract agreement/disagreement. This is an explanation layer over existing scorecards, not new score authority.
- 2026-06-24: Polished the Wheat reconciliation/spiderweb layer after source refresh. The judge now shows deciding-datum freshness proof, states the main counterweight, and avoids raw freshness labels such as `stale`. The relationship spiderweb now renders a true node-and-edge map with Wheat centered, bear pressure left, bull support right, numbered nodes, distance rings, and thicker edges for larger weighted score impacts. Targeted visual proof is in `scratch/wheat-spiderweb-section.png`; browser smoke verifies the spiderweb marker is reachable on desktop and mobile.
- 2026-06-24: Completed the Wheat source-freshness loop after the first reconciliation UI proof. Official CFTC SODA showed all three Wheat contracts available for 2026-06-16; `collect-cftc-cot.py --report-date 2026-06-16` wrote 8 CFTC rows and 9 heartbeat rows. Grain Monitor imported week 45 from the 2026-06-23 report. Producer Cars imported week 47, with Wheat at 587 week cars and 14,750 crop-year cars. `refresh-thesis-packet-cache.ts --force` refreshed 12/12 packets, and `check:source-freshness -- --json` passed with 0 alerts and freshness watch count 15. Export-sales dry-run confirmed ALL WHEAT still ends at 2026-06-11 pending the next USDA weekly release; quarterly stocks remains on its official reporting cadence.
- 2026-06-24: Reconciled the Wheat weekly-packet headline with the deterministic scorecard. `buildCanadaThesisBoardItem()` and `buildUsThesisBoardItem()` now keep driver-count scoring for explanation copy, but exact Wheat packets with populated scorecards return the rounded `ratingScorecard.overall_score`, `confidence_score`, and `confidence_label` as the visible weekly headline read. Daily overlays remain the only current-day override and are labeled separately in `/thesis`. Focused proof passed: `npx vitest run lib/__tests__/thesis-board.test.ts lib/__tests__/thesis-rating-domain-mappers.test.ts "app/(dashboard)/thesis/page.test.tsx" --pool=threads --maxWorkers=1 --no-file-parallelism --environment=jsdom`.
- 2026-06-24: Repaired deterministic Wheat price scoring in `lib/thesis/rating-domain-mappers.ts`. When multiple Wheat-class futures rows are present, the price domain now scores a Spring Wheat / HRW / SRW basket instead of the first/latest row; agreement sets direction and split contract direction lowers confidence. Focused proof passed: `npx vitest run lib/__tests__/thesis-rating-domain-mappers.test.ts --pool=threads --maxWorkers=1 --no-file-parallelism --environment=node`.
- 2026-06-23: Ran the first Wheat metric-relationship loop and documented it at `docs/plans/2026-06-23-wheat-metric-relationship-loop.md`. Refreshed USD/CAD FX through 2026-06-24, refreshed all tracked grain futures prices, repaired the CGC CSV `Accept` header fetch issue, imported CGC week 45 (4,411 rows, all 16 grains, week ending 2026-06-14), wrote collector heartbeats, and force-refreshed `thesis_packet_cache` 12/12 with source watermark `2026-06-24T00:32:06Z`. Final closeout price context is recorded in the checkpoint above. Corrected CGC Wheat week-45 flow after the 2026-06-24 export formula repair: 642.8 kt producer deliveries, 193.9 kt exports, 30.2% export/delivery ratio, and 2.0% process/delivery ratio. Hermes terminal daily-pulse no-write scout for 2026-06-23 completed with fresh price proof but 0 accepted signals; X Pulse remains watch-only. Main loop finding: official rows and prices support a lean bear / balanced-to-bear Wheat read until U.S. crop stress is confirmed by price, demand, or fresher supply shock.
- 2026-06-24: Closed the Wheat thesis redesign session cleanly. Added `docs/plans/2026-06-24-wheat-thesis-goal-handoff.md` for the next `/goal`, clarified Track 54 current state after the 2026-06-23 Hermes run, documented that Grok Composer 2.5 remains blocked by expired Grok CLI auth, preserved the no-write scout boundary, and tightened skill/agent guidance so retired Grok writers stay archived without blocking the no-write Hermes/Grok evidence scout. The next session should start from the deeper Wheat source sweep and scoring-replay questions, not from visual mockups.
- 2026-06-23: Redesigned the farmer-facing Wheat decision surface away from "Canada vs USA" framing and into one Wheat read with CA/US evidence chips, compact bull/bear cards, a source-to-pressure-to-read mini-flow, a latest USDA Crop Progress card, and sorted pressure-lane bars. The USDA update captured the 2026-06-22 Crop Progress release for week ending 2026-06-21: winter crop harvest 40%, winter crop good/excellent 26%, spring crop headed 16%, and spring crop good/excellent 54%. Added `docs/plans/2026-06-23-wheat-bull-bear-thesis-swarm-redesign.md` to define the lane-agent and reconciliation-judge contract for future Wheat rating consistency. Focused thesis page tests pass.
- 2026-06-16: Converted normal `/thesis` into a Wheat-first Bull/Bear decision surface. Added the centralized active farmer grain allowlist, filtered farmer-facing thesis/local-area data to Wheat only, preserved full multi-grain harness and audit graph proof behind `/thesis?audit=1`, and replaced the old all-grain quick-scan/matrix lead with a Wheat stance meter, top evidence cards, compact mobile decision snapshot, source-to-pressure-to-read data flow, pressure-lane breakdown, Wheat-only "What feeds this read", local area context, source health, Wheat pressure map, and Wheat packet details. Updated the Track 54 browser-smoke contract for desktop and 375px mobile `/thesis` plus `/thesis?audit=1`. Verification passed: typecheck, lint with only unrelated existing warnings, focused thesis/data-universe/grain-impact tests (133/133), production build, and browser smoke with fresh screenshots. No production Grok write, Supabase write-mode change, thesis-cache write, or retired Grok writer path was run.
- 2026-06-09 PM: Adversarial review + hardening pass over the grain relationship visualization before checkpoint. Fixed 24 SVG `fill`/`stroke` `var(--token)` attribute violations across `grain-impact-graph-panel.tsx` and `grain-relationship-explorer.tsx` by switching to theme-aware Tailwind `fill-*`/`stroke-*` classes per repo convention; exported `sourceAuthorityWeight`, `scoreRoleForSourceClass`, `scopeRelevanceWeight`, and the public/external scope rank multipliers from `lib/thesis/grain-impact-graph.ts` so the explorer's rank-proof strings derive from the model instead of hardcoded copies; replaced inline `boardReads = []` parameter defaults in the constellation and explorer client components with a stable module constant (the inline default re-created array identity per render and would have torn down and rebooted the Three.js scene on every re-render when the prop is omitted). Added 9 tests: full ranking-math branch coverage (source-class score roles, authority weights, every market-shape x scope relevance branch, the authority-x-scope rank invariant over all overview edges, the pinned Canola-to-Soybeans 0.70x90=63 proof, factor-node rank recomputation), a mocked-Three.js mount/unmount lifecycle test proving every created geometry/material/texture is disposed with frame-loop cancel and ResizeObserver disconnect, a pinned confidence-weighted vertical position formula, and the explorer empty-grains null render. Post-fix proof: focused tests 61/61, typecheck clean, lint 0 errors / 0 warnings, production build passed, and a fresh six-check browser smoke passed with zero console errors. One prior smoke run (pre-fix build) showed a single non-reproducing React #418 hydration exception on first desktop `/thesis` load right after server start; the rebuilt smoke run was clean on all routes/viewports, so it is recorded as an observed intermittent to watch, not a confirmed regression.
- 2026-06-09: Added the audit-only grain relationship visualization V1 on `/thesis?audit=1`. It renders a Grain Impact Graph, cross-grain relationship map, native Three.js relationship constellation, influence board, conversation matrix, selected-grain summary, rank-proof cards, current driver stack, and per-grain ranked network from the existing Bull/Bear impact model and current board reads. Normal `/thesis` remains clean and does not render this operator surface. React Three Fiber remains deferred; native Three.js is the scoped 3D layer for proving whether the ranked model is actually useful before adding a fuller 3D component stack. Latest proof passed focused tests, typecheck, lint, production build, Playwright desktop/mobile screenshots, current-server route smoke, and desktop/mobile Three.js canvas pixel checks proving nonblank render, animation, clickable node selection, and zero browser console errors.
- 2026-06-09 docs checkpoint: Added the canonical Track 54 daily Bull/Bear thesis flow reference at `docs/reference/track54-daily-bull-bear-thesis-flow.md`, linking official source rows, prices, Grok/Hermes X scout artifacts, artifact gates, promotion approval, bounded daily trajectory writes, and Friday thesis-of-record handoff in one operator map. Lessons learned now record the rolling clean-day gate failure mode and the Hermes `xai-oauth` provider requirement so future agents do not regress to raw artifact-count promotion math or the wrong Hermes provider path.
- 2026-06-09 early MT: Clarified Track 54 artifact-gate math across the no-write operator surface. Readiness now reports explicit `clean_artifact_days_found` alongside raw `artifact_days_found`, and both `npm --silent run track54:heartbeat-summary` and `npm --silent run track54:automation-runs` surface the clean count so the daily review no longer reads as "3/5 found" without showing that only 2 days are promotion-clean. The projection now simulates the rolling seven-day review window, so it shows 2026-06-09 through 2026-06-12 as the next eligible daily-pulse dates and 2026-06-12 as the earliest candidate date after the 2026-06-02 clean day falls out. The active heartbeat automation `track-54-promotion-review` was updated through Codex to inspect/report `clean_artifact_days_found`, and readiness now manifest-audits that prompt fragment so the recurring review cannot silently drop clean-day math. Fresh proof remains `overall_status = hold_manual_review`, `production_writes_enabled = false`, `daily_pulse = 3/5 found / 2 clean / 3 clean days missing`, `friday_deep = 0/1 found / 0 clean`, original `grok_runner.credential_issue = missing_credential`, and `hermes_terminal.model_smoke_ok = true`. Focused readiness/automation tests, typecheck, readiness rebuild, heartbeat summary, and production build passed. No X search, artifact retry, Supabase write, production write, or write-mode automation registration was run.
- 2026-06-03: Saskatchewan crop-development timing source admission is live. Targeted Supabase SQL applied and repaired migration history for `20260603190500`, `20260603192500`, and `20260603202000`; live `canada_crop_progress_metric_check` now admits `development_normal_pct`, `development_ahead_pct`, and `development_behind_pct`; live `get_canada_thesis_packet()` now includes condition and mapped development rows. `python scripts/import-canada-crop-progress.py --province SK --force` wrote 117 rows from the 2026-05-28 Saskatchewan Crop Report with source run `d0ebf5dd-bacb-47bf-a5b0-a8b30acbd6f8`. Direct live packet checks show Canola receives SK Oilseeds 73% behind / 27% normal development and Wheat receives SK Spring Cereals 63% behind / 37% normal development. Cached packets were refreshed 12/12 at `2026-06-03T19:51:17.680412+00:00`; cached rows show Canola, Wheat, Amber Durum, Barley, and Oats carry the mapped development rows while Corn/Soybeans have zero development rows. Focused source-admission tests, lint, source-freshness check, production build, and browser smoke passed.
- 2026-06-08 late MT: Updated the active primary no-write Codex collectors `grok-x-scout-artifact-week-review` and `grok-x-scout-friday-deep-artifact-review` to use the deterministic artifact-health Hermes fallback when the original Grok preflight fails, instead of hard-stopping before artifact recovery. Readiness now distinguishes "Hermes can collect no-write artifacts" from "original Grok credentials or `XAI_API_KEY` must be refreshed before write-mode approval." Fresh no-write readiness at `2026-06-09T03:14:07.491Z` is still `overall_status = hold_manual_review`, `production_writes_enabled = false`, `browser_smoke_clean = true`, `daily_pulse = 3/5`, `friday_deep = 0/1`, and write-mode automations remain missing/safe. `npm --silent run track54:automation-runs` now reports manifest update timestamps and marks the old 2026-06-08 primary collector failure as `latest_run_superseded_by_manifest_update = true`, so `automations_with_failed_or_blocked_latest_run = []` until the next scheduled run proves or disproves the repaired prompt. Focused guardrail verification passed: `npx vitest run lib/__tests__/track54-readiness-report.test.ts lib/__tests__/track54-automation-runs.test.ts lib/__tests__/track54-artifact-health.test.ts --pool=threads --maxWorkers=1 --no-file-parallelism --environment=node`.
- 2026-06-08 late MT: Hardened the Track 54 write gate so an approval phrase alone is not enough. Any future write-mode run must include the exact reviewed artifact window from the mode-scoped promotion brief via `--approval-review-from YYYY-MM-DD` and `--approval-review-to YYYY-MM-DD`; missing, malformed, or reversed windows fail before any bounded write can run. The promotion brief's proposed Codex write automations now also carry a structured `approval_review_window` object with mode, from/to dates, artifact-day counts, accepted-signal counts, and decision-grade counts; readiness rejects proposal drift if that structure no longer matches the mode gate. Focused proof passed: `npx vitest run lib/__tests__/track54-write-gate.test.ts lib/__tests__/track54-write-approval.test.ts lib/__tests__/daily-thesis-review-packet.test.ts lib/__tests__/daily-thesis-updates.test.ts --pool=threads --maxWorkers=1 --no-file-parallelism --environment=node` with 21 tests passing, then `npx vitest run lib/__tests__/grok-x-scout-promotion-brief.test.ts lib/__tests__/track54-readiness-report.test.ts lib/__tests__/track54-write-gate.test.ts --pool=threads --maxWorkers=1 --no-file-parallelism --environment=node` passed 80 tests. `npm --silent run daily-thesis-review -- --dry-run` wrote nothing and skipped the existing Wheat X Pulse decision as already applied (`writes_attempted = 0`, `writes_skipped_existing = 1`).
- Track 54 repo implementation and first live write proof are complete in this checkout: price collection wrapper, source-freshness watchdog price checks, X scout run/signals schema migrations, deterministic trusted-account validation/scoring, Grok/manual scout runner, daily thesis review packet/writer, Friday X bundle, desk prompt wiring, and `/thesis` X Pulse Watch UI are implemented and verified. Live Supabase has Track 54 migrations `20260601041000`, `20260601041100`, `20260601041200`, and `20260601052000`; Grok Build CLI `0.2.14` is installed, but the 2026-06-03 headless scout run exposed expired CLI auth; daily pulse run `f74c7b7c-fb69-4db1-aaa6-5b36ebe2bde8` wrote 2 X-verified accepted signals with fresh price status; daily review wrote CAD Wheat trajectory row `482` with `scan_type = opus_review_daily_pulse`; a retry dry-run skipped the already-applied daily pulse (`writes_attempted = 0`, `writes_skipped_existing = 1`). Focused tests, lint, build, source-freshness watchdog, price dry-run, Grok CLI dry-run, Friday X bundle, whitespace check, and fresh browser smoke proof passed before the 2026-06-03 auth expiry; readiness now consumes fresh proof but is held by artifact gates plus the current Grok auth blocker. Codex automation now has the official price/cache lane active at 3:45 PM MT, dry-run daily-pulse evidence active Mon-Fri at 4:10 PM MT, Mon-Thu daily artifact health at 4:45 PM MT, Friday-deep evidence active Fridays at 4:50 PM MT, Friday final artifact health active Fridays at 5:15 PM MT, and the Mon-Fri thread heartbeat at 9:30 AM MT for Grok auth warning plus 5:30 PM MT for evidence/promotion review. Same-day scout reruns preserve older raw artifacts with timestamped files, and the reviewer now selects the best valid no-write same-day artifact so a later quiet run cannot erase earlier useful evidence; any same-day write-mode evidence still holds the gate. Promotion briefs, readiness mode gates, and acceptance evidence now surface the selected raw artifact path plus SHA-256 hash so the trusted Grok run is audit-visible. The artifact-week gate requires at least 5 clean daily-pulse artifact days or 1 clean Friday-deep Friday artifact, plus at least 1 decision-grade accepted signal (`tier1`, `tier2`, or `tier3`), before it can become a human-approval candidate. The gate verifies no-write evidence from artifact summaries, raw artifact `run_date`/`mode` identity, Friday-deep schedule identity, and parsed artifact validation counts before claiming the no-Supabase-write and quality guardrails. A no-write, mode-scoped promotion brief builder now converts reviewer output into a human approval packet without enabling routines: `daily_pulse` reviews list only daily scout/review commands, and `friday_deep` needs its own reviewed Friday artifact before the Friday-deep writer is shown. Both `grok:x-scout --write` and `daily-thesis-review --write` now fail fast unless the Track 54 approval phrase is supplied and the artifact-week reviewer returns `candidate_for_enablement` for the intended mode. `/thesis?audit=1` now exposes accepted-signal allowed/blocked claims plus run-level rejected-signal reason summaries and corroboration status. Accepted X signal writes now persist deterministic `affected_decisions`, include the primary grain in `affected_grains`, dedupe affected regions, and reject signals missing affected-region evidence. X validation now enforces daily one-day and Friday seven-day search windows, rejects future-dated posts, includes a default corroboration block, and rejects numeric claims that Grok does not flag for official verification. Friday bundles, daily review trajectory evidence, and desk prompts now preserve the corroboration/allowed/blocked/official-verification evidence seal so unverified X claims stay review leads, not thesis facts. Friday bundle current-week resolution now picks the newest crop year before comparing grain-week numbers, so 2024-2025 week 52 no longer hides 2025-2026 week 42 accepted signals; older accepted rows with empty `affected_decisions` receive deterministic derived tags at read time. The xAI API fallback now loads `.env.local`, searches all trusted-handle batches, and merges/dedupes strict scout outputs before validation instead of searching only the first handle group. Write-mode scout runs now resolve the latest CGC grain week automatically when `--grain-week` is omitted, so the proposed approved automation commands are week-stable. The Grok CLI scout path now gets a temporary Grok home/config, web-search-only tool exposure, explicit MCP/file/shell/edit deny rules, a larger mode-aware turn budget, valid disabled MCP stubs, Grok CLI envelope parsing, Windows process-tree timeout cleanup, `.env.local` API-key loading, and fail-fast re-auth detection without changing the no-write boundary. Current daily-pulse gate evidence after the 2026-06-03 automation review: 2 clean artifact days, 4 accepted signals, 3 decision-grade accepted signals, 0 rejected, 1 parse/auth failure, fresh price context, 0 accepted unlisted handles after adding observed prairie commodity-org handle `SaskWheat` as tier3, 0 write-mode artifact days, 0 missing no-write-evidence days for selected valid artifacts, 0 summary-count mismatch days, 0 artifact identity mismatch days, and 0 schedule mismatch days. Current Friday-deep gate evidence: 0/1 clean Friday artifacts. Production Grok write automation remains disabled until the matching-mode gate passes and receives human approval.
- Added `npm run track54:readiness` as the single no-write operator gate for Track 54. It runs the daily-pulse and Friday-deep artifact reviews, converts both into mode-scoped promotion briefs, verifies the local price, dry-run, health-check, and heartbeat automation manifests are active with their expected schedules, repo `cwds` for workspace jobs, thread target for the heartbeat, no-write/no-retired-pipeline/browser-proof/readiness-report boundaries, and no explicit write-mode command fragments, verifies proposed write-mode Codex manifests are missing/inactive, verifies any inactive pre-staged write-mode manifest has the expected schedule, repo `cwds`, and approval/dry-run-disable prompt guardrails, verifies post-approval handoff disables matching dry-run collectors before write-mode registration, runs the read-only Grok CLI preflight, runs the read-only source-freshness watchdog, runs the focused Track 54 acceptance Vitest slice, and reports `overall_status`, `acceptance_audit`, `artifact_gate_projection`, `artifact_automation_coverage`, `write_automation_checks`, `production_writes_enabled: false`, and concrete next actions. `npm run track54:artifact-health` is now the deterministic same-day artifact guard used by the health-check automations; it retries only missing/structurally invalid no-write artifacts, can refresh browser smoke/readiness after retry, and does not treat an incomplete five-day promotion window as a retry condition. `npm run track54:heartbeat-summary` now carries `local_review_date`, `local_review_weekday`, `local_review_kind`, `review_modes_to_inspect`, and per-mode `selected_artifacts`, `write_mode_proposal_ids`, and `post_approval_transition`, so the Mon-Fri thread heartbeat reports the local review type, selected raw artifact hash/path, and approval-gated dry-run-to-write handoff without parsing the full readiness report. `npm run track54:browser-smoke` now starts local Next when needed, launches local Chrome headless, checks `/thesis`, `/thesis?audit=1`, and `/overview`, captures screenshots, rejects rendered forbidden public terms with whole-word matching, and emits timestamped proof JSON with `generated_at`; readiness rejects missing or older-than-six-hour browser proof. The stricter browser proof caught visible `/thesis` `price advice` copy and `/overview` `sell-down proxy` copy; thesis now uses `pricing instruction` wording and overview uses `farmer-movement proxy`. The active Codex dry-run automations `grok-x-scout-artifact-week-review` and `grok-x-scout-friday-deep-artifact-review` now run `npm run track54:readiness -- --out scratch/track54-readiness/latest-readiness-report.json --browser-smoke-proof-out scratch/track54-browser-smoke/browser-smoke-proof.json` after their mode-specific artifact review, so each automation report includes and persists `persisted_report_path`, `acceptance_audit.overall_status`, `acceptance_audit.grok_runner_proof`, `acceptance_audit.browser_smoke_proof`, `browser_smoke_clean`, source-freshness proof, focused acceptance-test proof, artifact automation coverage, write-automation safety, post-approval handoff proof, database/RLS migration contract proof, mode progress, projected next eligible artifact dates, selected artifact identity, and completion blockers without enabling writes. Latest 2026-06-07 proof is `overall_status = hold_manual_review` with `production_writes_enabled = false` and `browser_smoke_clean = true`: Grok CLI `0.2.14` is installed but credentials are missing (`credential_issue = missing_credential`), daily-pulse remains 2/5 clean artifact days in the reviewed readiness window with 4 accepted and 3 decision-grade accepted signals, 2026-06-03 and 2026-06-04 are explicit auth/parse failures, Friday-deep remains 0/1, projected dry-run dates are still covered by active dry-run automations, write-mode manifests remain missing/safe, and production Grok write routines remain disabled.
- 2026-06-03 MT: Inspected the latest Codex automation runs. Price/cache refresh succeeded, while `grok-x-scout-artifact-week-review` and `track-54-daily-artifact-health-check` failed to produce a valid 2026-06-03 daily artifact because Grok CLI auth expired after the wrapper-level MCP/config and JSON-envelope issues were fixed. The scout wrapper now fails fast on Grok re-auth prompts, and readiness now checks CLI/version plus credential proof by requiring either `XAI_API_KEY` or an unexpired local Grok `auth.json` expiry without printing secrets. Readiness and heartbeat are fresh enough for review, browser smoke is fresh/clean, daily-pulse remains 2/5 clean artifact days with 4 accepted and 3 decision-grade accepted signals, Friday-deep remains 0/1, production writes are false, and write-mode automation proposals remain unregistered approval-only specs.
- 2026-06-03 MT: Added lightweight `npm run track54:grok-preflight` and active Codex automation `track-54-grok-auth-preflight` for Mon-Fri 3:55 PM MT. It checks Grok version plus non-secret credential readiness before the 4:10 PM dry-run scout, exits nonzero when auth is expired, and is now manifest-audited by readiness. This does not run Grok search, the X scout, daily thesis review, write commands, Supabase writes, production Grok routines, or retired Grok endpoints.
- 2026-06-03 MT: Hardened the active no-write scout and artifact-health automations to require Grok preflight before any scout launch or retry. `scripts/run-track54-artifact-health-check.ts` now reviews existing artifact state first, then runs `npm run track54:grok-preflight` before retrying a missing/invalid same-day artifact; when credentials fail it returns `grok_preflight_ok = false` and `retry_blocked_by_grok_preflight = true` without launching Grok. Scheduled scout/retry prompts now use `--runner auto`, selecting `xai_api` when `XAI_API_KEY` is present and otherwise using the locked-down Grok CLI path. Readiness now manifest-audits that preflight-first wording in daily, Friday-deep, and health-check prompts.
- 2026-06-03 MT: Added active Codex automation `track-54-late-grok-auth-recovery` for Mon-Fri 5:05 PM MT. It runs the no-write `--mode both --retry-missing --refresh-readiness=always` artifact-health command after Grok preflight, giving a late same-day catch-up slot if Grok auth is refreshed after the normal health window but before the 5:30 PM review heartbeat. Readiness now manifest-audits this recovery guard; it does not enable writes or bypass artifact gates.
- 2026-06-04 MT: Updated active thread heartbeat `track-54-promotion-review` to run Mon-Fri at 9:30 AM and 5:30 PM MT. Before noon it runs only `npm run track54:grok-preflight`, notifies this thread when `credential_ok` is false, and stays quiet when credentials are ready; after noon it keeps the existing no-write evidence/promotion review. Readiness audits both branches, and the morning branch gives a same-day fix window before the 4:10 PM X scout. Added `npm run track54:recover-after-grok-login` as the one-command no-write recovery path after credentials are refreshed; it runs both-mode artifact health with missing-artifact retry and readiness refresh.
- 2026-06-04 MT: Strengthened `npm run track54:grok-preflight` from text-only evidence to structured non-secret fields: `credential_source`, `credential_issue`, `xai_api_key_present`, `cli_auth_file_present`, `cli_auth_expires_at`, and `cli_auth_expired`. The active auth preflight, late recovery, and thread heartbeat prompts now have to inspect those fields, so expired auth reports as `credential_issue = expired_auth` without launching Grok or relying on log parsing.
- 2026-06-04 MT: Added the same structured Grok auth diagnosis to `npm run track54:heartbeat-summary` under `grok_runner`. Afternoon heartbeat reviews now have to report `grok_runner.credential_issue`, so the thread-facing operator packet can identify `expired_auth` directly while still staying no-write.
- 2026-06-04 MT: Wired the locked-down Grok CLI scout path to accept a Grok Build model override through `--grok-cli-model`, `GROK_CLI_MODEL`, or the npm-safe positional form after runner. The local Grok model cache identifies Cursor/Composer 2.5 as `grok-composer-2.5-fast`; manual dry-runs, `npm run track54:recover-after-grok-login`, the active daily/friday no-write scout and artifact-health automations, and the approval-only future Grok scout write-mode proposals now pass that model while preserving web-search-only/no-write guardrails before approval and bounded write-gate guardrails after approval. Live scout execution is still blocked until Grok auth or `XAI_API_KEY` is restored.
- 2026-06-07 MT: Refreshed the no-write operator proof after recovery-command cleanup. `npm run track54:grok-preflight` reports `grok_version_ok = true`, `credential_ok = false`, `credential_source = none`, and `credential_issue = missing_credential` because neither local Grok CLI auth nor `XAI_API_KEY` is present. Added quiet fixed artifact-health aliases (`track54:artifact-health:daily`, `track54:artifact-health:both`, `track54:artifact-health:daily-retry`, `track54:artifact-health:both-retry`) to avoid Windows/npm 11 wrapper flag noise, and `track54:recover-after-grok-login` now delegates to `track54:artifact-health:both-retry`. Live shortcut proof returned `ok = true`, launched no Grok scout because Sunday artifacts were not due, refreshed readiness successfully, and kept production writes disabled. Fresh build and readiness proof passed; readiness still holds manual review on missing Grok credentials, 2/5 daily-pulse artifact coverage, two auth/parse failures, and 0/1 Friday-deep coverage.
- 2026-06-07 MT: Tightened the `/thesis` Track 54 readiness proof surface. The sanitized local readiness snapshot now carries non-secret Grok credential fields (`credential_source`, `credential_issue`, API-key presence, CLI-auth-file presence, and expiry flags) while still dropping raw command output, artifact paths, and artifact hashes. Normal `/thesis` now states the exact credential issue (`missing credential; source none`) inside `Daily automation gate progress`, and audit mode adds the safe recovery action without enabling Grok search, scout writes, daily thesis writes, Supabase writes, production Grok routines, or retired Grok endpoints. Focused readiness-query/thesis-page tests, production build, and fresh `track54:readiness` browser smoke passed.
- 2026-06-07 MT: Added a shared Track 54 safe-operator-action derivation for heartbeat and automation-run summaries. `npm --silent run track54:heartbeat-summary` now emits `next_safe_operator_action`, and `npm run track54:automation-runs` uses the same helper. When structured Grok proof reports a credential hold, both summaries tell the operator to refresh with `grok login` or `XAI_API_KEY`, then run `npm run track54:recover-after-grok-login`, and explicitly not to run the scout or write commands directly.
- 2026-06-08 MT: Added the Hermes/Grok 4.3 shadow X scout bridge for Track 54. `scripts/build-hermes-x-scout-artifact.ts` can generate a paste-ready Hermes prompt and import a saved Hermes JSON response into the existing local no-write artifact contract with `dry_run = true`, `write = false`, and `scout_run_id = null`; `docs/reference/hermes-grok-x-scout-bridge.md` documents the operator flow. Active Codex automation `track-54-hermes-x-scout-prompt-bridge` now runs as `Track 54 Hermes terminal X scout shadow recovery`: it runs `npm --silent run track54:hermes-preflight`, reviews today's daily_pulse artifact after the normal no-write scout window, skips Hermes when the same-day artifact is already structurally valid/no-write/date-matched/mode-matched/price-fresh, and runs the terminal Hermes shadow scout only when the same-day artifact is missing, invalid, parse-failed, identity-mismatched, or lacks fresh price proof. Track 54 readiness now audits that automation's schedule/workspace/prompt/no-write boundaries. It does not run Hermes for low signal count alone, does not run daily thesis review, does not run `--write`, does not write Supabase rows, does not enable production Grok routines, does not register write-mode automations, and does not call retired pipeline paths; Hermes remains a shadow X evidence scout subject to the existing Track 54 artifact reviewer.
- 2026-06-08 MT: Verified Hermes can run from the Codex terminal with xAI OAuth by using model id `grok-4.3` and added the no-write terminal runner `scripts/run-hermes-x-scout-terminal.ts` plus `npm run track54:hermes-x-scout:terminal`. The runner uses Hermes one-shot mode with `--provider xai-oauth`, `--model grok-4.3`, and `--toolsets x_search`, saves the raw Hermes response to `scratch/hermes-x-scout/`, performs the same read-only `grain_prices.price_date` freshness check as the normal scout runner, and imports the response into the same local Track 54 artifact contract with a shared prompt/raw/summary stem. It does not run daily thesis review, write Supabase rows, enable production Grok routines, or call retired Grok pipeline paths.
- 2026-06-08 MT: Added `scripts/check-track54-hermes-preflight.ts` and `npm run track54:hermes-preflight`. Live proof reports Hermes CLI available, xAI OAuth logged in, `model_smoke_ok = true` from a one-line no-search Grok response, `x_search` listed, global `x_search` disabled but explicit toolset override required, provider `xai-oauth`, and model `grok-4.3`; no X search or writes are run. Readiness and heartbeat summaries now include `hermes_terminal` separately from the original `grok_runner`, so missing normal Grok CLI/API credentials no longer hides the fact that Hermes terminal shadow recovery is available for no-write artifact collection.
- 2026-06-08 MT: Added deterministic Hermes fallback recovery to `scripts/run-track54-artifact-health-check.ts` behind explicit `--fallback hermes_terminal`. The daily, Friday, and late-recovery aliases now pass that fallback; live Codex automations `track-54-daily-artifact-health-check`, `track-54-friday-artifact-health-check`, and `track-54-late-grok-auth-recovery` were updated to inspect `hermes_preflight_ok`, `hermes_preflight_model_smoke_ok`, `retry_used_fallback`, and `retry_blocked_by_hermes_preflight`. If a same-day artifact is due and structurally missing/invalid, the health command still tries Grok preflight first, then may use Hermes terminal only after Hermes preflight passes with a no-search Grok model smoke proof. It remains no-write, does not retry for low signal count alone, and does not enable production routines.
- 2026-06-09 early MT: Current Track 54 operator proof is fresh and still held by artifact gates, not runtime/build failure. `npm --silent run track54:heartbeat-summary` reports local review date `2026-06-09`, `overall_status = hold_manual_review`, fresh readiness/browser proof, `production_writes_enabled = false`, `write_automations_safe = true`, original `grok_runner.credential_issue = missing_credential`, and `hermes_terminal.ok = true` with `model_smoke_ok = true`. Current daily-pulse evidence is `3/5` artifact days with `2/5` clean, `1` accepted and `1` decision-grade accepted signal; the selected 2026-06-08 Hermes terminal artifact hash is `57009259510318769b7191b23d1ff1373cc08979bdae0cdd71306c603f045339`. The rolling-window projection now lists 2026-06-09, 2026-06-10, 2026-06-11, and 2026-06-12 as the next eligible daily-pulse dates, with earliest candidate 2026-06-12. Friday-deep remains `0/1`, also earliest 2026-06-12. `npm run daily-thesis-review -- --dry-run` builds the bounded review packet without writes and skips the existing Wheat X Pulse decision as already applied (`writes_attempted = 0`, `writes_skipped_existing = 1`). Next action is to let the June 9 through June 12 no-write windows run, using Hermes fallback only through scheduled/deterministic health recovery if same-day artifacts are missing or invalid, and refresh original Grok credentials before any write-mode approval.
- 2026-06-02: `/thesis` now includes a `Daily Update Status` strip and a `Weekly Data Intake` section. The daily strip shows board packet/cache freshness, grain-price context, latest X Pulse accepted/rejected proof, and the daily soft-update gate boundary. The intake section shows which source data is pulled through the week, the target table each source feeds, the current board freshness/run status for that source, and what pressure it adds to the bull/bear read. It now leads with a plain week-flow strip: daily price base, early crop read, Thursday flow check, Friday thesis handoff, and monthly WASDE reset. Official rows aggregate the packet freshness already used by the board, while the X Pulse row shows latest supervised scout accepted/rejected proof and price-snapshot status. The intake rows now also fall back to latest `source_runs` collector proof, so non-rendered intake lanes can show the latest collector status, finished time, source period, and changed-row count instead of only saying the source is not in the current packet. Browser smoke now requires `Daily Update Status`, `Weekly Data Intake`, `Current board status`, and `Latest collector` on `/thesis` and `/thesis?audit=1`. Focused thesis/source-run tests, Track 54 readiness/browser smoke, lint, production build, and in-app browser DOM inspection passed; readiness remains blocked only by artifact gates.
- 2026-06-02: `/thesis` now also reads the latest bounded daily trajectory rows from `score_trajectory` / `us_score_trajectory` where `scan_type = opus_review_daily_pulse`, normalizes CAD text evidence and US JSON evidence into one display shape, and applies matching-period rows as a visible current-score overlay across Top Takeaway, quick scan, matrix, and grain cards. The overlay updates stance/confidence only; weekly thesis text, official-source drivers, packet freshness, and Friday thesis-of-record boundaries remain anchored to the source-backed packet. `Daily Update Status` shows `Latest Daily Trajectory Moves` with stance/confidence deltas, current stance, signal note, reasoning, and watch-evidence provenance. This does not enable write-mode Grok routines. Focused daily-update/source-run/thesis page tests passed.
- 2026-06-02: `Daily Update Status` now includes `Daily overlay coverage`, showing how many visible board rows have a matching current-period trajectory overlay versus how many remain on weekly packet scores. It also names the specific daily-overlaid rows and the rows still on weekly packet scores, so partial daily update coverage is explicit instead of implying that one bounded movement row updates the entire Bull/Bear board. Focused thesis page tests passed.
- 2026-06-02: Visible score rows and grain cards now label their score source inline as `Score source: Daily overlay` or `Score source: Weekly packet`. Daily trajectory overlays still update stance/confidence only, but the row itself now shows whether the displayed Bull/Bear score is today's bounded overlay or the Friday weekly packet score.
- 2026-06-02: `Daily Update Status` now separates current-period daily overlays from same-day overlays. A prior-day bounded trajectory row can still display as a daily overlay, but the top daily soft-update tile now says `prior-day overlay` until a current local-day row exists, and the coverage panel shows how many visible rows are stamped today.
- 2026-06-02: `Weekly Data Intake` now labels each source row with its allowed role (`Official thesis input`, `Price context`, or `Watch-only evidence`) and pressure lane (`Supply/weather pressure`, `Demand/flow pressure`, `Logistics pressure`, `Positioning pressure`, `Price/FX pressure`, `Social/evidence risk`, or `Supply/demand baseline`). This makes the weekly pull list more decision-useful without letting price context or X Pulse watch leads appear equal to official thesis inputs.
- 2026-06-02: `Weekly Data Intake` now includes an `Analyzed data` field for every pull, naming the measurements the board watches before translating a source into Bull/Bear pressure. Examples include USDA planting/condition/harvest percentages, CGC producer deliveries/stocks/receipts/exports, Export Sales commitments and admitted projection pace, CFTC position changes, futures/FX/CAD-normalized prices, and X Pulse source-tier/claim/decision metadata.
- 2026-06-02: `Weekly Data Intake` now has a mobile-first `Quick source cards` view for the same 12 data pulls while preserving the full desktop audit table at larger widths. Each mobile card shows timing, source, allowed role, pressure lane, current board/run status, target table, and what the source changes in the Bull/Bear read, so farmers do not need sideways table scrolling to understand the weekly data path.
- 2026-06-02: `Weekly Data Intake` now includes a `Current week pull tracker` that reads the static intake cadence against the current America/Edmonton day. It summarizes today's scheduled pull lanes, how many of those lanes already have current proof from packet/source-run/X artifacts, the next official pull after today, and the Friday source-of-record handoff inputs. Browser smoke now requires this marker on `/thesis` and `/thesis?audit=1`.
- 2026-06-02: `Weekly Data Intake` now includes a `Weekly pull calendar`, grouping the configured intake lanes by Monday-Friday so farmers can see which official sources, price context, and watch-only evidence land each day before the Friday thesis handoff. The calendar uses the same proof objects as the intake table and does not add new source authority.
- 2026-06-02: The top `Current snapshot` card now includes `Board update mode`, summarizing how many visible Bull/Bear rows are stamped today, how many use current-period daily overlays, and how many remain on weekly packet scores. This moves the daily-vs-weekly board state to the top of `/thesis` without changing source authority or enabling write-mode routines.
- 2026-06-02: `/thesis?audit=1` now includes an audit-only `Track 54 production gate` panel sourced from the latest local readiness report. It shows production writes disabled, browser proof status, daily-pulse and Friday-deep artifact gate progress, completion blockers, and next operator actions without enabling any Grok or daily-thesis write-mode routines. Normal `/thesis` stays farmer-facing and hides this operator gate.
- 2026-06-02: `Weekly Data Intake` now also includes `How data becomes Bull/Bear pressure`, a compact rule strip that explains bullish versus bearish interpretation for supply/weather, demand/flow, logistics, positioning, price/FX, social/evidence, and WASDE baseline lanes. The X Pulse lane is explicitly marked watch-only and not decisive alone unless official-source or desk review corroborates the lead.
- 2026-06-02: `Daily Update Status` now includes a compact `Daily decision path`: Price/FX refresh, X Pulse dry-run proof, bounded daily review, and Friday thesis source-of-record handoff. This makes the daily Bull/Bear update chain explicit on the board before write-mode routines are enabled; browser smoke now requires the marker on `/thesis` and `/thesis?audit=1`.
- 2026-06-02: `Daily Update Status` now also includes `Today update window`, showing whether the current local trading-day update is price-pending, waiting for the 4:10 PM MT X Pulse proof, in the post-scout artifact-health window, weekend-held, or already has same-day X proof stored. It uses current-day price freshness before falling back to clock-based status, so a fresh morning price refresh correctly advances the board to the X-proof gate without enabling write-mode routines.
- 2026-06-02: `Daily Update Status` now includes `Today update checklist`, a four-gate current-day proof board for `Price/FX context`, `X Pulse proof`, `Bounded daily row`, and `Daily automation gate`. The checklist counts completed gates and states exactly why the daily Bull/Bear status is current, pending, or proof-gated, while keeping automatic write mode disabled until artifact streak and approval gates clear.
- 2026-06-02: `Daily Update Status` price context now keys the top daily gate to the required `grain_prices` futures/FX feed only, so optional empty `posted_prices` rows no longer make the daily status badge read `empty + strong`. Grain cards can still expose posted-price gaps in source freshness, but the top weekday update gate now reads the required price proof cleanly.
- 2026-06-02: `Daily Update Status` now labels the scout tile as `Latest X Pulse proof` and says when that stored proof is from a prior local day. The same card points current-day proof status to `Today update window`, so a green stored proof cannot be misread as today's 4:10 PM MT no-write artifact already being complete.
- 2026-06-02: Normal `/thesis` now shows sanitized `Daily automation gate progress` inside `Daily Update Status`, using the local Track 54 readiness snapshot when present. Farmers can see daily-pulse and Friday-deep clean-artifact progress, accepted and decision-grade counts, next proof dates, and the write-disabled boundary without exposing local file paths, artifact hashes, or the audit-only production gate panel.
- 2026-06-02: The normal `Daily automation gate progress` card now also shows sanitized readiness-proof freshness: generated time, browser-smoke status, overall gate status, and the first blocker. This prevents stale local readiness counts from reading like a current daily Bull/Bear update while still keeping file paths, hashes, and operator-only detail out of normal `/thesis`.
- 2026-06-02: `scripts/run-track54-readiness.ts` now forwards browser-smoke server options such as `--base-url`, `--chrome-path`, `--settle-ms`, and `--no-start-server` to the smoke step while keeping them out of the readiness builder. It also recovers the Windows/npm 11 case where npm strips unknown flags into `npm_config_* = true` plus bare positional values, so the official operator command still smokes the intended fresh local server instead of silently falling back to 3110. A focused regression test covers this wrapper failure mode.
- 2026-06-02: `run-track54-artifact-health-check.ts --date <today>` no longer front-runs the local scout cutoff. Explicit same-day review dates now use the same America/Edmonton completed-run clock as implicit checks, so a missing 2026-06-02 daily artifact at 11:04 AM MT reports `artifact_due = false`, `retry_needed = false`, and `retry_blocked_until_due = true` instead of triggering a premature retry path.
- 2026-06-02 MT: Refreshed official USD/CAD and grain price context before the scheduled afternoon automation. FX import upserted 15 USD/CAD rows through `2026-06-02` with source run `d4f269dd-6676-48fa-b02a-49b30531d717`. `npm run collect:prices` fetched all 9 tracked contracts, upserted 65 price rows, wrote grain-price source run `81e8f359-342c-4c7a-953d-cd9d4395db16`, normalized CAD prices with 0 missing FX rows, and force-refreshed `thesis_packet_cache` to 12/12 packets at `2026-06-02T14:42:30.655584+00:00`. A manual morning `daily_pulse` dry-run attempt timed out before Grok returned JSON and produced only a prompt file, not a valid artifact; `track54:artifact-health --mode daily_pulse --date 2026-06-02` confirmed retry is deferred until the scheduled 4:10 PM MT window (`artifact_due=false`, `retry_blocked_until_due=true`). The scheduled afternoon `daily_pulse` dry-run produced a valid no-write artifact for `2026-06-02` with hash `04a6005cc972fefec32f8c12f52480037fb26f9ca00f3c327e697c769a5b6012`, 1 accepted / 1 decision-grade signal, and fresh price context. The scout runner now has a mode-aware timeout, explicit web-search-only/MCP-deny CLI guardrails, and writes failure summaries with `status = failed`, `parse_status = failed`, no `scout_run_id`, and no Supabase-write evidence when Grok times out or returns unparsable output; the artifact reviewer treats those summaries as auditable failed attempts, not clean artifact days. Final post-build readiness at `2026-06-03T00:29:53.188Z` (June 2, 6:29 PM MT) remains `blocked_by_artifact_gates`, browser smoke is clean against `http://127.0.0.1:3111` with daily-status/intake/current-status/readiness-proof/Analyzed-data markers present, production writes are disabled, daily-pulse is 2/5, and Friday-deep is 0/1. Fresh heartbeat summary at `2026-06-03T00:32:29.172Z` is `ready_for_review` for Tuesday daily-pulse review with no heartbeat blockers, fresh browser proof, and `safe_to_promote_without_human_approval = false`.
- Kalshi Prediction Board remains parked: public API wiring is proven, but no open Corn/Soybeans/Wheat markets were available at the last Kalshi check. `/kalshi` remains available only as read-only API proof.
- Phase 3A multi-grain harness refactor: shared grain profiles and grain-agnostic forecast harness path while preserving Canola compatibility. This remains no-write and not connected to production/dashboard writes.
- Phase 3B `/thesis` major-grain matrix exists, but the product scope is now narrowed further for V1: Corn, Soybeans, Wheat, Durum, Canola, Barley, and Oats only. Spring Wheat and Winter Wheat are intentionally parked until class-safe source mapping exists. Pulses, flax, smaller CGC labels, US rice/cotton, and Kalshi are excluded from the next pass.
- Transparent Thesis Rating Model V1 is complete through Task 9 in this checkout. The deterministic scorecard contract, source-quality adjustments, domain weights, aggregation, packet mappers, `ThesisBoardItem.ratingScorecard`, audit-only `/thesis?audit=1` UI, and LLM guardrail payloads are implemented and tested. Do not restart `docs/plans/2026-05-28-transparent-thesis-rating-model-v1.md` unless doing a review or follow-up migration.
- USDA export-sales freshness was repaired 2026-05-16. Live Supabase now has `usda_export_sales.week_ending = 2026-05-07` with 5 rows / 5 commodities for the latest week: ALL WHEAT, CORN, SOYBEANS, BARLEY, OATS.
- USDA quarterly grain stocks were added, imported, and wired into the US thesis packet/cache 2026-05-22. Live Supabase now has `public.usda_quarterly_stocks` with 47 rows for BARLEY, CANOLA, CORN, OATS, SOYBEANS, and WHEAT; latest regular quarterly stock report is `2026-03-01` for all except CANOLA (`2025-06-01`, because NASS only returned June canola stock rows for this pull). `get_us_thesis_packet()` now emits `supply.quarterly_stocks`, `get_thesis_data_freshness()` includes `usda_quarterly_stocks`, and the `/thesis` board has been visually audited/polished with a source-health banner, farmer-read top takeaway before the quick scan, clearer quick-scan legend, and more accurate missing-market copy for Durum/Canola US lanes.
- USDA acreage is now admitted into the US thesis context. Existing `crop_acreage_estimates` rows flow through `get_us_thesis_packet().supply.acreage`, `get_thesis_data_freshness()` reports `crop_acreage_estimates` for the US lane, and deterministic `/thesis` planting-progress drivers now express progress against the actual planted-acre base instead of percentage-only reads.
- US WASDE month-over-month revision context is live. Local migration `20260522151000_add_wasde_revision_context_to_us_thesis_packet.sql` was applied through Supabase MCP as live migration `20260522183715_add_wasde_revision_context_to_us_thesis_packet`; it adds previous-report deltas for ending stocks, stocks-to-use, exports, domestic use, crush, and production into `get_us_thesis_packet().supply.wasde` / `.demand.wasde`; `buildUsThesisBoardItem()` turns material WASDE ending-stocks/export/domestic-demand revisions into deterministic bull/bear drivers.
- Export Sales + WASDE projection compound scoring is implemented and live in `buildUsThesisBoardItem()`, but the board treats projection pace as an importer-admitted field only. Wheat has admitted guarded projection pace because live `usda_export_sales.export_pace_pct = 102.315` and `usda_projection_mt = 24,494,000` passed the 60–140% guardrail against `total_commitments_mt = 25,061,015`. The Export Sales importer commodity codes were corrected on 2026-05-25 to match the public USDA ESR catalog (`CORN=401`, `SOYBEANS=801`, `BARLEY=301`, `OATS=601`), then Corn/Soybeans/Barley/Oats were re-imported through `2026-05-14` and thesis cache was force-refreshed. Corn now admits `export_pace_pct = 95.287` against `usda_projection_mt = 83,824,000`; Soybeans now admits `export_pace_pct = 94.551` against `usda_projection_mt = 41,640,000`. Barley and Oats intentionally keep projection pace null because their implied paces still failed the guardrail, so `/thesis` does not render fake projection-pace drivers for those markets. UI code must not recompute pace from `total_commitments_mt` and `usda_projection_mt` without importer admission.
- `thesis_packet_cache` was force-refreshed after the Export Sales commodity-code repair and non-wheat re-import. Verification: 21 cache rows; Wheat, Corn, and Soybeans 2025 cached packets contain admitted export pace/projection fields; Barley and Oats cached packets keep projection fields null by design. Browser audit of `/thesis?audit=1` loaded successfully before this patch, showed fresh cached-board status, showed all 9 V1 quick-scan rows, surfaced Wheat's guarded export-sales/WASDE projection driver, did not show projection-pace claims for null-guarded markets, displayed WASDE/quarterly-stocks freshness as usable/strong rather than false empty-seed warnings when cached latest periods exist, and had a clean console.

Primary fresh-session handoff: `docs/plans/2026-05-31-bushel-board-source-quality-handoff.md`. Previous same-day handoff `docs/plans/2026-05-31-bushel-board-fresh-session-handoff.md` is superseded for current task selection but remains useful for the blocker-fix history. Earlier handoffs `docs/plans/2026-05-28-bushel-board-live-run-handoff.md`, `docs/plans/2026-05-26-bushel-board-fresh-session-handoff.md`, `docs/plans/2026-05-26-bushel-board-handoff.md`, `docs/plans/2026-05-25-bushel-board-handoff.md`, `docs/plans/2026-05-24-bushel-board-handoff.md`, and `docs/plans/2026-05-23-bushel-board-handoff.md` are superseded for current task selection but remain useful for collector/source-admission history.

Background harness handoff: `docs/plans/2026-05-10-multi-grain-weekly-thesis-harness-handoff.md`.

Parked Kalshi handoff: `docs/plans/2026-05-10-kalshi-prediction-board-next-session.md`.

Existing automations remain unchanged: `canola-predictive-harness-no-write-review` and `cgc-weekly-point-in-time-snapshot-capture` stay active, but they are not the current product focus.

GitHub CLI is authenticated for account `Bushels` with HTTPS protocol. `gh repo view` resolves this checkout as `Bushels/Bushel-Board`, default branch `master`.

## Known blockers
- **Friday bushel desk has been silent since 2026-04-25 (discovered 2026-06-09).** `market_analysis` last wrote 2026-04-18 (week 36, CAD) and `us_market_analysis` 2026-04-20; `pipeline_runs` shows failed runs on 2026-04-24/25 (root failure: phase 0.3 freshness guardrail aborted on stale grain prices, which predates the Track 54 daily price collector) and zero rows of any status after 2026-04-25 — meaning the Claude Desktop Routines `grain-desk-weekly` (Fri 6:47 PM ET) and `us-desk-weekly` (Fri 7:30 PM ET) stopped firing entirely. Repo-side prompt bugs that would have killed restarted runs were fixed 2026-06-09 in `docs/reference/grain-desk-swarm-prompt.md` + `us-desk-swarm-prompt.md`: failure/success `pipeline_runs` inserts used `triggered_by` values (`grain-desk-weekly`, `us-desk-weekly`, `claude-agent-us-desk`) that violate the live `manual|cron|retry` CHECK, the US prompt referenced nonexistent `source`/`metadata` columns, and Step 0.0 hard-pinned `claude-opus-4-7` so any newer Opus-class model would abort the desk (with the abort row itself failing on the CHECK — a zero-trace death). **Operator action required (Kyle):** re-enable or recreate both Claude Desktop Routines using the updated prompt docs before Friday 2026-06-12; daily prices now refresh at 3:45 PM MT so the phase 0.3 guardrail should pass. Until the desk runs again, grain detail and My Farm stale-guard the week-36 thesis (added 2026-06-09) so it cannot present as the current read or drive haul/hold framing.
- Grok CLI authentication expired for the noninteractive Track 54 scout run on 2026-06-03, and `.env.local` does not currently provide `XAI_API_KEY`. The latest preflight still shows `credential_issue = missing_credential`, `credential_source = none`, `cli_auth_file_present = false`, and `xai_api_key_present = false`. Hermes terminal is authenticated through xAI OAuth and is now wired into the active primary no-write collectors plus deterministic health/recovery fallback, so missing original Grok credentials no longer mean no-write artifact collection is blocked. Original Grok auth or `XAI_API_KEY` still must be refreshed before any write-mode approval; do not register write-mode routines before the artifact gate is candidate-ready and Kyle explicitly approves.
- Full all-files Vitest now passes with the bounded two-worker runner, and `npm run test` is pinned to that command. Last proof before pinning returned 147 files / 1088 tests in 169.07s on 2026-06-07. A single-worker serial run exceeded the 3-minute guard and should not be used as the release proof.
- Lint is now 0 errors with warnings only; latest verification showed 71 warnings. Warnings remain existing cleanup debt.
- Kalshi public API wiring is proven, but it returned `0` open Corn/Soybeans/Wheat markets at the last check. The Kalshi lane is parked until open grain commodity contracts return.
- Closed/finalized Kalshi markets may be shown as latest API proof only. They must not drive live probability comparisons or the Bushel Board Implied Line.
- Wording boundary: Bushel Board is not a live prediction market yet. Do not use trading/advice copy, and do not imply a model-training/fine-tuning loop from unreviewed Kalshi comparisons.
- Collector-triggered thesis-cache refresh stale-watermark behavior was patched 2026-05-17. The wrapper now calls `refresh-thesis-packet-cache.ts --force` after successful collectors, while dry-run collectors still skip refresh. Direct manual cache refresh still keeps the normal skip behavior unless `-- --force` is provided.
- Predictive harness production integration still requires review before release. Codex Automation now owns the scheduled no-write review path; Hermes automation is out. The primary learning loop is weekly thesis review against next-week evidence; price scoring is optional and must not block thesis-review learning while grain price history is incomplete. Kyle approved future harness phases and patches, but do not treat that as permission to skip review, write sidecar/production data, use private farmer/operator/chat data, or connect production dashboard reads before the review step is complete.
- `/api/pipeline/run` is permanently tombstoned as `grok_workflow_deprecated`; do not use it for CGC imports or analysis recovery.
- Grok-backed farm summary generation is tombstoned; personalized summary refresh needs a Claude/Codex replacement writer before it is current again.
- Grok write-mode automation is still not registered. The active Codex Grok jobs are dry-run only: `grok-x-scout-artifact-week-review` collects the one-week daily-pulse evidence gate, `track-54-daily-artifact-health-check` inspects/retries missing same-day daily-pulse artifacts without writes on Mon-Thu, `grok-x-scout-friday-deep-artifact-review` collects the separate Friday-deep evidence gate, and `track-54-friday-artifact-health-check` inspects/retries both final-day `daily_pulse` and `friday_deep` artifacts before the Friday review. Readiness now also inspects both health-check manifests, the thread-targeted `track-54-promotion-review` heartbeat, and the proposed writer IDs (`grok-x-scout-daily`, `daily-thesis-review`, and `grok-x-scout-friday-deep`) and holds manual review if any expected no-write boundary drifts or any write-mode Codex manifest is active before human approval. The scout and artifact-review scripts now default run/review dates to the `America/Edmonton` automation calendar and use the last completed local artifact cutoff, so late-evening Mountain-time operator checks do not skip the next local dry-run date by crossing midnight UTC and early-morning checks do not treat the current day's scheduled dry-run as already missed. Artifact health now also reports `artifact_due` and defers missing-artifact retries before the 4:10 PM MT daily-pulse or 4:50 PM MT Friday-deep cutoff, so a premature manual health check cannot front-run the price/scout schedule. The heartbeat now wakes this thread Mon-Fri at 9:30 AM MT for a Grok auth warning branch and 5:30 PM MT for daily_pulse no-write evidence review or Friday mode-scoped promotion review; its summary includes local review date/type, review modes to inspect, selected artifact hash/path, write-mode proposal IDs, post-approval dry-run-to-write transition, and browser-smoke proof age/freshness when a reviewed artifact exists. Grok CLI output is valid after prompt/cwd hardening, but it remains less deterministic than an xAI API `x_search` runner with structured output.
- Track 54 migrations are applied live. Because existing Supabase migration history has older timestamp drift, new Track 54 migrations were applied through `npx supabase@2.102.0 db query --linked` with explicit migration-history inserts instead of repairing unrelated history.
- `supabase migration list --linked` may still fail intermittently with a `SUPABASE_DB_PASSWORD` auth error in this shell. The live migration ledger was confirmed with `supabase_migrations.schema_migrations` via `supabase db query --linked`.
- `npm run collect:* -- --dry-run` did not reliably forward child dry-run flags in the Windows runner. Use importer-specific dry-run commands or call `scripts/run-collector-with-thesis-cache-refresh.ts` directly with the child `--dry-run` flag.
- Gemini 3.1 Pro Preview is useful for strict single-file audits, but it hit capacity/timeouts during package-level review. Do not treat a missing Gemini response as release proof.
- Barchart OnDemand intraday Canola remains paused until `BARCHART_ONDEMAND_API_KEY` is available.
- `npm run typecheck` now wraps the broad TypeScript gate, and `npm run verify` runs typecheck, lint, full bounded Vitest, and production build in order. Stale fixture/mock type repairs landed across seeding progress, Track 54 env helpers, overview supply-disposition fixtures, public-copy guardrails, forecast artifacts, Bushy/weather tests, and knowledge helpers. The repo lint backlog is now clean at 0 errors / 0 warnings. Latest full proof: `npm run verify` passed on 2026-06-07 MT with 148 Vitest files / 1126 tests passing and production build passing.
- `npm run track54:browser-smoke` now checks desktop and mobile viewports for `/thesis`, `/thesis?audit=1`, and `/overview`, captures screenshots for each route/viewport pair, rejects short/blank rendered bodies, validates PNG screenshot bytes/dimensions/visual-content stats, exercises the `Toggle theme` control to prove a real UI interaction changes state and restores it, and scroll-probes each required marker to prove it is visibly reachable and not covered by another rendered element. The proof builder is import-safe and covered by `lib/__tests__/track54-browser-smoke.test.ts`, and readiness now rejects legacy route-only smoke proof that lacks desktop/mobile coverage, screenshot files inside the browser-proof directory, screenshot byte-count evidence, PNG screenshot signatures, screenshot dimensions, screenshot visual-content stats, console, forbidden-copy, marker, visible-marker, and theme-toggle evidence for every route. Latest no-write browser/readiness proof refreshed on 2026-06-07 MT: browser smoke proof `2026-06-08T04:07:27.597Z` passed with all markers present, visible, and unobscured; no forbidden public copy; zero console errors; successful theme-toggle interaction on all six route/viewport checks; screenshot byte counts from 48,049 to 201,232 bytes; screenshot dimensions of 1024x768 desktop / 390x844 mobile; and visual stats with at least 37 distinct sampled colors, 0.5933 non-white ratio, and 159.2 luma range. Readiness rebuilt from that proof at `2026-06-08T04:27:26.127Z` with `overall_status = hold_manual_review`, `browser_smoke_clean = true`, `production_writes_enabled = false`, 3 completion blockers, daily_pulse still 2/5 clean artifact days with 4 accepted and 3 decision-grade accepted signals, Friday-deep still 0/1, and remaining blockers still Grok credentials/artifact gates.

- Loaded-route, origin, screenshot-identity, and actual-visual proof update (2026-06-07 MT): Readiness rebuilt from the same fresh browser proof at `2026-06-08T04:27:26.127Z` and now requires the proof command to carry `--base-url`, every loaded proof URL to be `http://127.0.0.1` or `http://localhost`, every loaded URL origin to match the command base URL, the path plus query to exactly match the expected route, each route/viewport check to claim a distinct screenshot file, each screenshot filename to match the route/viewport contract, and the referenced PNG itself to decode into nonblank visual-content stats; this run had zero loaded-URL, origin, screenshot-reuse, screenshot-filename, or actual-visual failures and kept production writes disabled.

- Readiness/browser proof freshness display update (2026-06-07 MT): The local Track 54 readiness snapshot now exposes sanitized readiness-report age plus browser-smoke proof timestamp, age, and six-hour freshness status without raw command output, screenshot paths, artifact paths, or hashes. Normal `/thesis` and audit `/thesis?audit=1` now distinguish fresh, stale, future-dated, missing browser proof, and present-but-missing-timestamp readiness/browser proof; stale or future-dated readiness gate counts are labeled as last-known proof instead of current proof; and stale ready-for-approval mode badges stay lagged instead of green. The thread heartbeat summary now also emits `report_freshness_status` and `browser_smoke_proof_freshness_status`, and the live `track-54-promotion-review` heartbeat prompt is manifest-audited for those fields, so automation JSON distinguishes fresh, stale, future-dated, missing proof, and timestamp-missing proof instead of collapsing every false freshness boolean into one operator blocker.

## Next action
1. Let the active no-write Codex windows collect the June 9 through June 12 `daily_pulse` artifacts. Hermes terminal fallback is available, but only through the scheduled prompt bridge or deterministic artifact-health path when a same-day artifact is missing, invalid, parse-failed, identity-mismatched, or lacks fresh price proof.
2. Refresh original Grok auth with `grok login` or add `XAI_API_KEY` to `.env.local` before any write-mode approval, then run the no-write `npm run track54:recover-after-grok-login`.
3. Use `npm run track54:readiness`, then `npm run track54:heartbeat-summary` as the Track 54 operator check after each artifact window. The readiness wrapper runs browser smoke, feeds the proof to the readiness builder, and persists `scratch/track54-readiness/latest-readiness-report.json`; use `npm run track54:readiness:build` only to rebuild with the default existing proof, or call `npx tsx scripts/build-track54-readiness-report.ts --browser-smoke-proof <path> --out <path>` when an automation or script supplies custom proof. When custom ports/paths are passed through npm on Windows, confirm the saved browser proof loaded the intended `base-url`; the wrapper now recovers npm 11 stripped values, but the proof URL is the final authority. Let the active dry-run Codex jobs collect five weekday `daily_pulse` artifacts and one Friday `friday_deep` artifact; use the quiet aliases `npm run track54:artifact-health:daily`, `npm run track54:artifact-health:both`, `npm run track54:artifact-health:daily-retry`, or `npm run track54:artifact-health:both-retry` for same-day health/retry details, and `npm run grok:x-scout:review-week` / `npm run grok:x-scout:review-week -- friday_deep` only when you need the lower-level gate details.
4. Keep Grok write-mode routines disabled during that review; readiness must show `write_automation_checks` safe/missing/inactive for `grok-x-scout-daily`, `daily-thesis-review`, and `grok-x-scout-friday-deep`. Pause only Grok routines if quality is poor, not official collectors or price collection.
5. If Grok CLI output becomes brittle, keep `--runner auto` and add/repair `XAI_API_KEY` so the scout uses the xAI API path without changing the no-write/promotion gate.
6. Keep `/thesis` and `/overview` V1 public scope to source-backed rows only: Corn, Soybeans, Wheat, Durum/Amber Durum, Canola, Barley, and Oats. Do not expand to Peas, Lentils, Flaxseed, Rye, Mustard Seed, Canaryseed, Chick Peas, Sunflower, Beans, US rice/cotton, global commodity boards, Kalshi, or new weather/social/local-cash sources while shipping the X scout lane.

## Recent milestones (rolling 30 days)
- 2026-06-01: Added deterministic Track 54 artifact health automation. `scripts/run-track54-artifact-health-check.ts` powers `npm run track54:artifact-health` and the live Codex health-check prompts; it reviews same-day `daily_pulse` and/or `friday_deep` artifacts, retries only missing or structurally invalid dry-run/no-write artifacts, refreshes browser smoke/readiness through the top-level `npm run track54:readiness` wrapper when configured, and leaves low signal counts to the artifact-week quality gate instead of rerunning endlessly. The persisted readiness report now confirms the live health-check prompts contain the exact deterministic command, `codex_automation_prompts_clean = true`, `codex_automation_missing_prompt_fragment_count = 0`, and production writes remain disabled.
- 2026-06-01: Made the human-facing Track 54 readiness command self-contained. `npm run track54:readiness` now runs browser smoke first, feeds `scratch/track54-browser-smoke/browser-smoke-proof.json` into the readiness builder, and writes `scratch/track54-readiness/latest-readiness-report.json` for the heartbeat summary; use `--browser-smoke-proof-out <path>` when a script needs to run fresh smoke to a custom proof path, use `npm run track54:readiness:build` only to rebuild from the default existing proof, and call `npx tsx scripts/build-track54-readiness-report.ts --browser-smoke-proof <path> --out <path>` directly for custom proof reuse.
- 2026-06-01: Hardened Track 54 date defaults to the Mountain-time automation calendar. `scripts/run-grok-x-scout.ts` and `scripts/review-grok-x-scout-artifact-week.ts` now default to `America/Edmonton` dates instead of UTC dates, so a late Monday operator review no longer projects Tuesday's local dry-run as already skipped. Focused artifact-week/readiness tests passed, and the persisted heartbeat now lists daily next eligible dry-run dates as `2026-06-02`, `2026-06-03`, `2026-06-04`, and `2026-06-05` while production writes remain disabled.
- 2026-06-01: Re-ran the current Track 54 operator check from Codex after adding health-check manifest enforcement and Grok CLI preflight proof. Grok CLI `0.2.14` daily-pulse dry-run evidence remains clean but incomplete; fresh browser smoke proof generated at `2026-06-01T19:46:33.437Z` passed for `/thesis`, `/thesis?audit=1`, and `/overview`; readiness passed its focused Track 54 slice and remains `blocked_by_artifact_gates` only: daily-pulse is 1/5 clean artifact days, Friday-deep is 0/1, weekday and Friday artifact health-check manifests are active/proven, and production writes remain disabled.
- 2026-06-01: Updated Codex thread heartbeat `track-54-promotion-review` to run Mon-Fri at 5:30 PM MT. It wakes this same goal thread after the dry-run evidence jobs: Mon-Thu it reviews daily_pulse no-write evidence and write-mode safety; Friday it also runs the mode-scoped no-write promotion review before any human approval decision. Its summary now includes `local_review_date`, `local_review_weekday`, `local_review_kind`, `review_modes_to_inspect`, selected artifact hash/path, write-mode proposal IDs, post-approval dry-run-to-write transition, and browser-smoke proof age/freshness when present, so the heartbeat can name the local review type, Grok artifact, gated handoff, and whether UI smoke proof has aged out. It verifies write-mode automations remain missing/inactive and still requires explicit Kyle approval before any production Grok write routine can be enabled. Created active workspace cron `track-54-daily-artifact-health-check` for Mon-Thu at 4:45 PM MT to inspect the daily-pulse dry-run artifact, retry only the no-write scout if the same-day artifact is missing/invalid, and report artifact health without writing Supabase rows. Created active workspace cron `track-54-friday-artifact-health-check` for Fridays at 5:15 PM MT to inspect both final-day `daily_pulse` and `friday_deep` artifacts, retry only no-write scout runs if either is missing/invalid, run fresh browser smoke/readiness, and report the exact blocker before the Friday promotion review. Readiness now verifies both health-check schedules/workspaces/retry commands/no-write prompt boundaries and verifies the heartbeat is active, scheduled, thread-targeted, no-write, and approval-gated.
- 2026-06-01: Implemented the Track 54 repo-side flow for daily prices, quarantined X scouting, deterministic X signal validation, daily bounded review, Friday accepted-signal bundle, desk-prompt integration, and `/thesis` X Pulse Watch. Routines are documented as proposed only; Grok weekday writes stay disabled until migration/application and one manual artifact week are complete.
- 2026-06-01: Applied Track 54 Supabase schema live and completed first live Grok/X write proof. Migrations `20260601041000`, `20260601041100`, `20260601041200`, and `20260601052000` are in migration history; Grok CLI `0.2.14` headless JSON works; daily pulse run `f74c7b7c-fb69-4db1-aaa6-5b36ebe2bde8` wrote two accepted X signals verified through X API; daily review wrote CAD Wheat trajectory row `482` using `opus_review_daily_pulse`; a retry dry-run skipped the already-applied daily pulse; thesis cache force-refresh produced 12 packets with 0 failures. Daily Grok routines remain manual-only until one artifact week passes quality review.
- 2026-06-01: Completed Track 54 final verification: focused Vitest passed 69 tests across 9 files, lint passed with 0 errors / 71 existing warnings, production build passed, source-freshness watchdog passed with 12 cache packets and complete MB/SK/AB Prairie crop-progress package, price dry-run fetched 9 tracked contracts, Grok CLI manual dry-run succeeded with fresh price snapshot, Friday X bundle built, browser smoke for `/thesis`, `/thesis?audit=1`, and `/overview` had no console errors or parked crop rows, and `git diff --check` had no whitespace errors. The later readiness proof slice now covers 18 Track 54 files / 115 tests, including readiness-report, Grok CLI preflight hold, artifact-week, promotion-brief, scout-contract, output-merge, write-gate, migration/RLS contract, stale browser-proof rejection, and retired-pipeline tombstone coverage.
- 2026-06-01: Added the Codex automation gate for Track 54. `canola-price-and-fx-freshness-import` is now active as the Mon-Fri 3:45 PM MT official price/cache refresh. New active automation `grok-x-scout-artifact-week-review` runs Mon-Fri 4:10 PM MT in dry-run/no-write mode and reports the artifact-week quality verdict. Added `scripts/review-grok-x-scout-artifact-week.ts`, `scripts/build-grok-x-scout-promotion-brief.ts`, `npm run grok:x-scout:review-week`, `npm run grok:x-scout:promotion-brief`, focused tests, and timestamped same-day scout rerun artifacts to preserve DB audit hashes. The artifact-week reviewer now requires at least 5 clean artifact days and at least 1 decision-grade accepted signal before returning `candidate_for_enablement`, verifies `dry_run = true`, `write = false`, `scout_run_id = null`, parsed artifact `run_date`/`mode`, and parsed validation counts before claiming quality/no-write proof. The registered Codex automation prompt now reports decision-grade accepted counts, artifact identity mismatch days, write-mode evidence days, missing no-write-evidence days, and summary-count mismatch days. X scout run metadata now stores rejected reason summaries for future write runs, and `/thesis?audit=1` displays those reasons alongside accepted-signal allowed/blocked claims. Current day-1 dry-run gate is clean but incomplete: 3 accepted signals, 2 decision-grade accepted signals, 0 rejected, fresh price context, 0 accepted unlisted handles, 0 write-mode artifact days, 0 missing no-write-evidence days, 0 summary-count mismatch days, and 0 artifact identity mismatch days. Production Grok write routines and daily thesis review writes remain disabled until the manual artifact week earns human approval.
- 2026-06-01: Closed the accepted-signal decision-tag gap in Track 54. X signal validation now derives controlled `affected_decisions` tags from category, seasonal phase, and direction; accepted writes persist those tags instead of an empty array, include the primary grain in affected grains, dedupe affected regions, and reject otherwise valid signals that omit affected-region evidence. Focused X signal tests passed; broad `npx tsc --noEmit --pretty false` still fails on unrelated existing test-fixture/type debt.
- 2026-06-01: Tightened X scout search-window validation. Daily pulse posts outside the one-day window, Friday deep posts outside seven days, and future-dated posts now reject before they can become accepted `x_market_signals`.
- 2026-06-01: Tightened X numeric-claim handling and daily-review admission. X signal metadata now carries the required `corroboration` object, numeric X claims reject unless Grok flags them for official verification, and daily/Friday handoffs preserve corroboration plus allowed/blocked claim evidence. Daily thesis review now creates soft trajectory decisions only from accepted `search_mode = pulse` signals with `scout_run_id` provenance; Friday-deep or legacy/provenance-missing X rows cannot become weekday trajectory ticks.
- 2026-06-01: Added executable Friday desk prompt contract proof for Track 54. CAD and US desk prompts now have tests requiring the accepted X bundle command, complete X signal metadata checks, X-as-untrusted-evidence wording, official-source/price precedence, `llm_metadata.x_signal_bundle_audit`, Viking knowledge access, and explicit "Grok never writes/ranks/authors" boundaries.
- 2026-06-01: Hardened Friday X bundle admission. The bundle builder now drops current-week rows that lack the Friday desk evidence seal: post URL, post date, source tier, affected-region context, allowed/blocked claim arrays, explicit verification flag, and valid corroboration when present. Legacy rows missing only the newer corroboration block are normalized to `0` matching claims / no official match, so the CAD/US desk can see them only as uncorroborated review leads instead of facts. Bundle counts now separate desk-ready accepted signals from `excluded_missing_evidence` rows and `legacy_missing_corroboration` rows.
- 2026-06-01: Fixed Friday X bundle week resolution and run provenance. The resolver now chooses the newest CGC crop year before the highest grain week, preventing prior-year week 52 from masking current-year week 42 accepted signals. Friday X bundles attach scout-run context only when the run matches the requested bundle mode, so a `friday_deep` desk packet cannot inherit rejected-count context from a newer `daily_pulse` run. Each bundled signal also carries its own `scout_run_id` and `search_mode` for per-signal chain of custody. Older accepted rows with empty `affected_decisions` now receive deterministic derived tags at read time.
- 2026-06-01: Hardened the xAI API fallback for Track 54. The `xai_api` runner now loads `.env.local`, searches all trusted-handle batches instead of only the first 20-handle group, and merges/dedupes strict scout JSON outputs before validation.
- 2026-06-01: Added executable tombstone proof for the retired Grok/xAI thesis-writing path. `/api/pipeline/run` GET/POST now has focused regression coverage requiring HTTP 410 `grok_workflow_deprecated`, and the shared Supabase Edge Function gate test proves the removed `ALLOW_V1_GROK` escape hatch cannot re-enable stale functions such as `search-x-intelligence` or `generate-intelligence`.
- 2026-06-01: Hardened the future write-mode Grok routine command. `scripts/run-grok-x-scout.ts --write` now resolves the latest CGC grain week from `cgc_observations` when `--grain-week` is omitted, while still allowing an explicit override. `scripts/run-grok-x-scout.ts --write` and `scripts/run-daily-thesis-review.ts --write` now also require the Track 54 approval phrase through `--approval-phrase` or `TRACK54_WRITE_APPROVAL` and re-run the artifact-week reviewer before writing, preventing accidental writes before the promotion brief is a real candidate and human approval is given. Runtime write commands cannot lower the five-day artifact requirement or switch the reviewed artifact root/mode through environment overrides. The write gate is now mode-scoped: daily-pulse evidence can approve only daily-pulse writers, while `friday_deep` writes require a separate Friday-deep review window. New Track 54 scripts now handle `--help`, and write-mode help examples include the reviewed artifact window instead of showing a phrase-only command. This prevents the proposed post-approval automation command from failing on a missing weekly argument and prevents premature local write runs.
- 2026-06-01: Added the consolidated Track 54 readiness report and wired it into both active Grok dry-run Codex automations. `npm run track54:readiness` now combines the daily-pulse and Friday-deep artifact gates, promotion briefs, local Codex automation manifest checks, artifact automation coverage, write-mode automation negative proof, post-approval dry-run/write-mode handoff proof, live source-freshness proof, focused acceptance tests, browser-smoke completion proof, and plan-level acceptance audit into one no-write JSON verdict. The manifest audit now requires exact expected Codex `rrule` schedules, the Bushel Board repo in `cwds`, and Grok dry-run prompts that refresh readiness through `npm run track54:readiness` with `--browser-smoke-proof-out`, `--out`, and the persisted report path, then report `acceptance_audit.overall_status`, `acceptance_audit.browser_smoke_proof`, `browser_smoke_clean`, `persisted_report_path`, and `completion_blockers`; an old dry-run-only prompt, wrong schedule, wrong working directory, uncovered projected artifact date, unsafe post-approval handoff, or active write-mode Codex manifest before human approval is a manual hold. `npm run track54:heartbeat-summary` now validates the persisted readiness report path/freshness/write-safety for the thread heartbeat and includes local review date/type, review modes to inspect, selected artifact hash/path, write-mode proposal IDs, and post-approval dry-run-to-write transition per mode. Promotion briefs now include machine-readable proposed Codex write-mode automation specs for the daily scout, bounded daily review, and Friday-deep scout, and readiness validates their exact commands, approval windows, workspace `cwds`, approval-only status, and dry-run-disable preconditions; those proposals remain create-after-approval only and do not register routines. Current status is `blocked_by_artifact_gates` / `acceptance_audit.overall_status = incomplete` with production writes hard-disabled.
- 2026-06-01: Promoted `docs/plans/2026-06-01-grok-x-price-daily-thesis-completion-plan.md` as the active Track 54 lane. The plan explicitly keeps Grok quarantined as an X evidence scout, not a thesis publisher, and supersedes the May 27 X signal valuation concept while leaving March Grok pipeline docs as retired history.
- 2026-05-31: Surfaced Canada crop progress in the source freshness spine without expanding public scope. Live Supabase migration `add_canada_crop_progress_freshness` updates `v_source_freshness` for `canada_crop_progress` and inserts active official primary mappings only for admitted V1 Canada lanes: Barley, Canola, Corn, Durum, Oats, and Soybeans. Verification: `v_source_freshness` returns latest period `2026 / AB / 2026-05-26`, 627 rows, `strong`, last run `success`; no Spring/Winter Wheat or generic-Wheat proxy mapping was added. Focused Canada crop-progress unittest, focused thesis tests, lint, and build passed.
- 2026-05-31: Tightened the `/thesis` Top Takeaway logic so the farmer-facing read is confidence-weighted and source-coverage-aware. One-country rows now say `CA only` or `US only` instead of reading like full Canada-US confirmation, while aggregate two-country reads are compared against split-market country-side bull/bear conviction so disagreement cannot hide a strong local signal. The headline cards also show the lead evidence/source behind the named constructive or cautious read. Verification: focused thesis page/board tests passed, scoped ESLint passed, and local `/thesis` returned 200 with the new takeaway copy.
- 2026-05-31: Added row-level action cues to the `/thesis` quick scan and major matrix. Split Canada/US rows get `Open first`; aligned bull/bear rows get confirmed-read cues; one-country rows are labelled as limited context; source gaps warn against inference. Verification: focused thesis page/board tests passed and scoped ESLint passed.
- 2026-05-31: Made the `/thesis` quick-scan legend and Top Takeaway status badges follow visible row statuses. The normal board no longer prints a stale `Mapping needed` legend or `0 mapping gaps` when Spring/Winter Wheat are parked off public V1; it now explains one-country reads when those rows are present. Verification: focused thesis page/board tests passed, lint passed with warnings only, build passed, and browser smoke confirmed no forbidden rows, no stale mapping legend, and clean console.
- 2026-05-31: Made `/thesis` scan and matrix stance bars confidence-scaled per the Bushel Board UX rule (`position = 50 + (target - 50) * confidence`). Verification: focused thesis page/board tests passed and scoped ESLint passed.
- 2026-05-31: Added a Top Takeaway inspection queue that names the first rows to open based on split markets, source gaps, one-country reads, and confirmed reads. Verification: focused thesis page/board tests passed and scoped ESLint passed.
- 2026-05-31: Exposed partial Prairie crop-progress package status in the `/thesis` source-health banner. The board can now show `Source health clean; Prairie crop-progress package is partial` when rendered source rows are strong but `source_runs.metadata.prairie_week_status` is still `partial_prairie_week`, avoiding a false complete-package read. Verification: focused thesis tests passed, lint passed with warnings only, source-freshness watchdog returned OK with `prairie_week_status=partial_prairie_week`, production build passed, and browser smoke showed the partial banner with no console errors.
- 2026-05-31: Tightened the `/thesis` Watch Source Groups KPI so it counts stale/empty/lagged/broken freshness rows and explicitly points partial Prairie completeness to the source-health banner. Verification: focused thesis page/board tests passed, lint passed with warnings only, source-freshness watchdog returned OK, production build passed, and browser smoke showed the revised KPI with no console errors.
- 2026-05-31: Reordered `/thesis` so source health and Top Takeaway come before the compact current snapshot and KPI summary cards. This keeps source-health provenance ahead of the farmer read while preventing snapshot metadata or packet counts from outranking the market call on mobile. Verification: focused thesis page/board tests passed, lint passed with warnings only, source-freshness watchdog returned OK, production build passed, and browser smoke verified source-health-before-takeaway, takeaway-before-snapshot/KPIs, seven V1 rows, no forbidden rows, and clean console.
- 2026-05-31: Fixed Prairie crop-progress package selection so a later province-only rerun cannot downgrade a same-period complete MB+SK+AB package on `/thesis` or the source-freshness watchdog. The board now reports source health clean when the selected package is `complete_mb_sk_ab`, while still surfacing partial package context when the best current-period package is actually partial. Verification: focused thesis page/board tests passed, source-freshness watchdog returned OK with `prairie_week_status=complete_mb_sk_ab`, and browser smoke showed no partial-Prairie warning, seven V1 rows, no forbidden rows, and clean console.
- 2026-05-31: Replaced prominent internal source keys on `/thesis` with farmer-readable labels while preserving raw IDs inside source provenance rows, rewrote the Top Takeaway headline pattern to avoid plural crop grammar errors such as `Soybeans has`, changed the inspection queue from redundant `Open first: Corn (Open first)` copy to reason-backed `Start with: Corn (country split)` copy, and removed trading/advice wording (`trade signal`, `pricing plan`, `price-advice certainty`) from visible thesis copy. Lead evidence and driver cards now show names such as `CGC weekly grain stats`, `USDA Crop Progress`, `USDA Export Sales`, and `USDA WASDE` instead of identifiers like `cgc_observations`. Added a regression test proving a newer partial Prairie crop-progress period still outranks an older complete package, so the board cannot hide a newly incomplete week behind last week's complete run. Verification: 44 focused thesis page/board tests passed, source-freshness watchdog returned OK with `cache_items=12` and `prairie_week_status=complete_mb_sk_ab`, lint passed with 0 errors / 71 existing warnings, production build passed, normal `/thesis` browser smoke confirmed friendly lead-evidence labels, grammar-safe Top Takeaway copy (`The strongest split-market bull pressure is Soybeans...`), reason-backed queue copy (`Start with: Corn (country split), Soybeans (country split), Canola (Canada-only read).`), no trading/advice wording, raw IDs absent from lead evidence, seven allowed rows, no forbidden rows, clean source health, and no console errors, and `/thesis?audit=1` browser smoke confirmed scorecard audit details still render with no forbidden rows and no console errors.
- 2026-05-31: Reconciled the source-freshness watchdog with the hard-gated V1 thesis cache count. The live board now expects 12 cache packets (7 Canada + 5 US) instead of the older pre-prune 21-row target. Verification: script ESLint passed and `npx tsx scripts/check-bushel-source-freshness.ts --summary` returned OK with `cache_items=12`.
- 2026-05-31: Hardened the Canada crop-progress importer for prairie parser drift. Saskatchewan parsing now uses non-layout narrative text and current-report excerpts for the newer wording, and Alberta parsing skips embedded historical rows inside current tables. Added regression tests for Saskatchewan narrative wording/period extraction and Alberta historical-row exclusion. Verification: `python3 -m unittest tests/scripts/test_import_canada_crop_progress.py`, `npm run lint`, and `npm run build` passed.
- 2026-05-31: Completed current-state blocker-fix pass for the Bullish/Bearish board. Live Supabase migration `hard_gate_thesis_packet_cache_v1` hard-gates thesis cache reads/refresh inputs to source-backed V1 rows and pruned smaller Canada crops from `thesis_packet_cache`; live `prediction_scorecard` RLS is enabled with public read-only and service-role write policy. App fixes cleaned proxy/weather wording, V1 overview leakage through `ALL_GRAINS`, and lint/build regressions. Verification: focused thesis tests passed, `npm run lint` passed with 0 errors / 68 warnings, `npm run build` passed, and local browser DOM probes for `/thesis?verify=1` and `/overview?verify=1` showed no forbidden V1 crops and clean console. Full Vitest all-files run still hangs as test-runner/open-handle debt.
- 2026-05-29: Completed Transparent Thesis Rating Model V1 through Task 9. The branch has deterministic scorecard types, source-quality adjustments, domain weights, aggregation, packet-to-domain mappers, board-item scorecards, audit-only UI, and LLM payload guardrails. Verification at handoff: 86 focused tests passed, scoped ESLint passed, `npm run build` passed, normal `/thesis` hid scorecard audit details, and `/thesis?audit=1` showed them.
- 2026-05-26: Registered the full Hermes mechanical collector cron suite as 13 active `no_agent=true` script-only jobs. The suite covers USDA Crop Progress; Manitoba Tuesday plus Wednesday retry; Grain Monitor; Export Sales; MB+SK crop progress; CGC weekly; Producer Cars; Friday all-Prairie crop progress plus retry; CFTC COT; monthly WASDE window; and monthly WASDE archive. Jobs run from `/mnt/c/Users/kyle/Agriculture/bushel-board-app`, wrapper scripts live under `/home/kyle/.hermes/scripts/`, and logs write to `/home/kyle/.hermes/logs/bushel-board-collectors/`. The Alberta missing fallback is deliberately not scheduled automatically; it remains manual-only after Friday retry proof. Added `scripts/check-bushel-source-freshness.ts` plus two Hermes no-agent watchdog cron jobs (`bushel-source-freshness-watchdog-tue`, `bushel-source-freshness-watchdog-mon-wed-fri`) that stay silent when OK and alert on missing due collector runs, failed latest source runs, cache item-count drift, cache lag after source imports, or partial Prairie status after the Friday checkpoint.
- 2026-05-26: Added package-level Canada crop-progress collector wrappers and updated `docs/reference/collector-task-configs.md` for the province-staggered routine cadence: `collect:canada-crop-progress:mb` for Manitoba Tuesday/Wednesday retry, `collect:canada-crop-progress:mb-sk` for the Thursday MB+SK partial bundle, `collect:canada-crop-progress:all` for the Friday full Prairie checkpoint after Alberta metadata advances, and `collect:canada-crop-progress:missing-ab` only for explicit stale/missing Alberta after retry. These wrappers use the existing collector-with-thesis-cache-refresh path and do not add a Vercel cron or Grok pipeline dependency.
- 2026-05-26: Added province-specific source-discovery evidence and `prairie_week_status` to `scripts/import-canada-crop-progress.py`. Manitoba discovery records the latest indexed PDF/date/candidate count, Saskatchewan discovery records report/table links separately, and Alberta discovery records Open Alberta package/resource metadata. New tests cover status calculation and discovery metadata. Dry-runs parsed MB 1 row (`partial_mb_only`), SK 99 rows, AB 72 rows, and all provinces 172 rows (`complete_mb_sk_ab`) without a live Canada re-import; `npm run validate-data-layer`, `npm run build`, and local `/thesis?audit=1` verification passed.
- 2026-05-26: Added row-level `projection_admission.latest_by_commodity` diagnostics to the USDA Export Sales importer, then live re-imported Barley/Oats only. Source run `7dfecb68-b415-4462-9662-9fb2f6bfce49` upserted 61 rows through 2026-05-14 and admitted 0 projection-pace rows: Barley implied pace was 38.182% against 196,000 mt WASDE exports; Oats implied pace was 1.964% against 44,000 mt. Both remain intentionally null-guarded. Thesis cache was force-refreshed to 21 rows with source watermark `2026-05-26T17:17:12.201648+00:00`, and local `/thesis?audit=1` verification showed all 9 V1 rows, no Barley/Oats projection claims, Spring/Winter Wheat no-proxy copy, and clean console.
- 2026-05-26: Verified and documented the province-staggered Canada crop-progress release schedule in `docs/reference/canada-crop-progress-release-schedule.md` and refreshed the next-session handoff at `docs/plans/2026-05-26-bushel-board-handoff.md`. Manitoba is Tuesday with Wednesday retry, Saskatchewan is Thursday, Alberta is Friday after the official ~1:30 PM MT release target, and thesis automation must treat Prairie crop progress as partial until Alberta lands or is explicitly stale/missing. After the Export Sales/Barley/Oats diagnostic audit, the next code patch is Canada crop-progress automation, not UI-side projection inference or scope expansion.
- 2026-05-25: Corrected USDA ESR commodity codes in `scripts/import-usda-export-sales.py` after root cause showed Corn/Soybeans/Barley/Oats were using wheat-class codes (`104/201/101/105/108`) rather than the public ESR catalog (`401/801/301/601/701`). Added a focused Python unittest to lock the code map. Live re-imported Corn/Soybeans/Barley/Oats for market year 2026 and force-refreshed thesis cache. Corn and Soybeans now have importer-admitted guarded WASDE projection pace in cached US packets; Barley and Oats remain null-guarded because their implied paces still fail the 60–140% guardrail.
- 2026-05-25: Resolved the Spring/Winter Wheat mapping decision for V1: both rows remain visible, explicit `Mapping needed` placeholders. Tests prove generic Canada/US Wheat packets are not aliased into Spring Wheat or Winter Wheat rows; browser audit of `/thesis?audit=1` showed both rows with `CA: class mapping pending`, `US: class mapping pending`, `Mapping pending`, and `Generic Wheat is not used as a proxy`, with a clean console.
- 2026-05-24: Completed V1 Canada+US source-sufficiency audit at `docs/plans/2026-05-24-v1-source-sufficiency-audit.md` and completed the first two follow-up fixes. May 2026 WASDE was available, imported for Corn/Soybeans/Barley/Oats/Wheat, source freshness now reports `usda_wasde_raw` as `2026-05-01` / 2,112 rows / strong, and thesis cache was force-refreshed to 21 rows with watermark `2026-05-24 18:02 UTC`. Cached US packets expose May-vs-April WASDE revision deltas for Corn/Soybeans/Wheat/Barley; Oats 2025 remains on April because the May PSD response had no 2025 Oats rows, while 2026 Oats rows are present. Also patched `scripts/import-usda-wasde.py` to use the active Python executable for heartbeat writes after WSL lacked a `python` binary. Canada crop progress is now wired into `get_canada_thesis_packet().supply.canada_crop_progress`, source freshness includes `canada_crop_progress`, cache refresh verified mapped Canada packets for Amber Durum, Barley, Canola, Corn, Oats, and Soybeans; generic Wheat remains unmapped to avoid silently aliasing Spring/Winter classes. Remaining gates are Spring/Winter Wheat class mapping and guarded Export Sales + WASDE projection admission expansion beyond Wheat. Earlier in the audit, ran `ANALYZE` on small source tables so freshness row estimates correctly show `usda_quarterly_stocks` as 47 rows / strong instead of false empty.
- 2026-05-23: Verified `/thesis?audit=1` after Export Sales + WASDE projection admission. Wheat shows the guarded `Export sales outrunning WASDE projection` driver at 102.3% of WASDE export projection; Corn, Soybeans, Barley, and Oats do not show projection-pace claims because their guarded fields remain null. Polished cached packet freshness normalization so source rows with latest periods do not display false `empty`/`Build or seed this source` warnings from stale row-count estimates. Verification: browser audit with clean console, focused `thesis-board` Vitest via `npx vitest run lib/__tests__/thesis-board.test.ts --maxWorkers=1 --no-file-parallelism --reporter=dot`, `npm run validate-data-layer`, and `npm run build` passed locally.
- 2026-05-22: Applied US WASDE month-over-month revision context live and refreshed thesis cache. Local migration `20260522151000_add_wasde_revision_context_to_us_thesis_packet.sql` was applied as live migration `20260522183715_add_wasde_revision_context_to_us_thesis_packet`; cache refresh succeeded for 12 source-backed rows with 21 total cache rows. Live cached US Corn/Soybeans/Wheat 2025 packets now expose previous-report WASDE revision fields, including `ending_stocks_change_kt` and `exports_change_kt`. Browser audit of `/thesis?audit=1` confirmed fresh cache status, all 9 V1 rows, visible WASDE revision driver copy, and clean console. Earlier local verification passed focused `thesis-board` Vitest, scoped ESLint, and `npm run build`.
- 2026-05-22: Added USDA acreage context to the US thesis board. Existing `crop_acreage_estimates` national rows now appear in thesis freshness as `crop_acreage_estimates`, cache rows include `supply.acreage`, and `/thesis` planting-progress drivers show progress against actual planted-acre bases. Verification: live REST checks, focused `us-acreage` + `thesis-board` Vitest suites, scoped ESLint, and `npm run build` passed.
- 2026-05-22: Visually audited and polished `/thesis` after quarterly-stocks drivers landed. Added a source-health/provisional-use banner above the farmer read, moved `Top takeaway` before the all-grains quick scan, added an explicit quick-scan legend, and corrected missing-market copy so Durum/Canola US cells do not imply a broken source mapping when the V1 board intentionally has no matching US overview lane. Verification: browser visual audit, scoped ESLint, focused `thesis-board` Vitest, and `npm run build` passed.
- 2026-05-22: Integrated USDA Quarterly Grain Stocks into the live US thesis packet/cache. Migration `20260522035638_wire_usda_quarterly_stocks_into_us_thesis` is applied; `get_us_thesis_packet()` emits `supply.quarterly_stocks`, `get_thesis_data_freshness()` reports `usda_quarterly_stocks`, `buildUsThesisBoardItem()` can surface measured stocks-surprise supply drivers, and `thesis_packet_cache` was force-refreshed to 21 rows with 5 US quarterly-stocks payloads. Verification: live RPC/cache SQL checks, focused `thesis-board`/quarterly-stocks Vitest suites, scoped ESLint, and `npm run build` passed.
- 2026-05-22: Added USDA Quarterly Grain Stocks as a tested US data-spine source. Live Supabase migration `20260521184630_create_usda_quarterly_stocks` is applied; `public.usda_quarterly_stocks` has 47 imported rows from 2024+ NASS QuickStats for BARLEY, CANOLA, CORN, OATS, SOYBEANS, and WHEAT. The importer handles `FIRST OF ...` reference periods, filters wheat subclass rows so all-wheat stocks are not overwritten by Durum, converts bushel and pound units correctly, writes `source_runs`, and has 8 focused Vitest tests. Verification: focused Vitest, scoped ESLint, and `npm run build` passed locally.
- 2026-05-17: Audited `/thesis` against the V1 major-grain lane list. Visible rows are exactly Corn, Soybeans, Wheat, Spring Wheat, Winter Wheat, Durum, Canola, Barley, and Oats; excluded grains/US rice/cotton/Kalshi do not appear. Polished stance language so modest non-zero scores show as `Lean bull` / `Lean bear` instead of contradictory balanced copy, renamed the top summary from Markets to Source Packets, added a score/confidence guide, and added a farmer-read `Top takeaway` card above the matrix. Wheat-class placeholder rows now label as `Mapping needed` instead of `Country split`. Console was clean, focused tests passed, and `npm run build` passed locally.
- 2026-05-17: Patched collector-triggered thesis-cache refresh behavior from the USDA export-sales repair. `scripts/run-collector-with-thesis-cache-refresh.ts` now force-refreshes thesis packets after successful collectors so stale cache watermarks cannot hide newly imported source data; dry-run collectors still skip refresh. `refresh-thesis-packet-cache.ts` defaults to the V1 source-backed board grains only, and focused wrapper/thesis-board tests plus `npm run build` passed locally.
- 2026-05-16: Repaired stale USDA export-sales source data and refreshed thesis packets. Live Supabase now has export sales through `2026-05-07` for ALL WHEAT, CORN, SOYBEANS, BARLEY, and OATS; `thesis_packet_cache` was force-refreshed to 21 rows. Next product focus is narrowed to the Bullish/Bearish first major-lane board only: Corn, Soybeans, Wheat, Spring Wheat, Winter Wheat, Durum, Canola, Barley, and Oats.
- 2026-05-10: Canola predictive harness Patch Set 2G added a CGC historical replay input builder and first real public-data replay bundle from `data\CGC Weekly\gsw-shg-en.csv`. The tracked bundle covers 2025-2026 Weeks 1-3, produced 0 historical training candidates and 3 review-only revision-tainted labels, and rebuilds to package hash `sha256:00d5b0053a99929343355a77ef41af66012952bbe4c3bd20cd46340ee1cf5122`. The builder handles quoted CGC numbers, requires explicit timing assumptions, records estimated publication availability assumptions, and keeps historical candidates separate from forward calibration. Gemini correctly flagged annual-CGC revision risk and fake-thesis-verdict overclaim; the bundle is now review-only unless point-in-time source certification is explicit.
- 2026-05-10: Canola predictive harness Patch Set 2H added point-in-time CGC weekly snapshot capture. `npx tsx scripts/capture-cgc-weekly-snapshot.ts --output-dir "data\CGC Weekly Snapshots"` captured the current public CGC CSV as local ignored artifacts for 2025-2026 Week 39, week ending 2026-05-03, with raw CSV hash `sha256:6d75c3b7fda56997416ce0234228809a63325bfa30538a916aa1f03484d20c2a`. A second run returned `already_current`, proving idempotency. This is the first clean point-in-time source capture for future historical replay.
- 2026-05-10: Created active Codex Automation `cgc-weekly-point-in-time-snapshot-capture` for Thursdays at 1:25 PM MT. It captures the as-published CGC weekly CSV before the 1:35 PM importer, reports hashes/paths, and keeps raw snapshots local-only and ignored by git.
- 2026-05-10: Opened Phase 3 handoff for a multi-grain weekly thesis harness at `docs/plans/2026-05-10-multi-grain-weekly-thesis-harness-handoff.md`. The operating decision is one shared engine plus canonical 16-grain profiles, with Gemini as adversarial reviewer and Codex as final judge. Early-week reads and midweek updates must not overwrite the Friday frozen thesis artifact.
- 2026-05-10: Canola predictive harness Patch Set 2F added a local historical replay training-candidate harness. It builds deterministic no-write replay packages from point-in-time source rows, next-window public evidence, and outcome labels; separates `historical_training_candidate` from `forward_calibration_candidate`; blocks future/forbidden/already-known evidence; taints model-assisted labels when the model cutoff can include later outcomes; and preserves review-before-training. Gemini warned to keep historical replay separate from forward calibration and to enforce review-window/source-boundary checks; those checks are now in code and tests.
- 2026-05-10: Canola predictive harness Patch Set 2E added the first real frozen bull/bear thesis from the Week 38 source artifact. The prompt pack hash is `sha256:d368cefe0bc578826793c5ec6a34b712389b0098d54695a713a28110a230af72`; the run artifact hash is `sha256:ce840bc5aae95129cb7cab67b3bdf18fb736ed1c52359a93de63a841d9edc56a`. The forecast is bullish with confidence `61`, stance score `32`, recommendation `WATCH`, and explicit model training cutoff `2024-06-01`. It remains a frozen forecast only: no next-week review, no training candidate, no sidecar write, no production write, no model/API runner, and no dashboard connection. Gemini blocker audit returned no blockers.
- 2026-05-10: Canola predictive harness Patch Set 2D added a local market-read source-row exporter and the first non-synthetic frozen source artifact at `docs/reference/forecast-experiments/canola-forward-week-38-2026-05-10/`. The tracked artifact has 39 admitted rows, redacts unadmitted/forbidden omitted lanes, builds a strict snapshot hash `sha256:784465b816f64a7cb3fd6a740c47ba0b678dba9dec0d151db175d379d50a89a6`, and still contains no model output, no training candidate, no sidecar write, and no dashboard connection. Gemini audit found and rechecked fixes for omission-name leakage, current-table replay overclaim, and artifact misuse. Added a lessons-learned entry for pretraining/source-boundary guardrails.
- 2026-05-10: Canola predictive harness Patch Set 2C added the first tracked walk-forward fixture bundle at `docs/reference/forecast-experiments/canola-walk-forward-week-38/`. The bundle contains synthetic source rows, frozen thesis JSON, next-week evidence, completed review labels, generated local workflow output, review prompt pack, and review package. Added `test:forecast:walk-forward-fixture` to prove the tracked outputs rebuild deterministically and preserve accepted/blocked evidence gates. Gemini flagged wording that could let a future agent mistake the fixture for proof; the package now classifies fixture provider/model runs as `review_only_fixture`. The fixture is not a public market-performance claim, model-output trace, training proof, live Supabase output, or price-skill proof.
- 2026-05-09: Canola predictive harness Patch Set 2B implemented as a local thesis-review prompt-pack builder. Added deterministic frozen-run + next-week-evidence -> reviewer-prompt packaging, accepted/blocked evidence filtering, allowed evidence keys, required driver indexes, review JSON response contract, CLI, package scripts, and focused tests. Updated Codex automation `canola-predictive-harness-no-write-review` to run the prompt-pack lane before completed review packaging when artifacts are present. Gemini review flagged one wording gap around price-only logic; the prompt now explicitly forbids it. No model/API calls, Supabase reads, price-history dependency, sidecar writes, SQL execution, Hermes automation, dashboard imports, or production integration were added. `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Patch Set 2A implemented as a local weekly thesis review package builder. Added deterministic frozen-thesis -> next-week-evidence review packaging, accepted/blocked evidence windows, driver review labels, missed-signal adjustments, and forward-calibration candidate classification that does not require complete grain price history. Updated Codex automation `canola-predictive-harness-no-write-review` to treat thesis review as the primary learning loop and price scoring as optional. No model/API calls, Supabase reads, sidecar writes, SQL execution, Hermes automation, dashboard imports, or production integration were added. Gemini review returned no blockers; `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Superseded the Hermes automation path for the Canola predictive harness. Created active Codex automation `canola-predictive-harness-no-write-review` for a Friday no-write review gate after CFTC publication and before the weekly desk swarm. The automation runs forecast tests/build guardrails and static scans only; it must not call model APIs, write Supabase, write sidecar tables, edit dashboard/source files, use private farmer/operator/chat data, or use Hermes.
- 2026-05-09: Canola predictive harness completion audit passed and the thread goal was marked complete. Verified current branch state, PR #13, Vercel status, full forecast harness test coverage, production build, guardrail scan, and review-gate preservation. The completed local workflow covers source rows -> source records -> snapshot -> prompt pack -> manual model output -> run artifact -> sidecar package -> outcome score package, with no production write path.
- 2026-05-09: Canola predictive harness Patch Set 1I implemented as a local outcome score package builder. Added deterministic sidecar-package -> score-row mapping, package tamper checks, outcome timing validation, Brier score / magnitude error / confidence bucket output, CLI dry-run no-write behavior, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, SQL execution, Hermes automation, dashboard imports, or production integration were added. Gemini audits returned no blockers; `npm run test:forecast:score-package`, `npm run test:forecast`, and `npm run build` passed locally. `npx tsc --noEmit --pretty false` still fails on known non-harness test type debt.
- 2026-05-09: Canola predictive harness Patch Set 1H implemented as a local manual model-output runner. Added strict raw JSON / single fenced JSON payload parsing, commentary rejection, prompt-pack horizon/contract binding, deterministic raw-output/run-result hashing, CLI dry-run no-write behavior, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, SQL execution, Hermes automation, dashboard imports, or production integration were added. Gemini final constrained re-audit returned no blockers; `npm run test:forecast:manual-runner`, `npm run test:forecast`, and `npm run build` passed locally. `npx tsc --noEmit --pretty false` still fails on known non-harness test type debt.
- 2026-05-09: Canola predictive harness Patch Set 1G implemented as a local sidecar review package builder. Added deterministic workflow -> sidecar-row package mapping, nested hash tamper checks, review-required/no-write guardrails, CLI dry-run no-write behavior, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, SQL execution, Hermes automation, dashboard imports, or production integration were added. Gemini audits returned no blockers; `npm run test:forecast:sidecar-package`, `npm run test:forecast`, and `npm run build` passed locally. `npx tsc --noEmit --pretty false` still fails on known non-harness test type debt.
- 2026-05-09: Canola predictive harness Patch Set 1F implemented as a local dry-run workflow orchestrator. Added deterministic source-records -> snapshot -> prompt-pack -> run-artifact chaining, workflow hashing, CLI dry-run no-write behavior, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, Hermes automation, dashboard imports, or production integration were added. Gemini audits returned no blockers; `npm run test:forecast:local-workflow`, `npm run test:forecast`, and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Patch Set 1E implemented as a local prompt-pack builder. Added deterministic prompt hashing, model-training-cutoff requirement, snapshot hash/content verification, strict supplied-snapshot-only prompt text, local CLI, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, Hermes automation, dashboard imports, or production integration were added. Gemini final re-audit returned no blockers; `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Patch Set 1D implemented as a local run-artifact builder. Added deterministic snapshot+forecast artifact hashing, snapshot/forecast field matching, forecast evidence-clock cutoff checks, model-training-cutoff future-taint blocking, local run-artifact CLI, focused tests, and package scripts. No model/API calls, Supabase reads, sidecar writes, Hermes automation, dashboard imports, or production integration were added. Gemini final re-audit returned no blockers; `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Patch Set 1C implemented in local-export mode after Kyle gave standing approval for future phases. Added source-record conversion, a mockable read-boundary adapter, local source-record CLI, source-record/read-adapter tests, and package scripts. Guardrails cover missing/future clocks, mutable-row ingestion/update proof, future observed periods, timezone-less clocks, forbidden source families, grain price cutoff/market-close handling, untrusted contract mappings, and `canola_market_read` cutoff support. Gemini final re-audit returned no blockers; `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Phase 1C approval brief added and pushed. It is a review gate only: no read adapter, live Supabase reads, sidecar writes, model calls, Hermes automation, dashboard work, or production integration were added. Gemini 3.1 Pro Preview reviewed the brief; its blockers around mutable-row leakage, observed-period timing, timezone normalization, read-only client risk, and current-state helper leakage were incorporated.
- 2026-05-09: Canola predictive harness Phase 1B deterministic snapshot builder implemented locally. Added stable canonical hashing, source-clock freshness, future `available_at` / `published_at` blocking, revision-taint marking, blocked forecast/score source keys, local-file CLI, focused CLI/compiler tests, and package scripts. Gemini final re-audit found no blockers; `npm run test:forecast` and `npm run build` passed locally.
- 2026-05-09: Canola predictive harness Phase 1A and 1B review gate landed. Phase 1A committed sidecar-only experiment tables under `experimental`, strict `canola-forecast-v1` validation, pure scoring helpers, focused tests, and `test:forecast`; `npm run test:forecast` and `npm run build` passed locally. Phase 1B approval brief was Gemini-reviewed and now waits for Kyle approval before any snapshot-builder implementation.
- 2026-05-08: Built Bull/Bear Thesis Board V1 on the cached Canada/US thesis packet spine. `/thesis` renders cached facts-only packets, nav links expose the tab, `thesis_packet_cache` is live and warmed, collector wrappers refresh cache after successful source imports, and Gemini 3.1 Pro Preview audited the wrapper failure modes.
- 2026-05-08: Expanded Seeding V1 with public USDA/Canada seeding progress, spring-wheat pulse drilldown, planted-acre badges, and prior-year progress context. Overview now includes source-backed delivery bins and price/source freshness improvements.
- 2026-05-06: Hardened the FX CAD recalculation RPC grant boundary. Migration `20260506180718` is applied live and restricts `public.recalculate_grain_prices_cad(date, date)` execution to `service_role`; live grant verification shows only `postgres` and `service_role` have `EXECUTE`.
- 2026-05-06: Fixed the Canada thesis packet supply-row selector. Migration `20260506181223` is applied live and makes `get_canada_thesis_packet('Canola','2025-2026', ...)` prefer the requested packet crop year before falling back to older balance-sheet context. The deterministic Canola read now shows 2025-2026 production, seeded acres, harvested acres, yield, and 2026 intended seeded acres from the live packet.
- 2026-05-05: Implemented and live-verified the Canola source-admission automation pass. New live paths cover Producer Cars rail staging, CFTC COT positioning, StatsCan final/intended-acre baseline, price/FX freshness, and Canola Council Markets & Stats inventory. Codex automations now refresh those sources plus Grain Monitor weekly logistics on expected cadences, and `source_runs` has successful rows for the live-verified lanes.
- 2026-05-04: Documented the next Canola source-admission pass. The source registry now adds Canola Council Markets & Stats as a Canola-specific aggregator, records first public baseline facts to seed (2025 final production/yield/seeded/harvested acres and 2026 intended seeded acres), and adds a paste-ready next-session handoff at `docs/plans/2026-05-04-canola-source-admission-handoff.md`.
- 2026-05-04: Normalized the scanned Ferris and Norwood/Lusk grain-knowledge path. `scripts/gemini-ocr-distill.py` now resolves the `raw/Grain Knowledge` folder, runs Gemini CLI without the broken Bash shim path, supports `--force`, and emits `.distilled.json` metadata. Local weak Step-era outputs were archived, live Supabase was re-ingested, and stale legacy knowledge rows/chunks were removed so retrieval sees only current source paths plus normalized redistillations.
- 2026-05-04: Refreshed the Viking knowledge architecture away from retired Grok/xAI assumptions. Advisor prompts now state the current-data boundary explicitly instead of claiming `x_search` access, and the knowledge docs record the quick quality audit of the local Grain Knowledge distillations.
- 2026-05-04: Added `docs/reference/cgc-market-mechanics-v1.md` as the first source-specific relationship and interpretation contract. It defines CGC row identity, country producer-delivery math, export math, terminal grade-summing rules, source/interpretation/speculation boundaries, outside-source requirements, training example format, and a live Canola Week 38 grounding snapshot.
- 2026-05-04: Hardened `scripts/import-cgc-weekly-codex.mjs` for the Thursday CGC routine. The importer now derives crop year from the live CGC CSV instead of a fixed year, attributes post-import verification to a same-run `cgc_imports` row, writes failure summaries to `source_runs` when failures occur outside dry-run, and includes dynamic crop-year evidence in collector heartbeats. Verified with `npm run import-cgc:dry`; no live import was triggered during this hardening pass.
- 2026-05-04: Added `/source-spine` source-watch dashboard and source registry operating precedence. The board now leads with CGC weekly stats, Grain Monitor, producer cars / railcar staging, COT, and AAFC/StatsCan crop-size baseline before the AAFC drought, Agroclimate, VegDRI, SMOS satellite soil moisture, NASA/SERVIR ESI, and future GEE derived drought watchlist. Weather/satellite layers remain watch/proxy/research lanes and are not admitted Canola V1 thesis inputs.
- 2026-05-04: Deterministic Canola Market Read V1 generator built on the live Canola packet RPC. Added `lib/canola-market-read.ts`, `scripts/generate-canola-market-read.ts`, focused Vitest coverage, and `npm run canola-market-read`. The read separates facts, interpretation, speculation, watch items, freshness, quality warnings, and source links without using `market_analysis` prose or an LLM.
- 2026-05-03: Sprint-1 grain-intelligence pivot started locally and Data Layer Foundation migrations applied live. Added source-registry, canonical fact model, canola market-read V1 contract, recovered the missing remote predictive-market migration into local migrations, added freshness optimization, and verified `npx tsx scripts/validate-data-layer-foundation.ts --grain Canola --market Canola` passes.
- 2026-05-03: Data Layer Foundation V1 handoff docs committed and pushed as `cd7bbda` (`Document data layer foundation handoff`).
- 2026-05-03: Data Layer Foundation V1 contracts committed and pushed to GitHub on `codex/data-layer-foundation-v1` as `18a0935` (`Add data layer foundation contracts`). Working tree was clean after push.
- 2026-05-03: GitHub CLI authenticated successfully as `Bushels` using HTTPS. `gh repo view` resolves to `Bushels/Bushel-Board`, default branch `master`, with usable `repo` and `workflow` scopes.
- 2026-05-03: Data Layer Foundation V1 local integration started on `codex/data-layer-foundation-v1`: `source_runs`, `grain_market_mappings`, source freshness, Canada/US thesis packet RPC migrations, importer run-summary hooks, validator script, and seeding-drill price-query contract fix. Local SQL rollback check and `npm run build` passed; live DB push remains blocked by remote migration-history drift.
- 2026-05-02: CGC Week 38 imported via new Codex deterministic importer. Added `scripts/import-cgc-weekly-codex.mjs`, `npm run import-cgc`, Codex automation `cgc-weekly-grain-stats-import`, data-lineage map updates, collector docs, and journal note. Supabase verification: 4,313 Week 38 rows, validation pass, 16 `collector_cgc` heartbeats.
- 2026-05-02: Grok/xAI analysis workflow retired. Tombstoned `/api/pipeline/run` and legacy Grok Edge Functions, removed Grok scripts/adapters, and moved live dashboard reads away from `grain_intelligence` toward `market_analysis`.
- 2026-05-02: Data Layer Foundation V1 plan opened. Supabase check confirmed source data is ahead of thesis data: CGC Week 38 and USDA crop progress through 2026-04-26 are present, while Canada thesis rows are Week 36 and US thesis rows are Grok-written legacy.
- 2026-05-01: MPS portfolio cleanup pass complete - 6 commits (`757e2c5` -> `60413e6`). Anchored gitignore, AGENTS.md rules-only rewrite, PROJECT_STATE.md introduced, baoyu skills promoted to `~/.claude/skills/`, 135K-line `.bak` and Excel-lock junk removed from index, journal scaffolded. See `docs/journal/2026-05.md`.
- 2026-04-30: Grain Monitor parser seatbelt + tiered autonomy charter (`docs/hermes/skills/import-grain-monitor.md`).
- 2026-04-28: Sentiment voting paused; My Farm storage tracker promoted to headline. `LandingPage` retired.
- 2026-04-27: Bushel Board cohesion audit (`docs/plans/2026-04-27-bushel-board-cohesion-audit.md`).
- 2026-04-24: US Desk swarm GA - 8 scouts + 4-5 analysts + meta-reviewer.
- 2026-04-19: Section 3 + Section 4 audits - runtime bugs in swarm orchestration prompts fixed.
- 2026-04-17: Track 45-A (`get_intraweek_trajectory` RPC) + canonical 16-grain DB-name fix-up.

## What's where (truth files)
- `AGENTS.md`, `CLAUDE.md` - rules only.
- `PROJECT_STATE.md` (this file) - current truth, updated when state changes meaningfully.
- `docs/journal/YYYY-MM.md` - append-only history of structural / cleanup events.
- `docs/plans/STATUS.md` - feature track ledger.
- `docs/lessons-learned/issues.md` - bug post-mortems.
