---
category: Brand
---

Section heading with the canola left-accent bar, a Fraunces display `title`,
and an optional muted `subtitle`. Pass `children` to render actions on the
right (the standard top-level section header across the app).

## Examples

```tsx
<SectionHeader title="Market Thesis" subtitle="Where Canola is heading this week" />
```
