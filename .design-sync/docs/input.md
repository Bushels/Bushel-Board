---
category: Primitives
---

Single-line text field on the wheat-themed surface, with focus ring and
`aria-invalid` styling. Standard `<input>` props (`type`, `placeholder`,
`value`, `disabled`, …) pass through.

## Examples

```tsx
<Input placeholder="Tonnes delivered" />
<Input type="email" defaultValue="grower@farm.ca" />
```
