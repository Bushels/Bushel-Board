---
category: Surfaces
---

Recharts custom tooltip — a translucent floating card listing series
color/name/value rows. Pass as a chart's `content`; it renders only when
`active` with a non-empty `payload`. An optional `formatter` formats values.

## Examples

```tsx
<Tooltip content={<GlassTooltip />} />
```
