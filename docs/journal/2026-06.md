# Journal - 2026-06

Append-only history of structural changes, cleanup passes, and other repo-shape events. Day-to-day feature work goes in `docs/plans/STATUS.md`; bug post-mortems go in `docs/lessons-learned/issues.md`. This file only logs things that change the shape of the repo or the operating model.

---

## 2026-06-24 - Wheat historical price context RPC

**Driver:** The Wheat price proof had a useful packet-window read, but farmers still needed a quick visual check of whether Spring Wheat, HRW, and SRW have been confirming the official-data thesis over a broader recent window.

**Repo shape changes:** Added migration `20260624192708_get_wheat_price_history.sql`, which creates bounded public RPC `get_wheat_price_history(integer)` for Spring Wheat, HRW, and SRW only. The RPC clamps requests to 7-180 days, runs as `SECURITY DEFINER`, grants execute to `anon` and `authenticated`, and avoids broad public `grain_prices` table access. Added `lib/queries/wheat-price-history.ts` plus a `Historical price context` panel on `/thesis` under the existing price-basket proof. The panel renders three compact 60-day sparklines and keeps the copy explicit that price history is confirmation, not score authority.

**Live proof:** The live migration was applied to Supabase project `ibgsloyjxdopkvwqcqwh`. Database proof returned 25 Spring Wheat rows, 39 HRW rows, and 39 SRW rows for the 60-day window. An anon-key RPC proof returned 103 total rows, confirming the farmer-facing page can call the function without exposing the raw table.

**Current assessment:** Historical futures context is no longer the price-lane blocker. The next comparable Wheat loop gap is historical export context, plus the spiderweb authority/freshness/replay decision and the still-weak local basis/global-origin lanes.

## 2026-06-24 - Wheat USDA source-sweep panel

**Driver:** The Wheat board needed farmers to consume the official U.S. evidence visually before scrolling into deeper explanation, without reverting to a Canada-vs-U.S. framing.

**Repo shape changes:** `/thesis` now includes a USDA Wheat source-sweep panel below the reconciliation judge. It inventories Crop Progress, WASDE balance, Export Sales, and Quarterly Stocks with source freshness, official cadence, latest row date, weighted point effect, source role, and a relation label such as deciding row, bear pressure, bull support, or cadence-limited context. The panel reuses existing scorecard and freshness rows; it does not add scoring authority. An unfinished untracked Wheat X-sentiment prototype was parked as `docs/plans/2026-06-24-wheat-x-sentiment-ranking-plan.md`; its executable TypeScript/test draft was removed because the prototype scored trusted prairie bearish signals as bullish.

**Current assessment:** This starts the USDA Wheat sweep as a farmer-facing visual inventory. The next Wheat loop should deepen it with live-packet verification, historical price/export context, and source-specific judge language before changing weights or spiderweb distance rules.

## 2026-06-24 - Wheat closeout hygiene and skill consolidation

**Driver:** Kyle asked to close the session cleanly before starting a fresh Wheat goal: read Hindsight, update docs, improve any agents or skills used, and archive anything deprecated enough to interfere with the Wheat Bull/Bear work.

**Repo shape changes:** Rechecked Hindsight memory for Wheat/USDA/Rule 21/Hermes/Grok context and found no matching documents, memories, tags, or mental models. Updated the Wheat handoff and project status to make checked-in repo docs the next-session source of truth. Tightened `grain-report` so the next session starts from the Wheat source sweep and existing lane-agent/reconciliation-judge contract instead of a new mockup or new agent. Tightened `ai-pipeline-v2` into an archive-only retired Grok tombstone and clarified `cftc-cot` so Wheat CFTC positioning remains timing/crowding context, not a primary direction creator.

**Current assessment:** This pass added no new dashboard behavior and no new scoring authority. It reduces next-session ambiguity: start with the full USDA Wheat sweep, then price/export history, source-specific judge language, and the spiderweb distance model.

## 2026-06-24 - Wheat judge and spiderweb visual polish

**Driver:** The Wheat board needed the farmer-facing relationship map to be understandable at a glance before the next session moved into deeper USDA and scoring-replay work.

**Repo shape changes:** `/thesis` now renders the relationship spiderweb as a true node-and-edge map: Wheat centered, bear pressure left, bull support right, numbered nodes, distance rings, and edge thickness tied to weighted score impact. The reconciliation judge now shows source freshness for the deciding datum, states the strongest opposite-side counterweight, and uses farmer-readable freshness labels such as `Older official row` instead of raw status strings.

**Current assessment:** This closes the v1 visual-readability pass for the Wheat relationship map. The next Wheat loop should focus on the full USDA Wheat sweep, historical price/export context, source-specific judge explanations, and whether spiderweb distance should also incorporate source authority, freshness, and replay accuracy beyond weighted score impact.

## 2026-06-24 - Wheat source-freshness closeout

**Driver:** The Wheat board had its first scorecard/judge/spiderweb UI proof, but the next loop still needed official source rows refreshed so the judge would not be explaining stale CFTC/logistics evidence.

**Repo shape changes:** Updated the Wheat handoff, Wheat metric-loop doc, project state, feature status, and project skills with the refreshed source state. `scripts/import-usda-export-sales.py` now accepts `--commodity Wheat` as the documented alias for USDA ESR `ALL WHEAT`, with a focused regression test. `scripts/refresh-thesis-packet-cache.ts` now also recognizes npm-forwarded boolean force flags so cache-refresh operators do not get a skipped run when they intended a forced refresh.

**Live source proof:** Official CFTC SODA showed Wheat contracts available for 2026-06-16; the CFTC collector imported 8 rows and wrote 9 heartbeat rows. Grain Monitor imported week 45 from the 2026-06-23 report. Producer Cars imported week 47, with Wheat at 587 week cars and 14,750 crop-year cars. Thesis packet cache refreshed 12/12 with source watermark 2026-06-24T15:46:49Z. Source freshness watchdog passed with 0 alerts and freshness watch count 15.

**Current assessment:** CFTC, Grain Monitor, producer cars, CGC, crop progress, prices, and WASDE are no longer the confidence cap for the Wheat mechanical spine. Export sales remains at 2026-06-11 until the next USDA weekly release, quarterly stocks remains on its official cadence, and local bids/weather-cache remain empty local lanes.

## 2026-06-24 - Wheat headline scorecard reconciliation

**Driver:** The Wheat board had a hidden mismatch: the farmer-facing weekly headline score could come from bull/bear driver counting while the lane bars came from the deterministic rating scorecard. That made the board harder to trust because two truthful internal reads could disagree on the first screen.

**Repo shape changes:** `lib/queries/thesis-board.ts` now uses the deterministic `ratingScorecard` as the visible Wheat weekly headline source when an exact Wheat packet has populated scorecard domains. Driver counting remains explanation copy, and daily overlays remain the only current-day override. `/thesis` now labels these Wheat weekly reads as `Deterministic scorecard` instead of `Weekly packet`, while daily overlay labels stay visibly separate. Focused regression tests now pin that Wheat Canada and U.S. packet headline scores, labels, and confidence values match the scorecard output.

**Current assessment:** This closes the first score-reconciliation bug. The follow-up UI proof added the first reconciliation judge and three-contract price split, and the later spiderweb polish pass made distance and edge thickness readable for v1. The next Wheat loop should not rework those repairs; it should deepen the USDA/source-specific scoring logic.

**Follow-up UI proof:** Added the first farmer-facing reconciliation layer on `/thesis`: a `Reconciliation judge` card that selects the largest weighted scorecard datum, a `Relationship spiderweb` ring view that groups scorecard lanes by impact distance, and a `Price basket proof` strip for Spring Wheat/MGEX, HRW/KCBT, and SRW/CBOT. The visual layer reads existing scorecard metrics only; it does not add scoring authority. Public-copy guardrails now allow Spring Wheat as a futures contract leg while still blocking separate Spring/Winter Wheat product-lane or mapping claims.

## 2026-06-24 - Wheat price basket scoring repair

**Driver:** The Wheat Bull/Bear closeout identified a scoring bug: the deterministic price mapper used the first/latest price row, so a fresh Spring Wheat/MWK26 row could dominate the Wheat price lane while HRW and SRW were ignored.

**Repo shape changes:** `lib/thesis/rating-domain-mappers.ts` now treats Wheat price context as a Spring Wheat / HRW / SRW futures basket when multiple Wheat-class rows are present. Basket agreement sets price direction; split contract direction lowers confidence. Focused mapper tests now cover the three-contract bearish basket and a split-basket confidence downgrade. The Wheat handoff docs and grain-report skills were updated so future sessions start from the repaired mapper rather than the old "latest row wins" issue.

**Current assessment:** This fixes deterministic price scoring only. The later headline reconciliation, UI-proof, and spiderweb-polish passes made the deterministic rating scorecard the Wheat weekly headline source, exposed the three-contract split visually, and made the relationship map readable for v1. The next Wheat loop should verify the repaired basket against refreshed live packets and add historical price/export context.

## 2026-06-24 - Wheat thesis closeout and evaluator-skill repair

**Driver:** Kyle asked to cleanly close the session before starting a new one: update docs, improve any agents or skills used, archive deprecated paths that could interfere with the Wheat Bull/Bear goal, and commit the work.

**Repo shape changes:** Added `docs/plans/2026-06-24-wheat-thesis-goal-handoff.md` as the next-session start point for the Wheat-first Bull/Bear `/goal`. Updated Track 54 docs to reflect the actual 2026-06-23 Hermes no-write run, the expired Grok CLI auth blocker, and the fact that `grok-composer-2.5-fast` is referenced but not callable until Grok auth is repaired. Clarified current docs and agent guidance so retired Grok thesis writers stay tombstoned while the reviewed Track 54 no-write Grok/Hermes evidence scout remains allowed. Added shared `.agents/skills/_eval-core` evaluator references and repaired `agent-auditor` / `skill-auditor` paths that previously pointed at missing local files.

**Current assessment:** The next session should not re-open visual mockups first. The open work is the Wheat scoring/spiderweb system: full USDA Wheat sweep, one canonical reconciliation judge, visible Wheat price-basket proof with packet-window trend context, public-safe full historical price/export context, and a farmer-facing relationship graph where distance and edge weight communicate impact.

## 2026-06-23 - Wheat Bull/Bear board redesign and lane-swarm contract

**Driver:** Kyle clarified that the farmer-facing Wheat thesis board must not read as USA versus Canada. It needs one fast Wheat bullish/bearish read, with geography treated as evidence context, plus a repeatable data-integration workflow that can explain why each source lane gets its rating.

**Repo shape changes:** `/thesis` now renders one Wheat decision board with CA/US evidence chips, bull/bear evidence cards, a compact source-to-pressure-to-read flow, a latest USDA Crop Progress update card for the 2026-06-22 release, and sorted pressure-lane bars instead of equal country cards. Added `docs/plans/2026-06-23-wheat-bull-bear-thesis-swarm-redesign.md` as the operating contract for lane agents and a reconciliation judge.

**Current assessment:** The page remains Wheat-first and farmer-facing. USDA crop-progress condition is an admitted supply/weather pressure input; harvest and heading are currently explanation/context metrics unless replay proves they should move score. The board preserves the no-advice boundary and the class-specific Wheat mapping guardrail.

## 2026-06-22 - Environmental flood / excess-moisture watch v0

**Driver:** Kyle asked for a watch-only flood/excess-moisture layer for Western Canada and the U.S., then moved it under an Environmental tab with public wet/dry field checks and mobile support.

**Repo shape changes:** Added `/environmental` and `/environmental/flood-watch`, a legacy `/data/flood-watch` redirect, a reproducible GEE script at `scripts/gee/flood_excess_moisture_watch.py`, the public acreage artifact at `public/data/flood-watch-v0.json`, Mapbox/react-map-gl watch rendering, AAFC/Crop-CASMA tile proxy routes, and a public `/api/environmental/field-check` intake route that saves unreviewed calibration reports to `feedback_log`.

**Current assessment:** The layer is explicitly public watch-only: acres at risk, not confirmed damaged acres. Public wet/dry reports are calibration evidence only and do not automatically alter map acres or thesis scores.

## 2026-06-20 - Prairie crop-progress evaluator and Wheat thesis overlay rule

**Driver:** Kyle built a weekly Prairie crop-progress report loop for Alberta, Saskatchewan, and Manitoba, then narrowed the public crop-quality comparison to source-backed Prairie major crops: Spring Wheat, Canola, Barley, and Pulses/Other.

**Repo shape changes:** Added a no-write weekly evaluator at `scripts/build-prairie-crop-progress-report.py` and `npm run report:prairie-crop-progress`, producing a local report package under `output/prairie-crop-progress/latest/` with summary JSON, report markdown, and SVG infographics. The package keeps Manitoba in progress/risk reporting but excludes Manitoba from crop-quality graphics because the current official Manitoba report has narrative condition/flooding evidence, not structured Excellent/Good/Fair/Poor quality buckets. The existing `grain-report` skill now records how to treat crop progress as a bounded Wheat bull/bear pressure overlay.

**Current assessment:** This is source-backed report generation and operating guidance, not a thesis-cache write path. Weekly crop-progress facts should feed the Wheat read as pressure evidence: Manitoba flooding/unseeded exposure and weak North West Alberta conditions are bullish supply/quality risks, Saskatchewan seeding progress is a bearish/neutral offset, and Saskatchewan development lag/surplus moisture remains a watch risk.

## 2026-06-10 - Five-source admission sweep: StatsCan field crops + crush and SK cash prices live

**Driver:** Kyle approved adding all five recommended data lanes. A parallel research workflow verified source identity for each (endpoints fetched live, formats byte-checked, adversarially re-verified) before any importer was written.

**Repo shape changes:** New statcan_wds_raw + sk_cash_prices tables (migration 20260610190000, applied live), scripts/import-statcan-wds.py (WDS coordinate/vector pulls for 32-10-0359 / 32-10-0007 / 32-10-0352 with statusCode-means-unreleased handling), scripts/import-sk-cash-prices.py (browser-UA CSV pulls, 10 crops), collect:statcan / collect:sk-prices wrappers, and three registry admissions plus two identified-pending entries (per-crop conditions with the June-19 activation date and exact AB format template; CN rail with the verified deterministic PDF URL pattern). The cash-bids research finding is recorded honestly: no clean public per-elevator basis source exists; the SK provincial average is the admitted proxy and posted_prices remains the local lane.

**Current assessment:** Data-first admission - both lanes are live with source_runs freshness, no scoring weight changes. Mapper/scoring admission (supply domain for StatsCan, demand context for crush, area-layer display for SK prices) is each its own bounded follow-up.

## 2026-06-09 - Track 55: world veg-oil admission + new `bounded_context` source class

**Driver:** The grain coverage matrix's top Canola admission priority ("China policy, palm oil, and global vegetable-oil trade") cited `usda_wasde_raw`, but the table only held the 5 US PSD commodities. Admitting the world veg-oil complex required a lane that is admitted and deterministic yet structurally incapable of becoming a primary score driver.

**Repo shape changes:** `scripts/import-usda-wasde.py` gained world-scope markets (PSD `/world/` endpoint, `country_code '00'`, `desk_heartbeat=False`); the archive importer documents why it cannot cover them. Migration `20260609170000` adds a Canola-gated `demand.global_veg_oil` packet block + 4 `grain_market_mappings` context rows. The operating-model change is the new fifth `ImpactSourceClass`, `bounded_context` (admitted + deterministic + weight-neutral), wired through coverage (`scored`), domain weights (boost 0), the audit graph (bounded role, authority 0.7), and both audit UIs — created because classifying the lane `official_thesis_input` silently inflated Canola's demand weight share (caught by the weights regression test; see issues.md 2026-06-09).

**Current assessment:** Lane is live end-to-end and currently neutral (avg world S/U YoY shift -0.13 pp, inside the ±0.5 dead zone). Remote migration history was repaired in the process (7 MCP-applied version stamps reconciled by content-equivalence probes; never `db push --include-all` over old `CREATE OR REPLACE` files). The monthly `collect-wasde` routine picks up the world markets automatically starting with the June 11 WASDE.

## 2026-06-09 - "In their area" layer V1: postal-code localization on the thesis board

**Driver:** The original product ask is a thesis a farmer can use *in their area*; until now nothing on the board was localized. V1 ships the area spine: anonymous postal-code entry, province-level CGC movement context, and the local-bids slot wired to the existing posted-prices lane.

**Repo shape changes:** Added `lib/utils/area.ts` (FSA normalize + province mapping), `lib/queries/area-read.ts` + `area-read-utils.ts` (provincial flow query + pure builders, `get_area_prices` bids), `app/(dashboard)/thesis/area-actions.ts` (cookie server actions), and `components/dashboard/your-area-card.tsx`, mounted on `/thesis` after the major matrix. Smoke contract gained the `Your area` marker on both thesis routes. New tests cover FSA parsing, province mapping, flow folding/YoY math, PostgREST numeric-string handling, and the page-level prompt/localized/bids states.

**Current assessment:** Province-level movement pace is the real value today (live CGC rows verified end-to-end); the bids lane is honest-empty until operators post. This is read-only localization - it adds no scoring authority and no auth wall. Next steps recorded in STATUS: profile prefill, bid-vs-futures gap once bids exist, grain-detail integration.

## 2026-06-09 - Farmer-first /thesis split: operator telemetry moved to audit mode

**Driver:** Kyle's re-evaluation found the Bull/Bear board buried under pipeline telemetry — the first screen a farmer saw was freshness-row counts and automation gate status, not the market read. The operating decision: normal `/thesis` is the farmer surface; `?audit=1` is the operator console.

**Repo shape changes:** Reordered the `/thesis` page composition (farmer read leads; source health and snapshot follow it) and gated DailyUpdateStatusPanel, XPulseWatchPanel, WeeklyDataIntakePanel, and the Source Packets KPI grid behind audit mode. Updated the Track 54 browser-smoke route contract in `scripts/run-track54-browser-smoke.ts`: farmer-marker set + forbidden operator sentinels on normal `/thesis`, full superset on the audit route. Rewrote the page-test ordering contract and switched 18 moved-panel tests to audit-mode rendering.

**Current assessment:** The farmer surface now answers "what is the read" before anything else; the smoke contract structurally locks the split so telemetry cannot creep back onto the public board. One latent trap recorded: forbidden-sentinel phrases must be unique to the moved panel — case-insensitive whole-word matching collides across superstring copy (the "X Pulse Watch" title vs "X Pulse watch leads" map copy).

## 2026-06-09 - Data Universe: farmer-facing React Three Fiber system visualization

**Driver:** Kyle wants farmers to see how intricate the data system behind the board actually is - a living node graph, not a table. React Three Fiber was previously deferred; this is its first production use, on a dedicated farmer-facing route rather than the operator audit surface.

**Repo shape changes:** Added `lib/thesis/data-universe.ts` (+ tests) - a pure system-graph builder that aggregates the deterministic grain impact graphs into one universe: source lanes -> pressure lanes -> grains -> board, with farmer-readable source names and confidence-weighted board reads. Added `components/dashboard/data-universe-scene.tsx` (R3F Canvas, flow particles, node selection, reduced-motion + fallback) and `data-universe-client.tsx`, new route `app/(dashboard)/data-universe/page.tsx`, and "Data Map" nav links. New dependencies: `@react-three/fiber@^9`, `@react-three/drei@^10` (three was already present).

**Bugs caught during build:** the WebGL probe leaked a context per mount (now released + cached - applied to the audit constellation too), and React StrictMode's dev double-mount permanently killed the R3F context (self-healing keyed remount on `webglcontextlost`, capped at 3 recoveries, which also covers real GPU resets in the field). Verified live with pixel reads: full drawing buffer, 83 distinct colors at center, zero console errors.

**Current assessment:** This is a farmer-facing showpiece and transparency surface; it adds no scoring authority and reads from the same impact-graph model as the audit tools. R3F is now the sanctioned 3D stack for farmer surfaces; the audit constellation stays native Three.js until there is a reason to converge.

## 2026-06-09 - Friday desk outage discovery, stale-thesis guards, and farmer read linkage

**Driver:** Kyle's re-evaluation of the board against the original ask ("pull data, analyze, produce a bull/bear thesis a farmer can reliably use in their area") surfaced that the Friday bushel desk silently stopped writing after 2026-04-25 while grain detail pages kept presenting the week-36 desk thesis as current, and that the relationship/scoring work lived only on the operator audit surface.

**Repo shape changes:** Added `lib/utils/thesis-staleness.ts` (+ tests) and wired `assessDeskThesisStaleness()` into `app/(dashboard)/grain/[slug]/page.tsx` and `app/(dashboard)/my-farm/page.tsx` so a desk thesis 2+ weeks behind the display data week renders as labeled history and cannot drive haul/hold recommendation inputs. Repaired both Friday desk prompt docs (`docs/reference/grain-desk-swarm-prompt.md`, `us-desk-swarm-prompt.md`): CHECK-legal `triggered_by='cron'` with routine names moved into JSON payloads, removed nonexistent `pipeline_runs.source`/`metadata` column references, replaced the exact `claude-opus-4-7` pin with an Opus-class requirement, and added schema-trap warnings. Added the farmer-facing `components/dashboard/grain-read-linkage.tsx` + `lib/thesis/grain-read-linkage.ts` (+ tests): normal `/thesis` grain cards now show "What feeds this read" counts and top lanes derived from the same deterministic impact graph as the audit surface, with an explicit watch-leads-never-move-the-score boundary line.

**Current assessment:** The desk restart itself is an operator action (re-enable the `grain-desk-weekly` / `us-desk-weekly` Claude Desktop Routines before Friday 2026-06-12); the repo-side schema traps that would have killed restarted runs are fixed, and the board no longer misrepresents stale desk output while the desk is down. The linking visual now serves farmers, not just operators. The top-ranked scoring admission (Canola global veg-oil demand context) is blocked on data: `usda_wasde_raw` holds only the five imported US commodities, so the WASDE importer needs a world-level rapeseed/palm/soy-oil extension as its own admission task.

## 2026-06-09 - Grain relationship visualization audit surface

**Driver:** The thesis board needed a high-level operator visualization for how public V1 grains talk to and rank against each other, with a scoped 3D layer that stays subordinate to the ranked audit proof.

**Repo shape changes:** Added `lib/thesis/grain-impact-graph.ts`, `components/dashboard/grain-impact-graph-panel.tsx`, `components/dashboard/grain-relationship-constellation.tsx`, `components/dashboard/grain-relationship-explorer.tsx`, and focused tests for the graph model, Three.js constellation layout, and client explorer. `/thesis?audit=1` now renders an operator-only Grain Impact Graph with a cross-grain relationship map, native Three.js relationship constellation, influence ranking matrix, selected-grain influence board, conversation matrix, current board-read overlay, link-rank proof, current driver stack, and per-grain ranked relationship networks. Normal `/thesis` remains farmer-facing and does not render the graph.

**Current assessment:** This is an audit/proof surface, not a new scoring source and not a public 3D experience. Native Three.js is now used for the 3D canvas; React Three Fiber remains deferred until the grain relationship model and ranking logic prove they need a fuller 3D component layer. Fresh proof covers focused tests, typecheck, lint, production build, Playwright desktop/mobile screenshots, current-server route smoke, and targeted desktop/mobile Three.js canvas checks for nonblank pixels, animation, clickable node selection, and zero browser console errors.

**Review hardening (same day, pre-checkpoint):** An adversarial multi-agent review of the uncommitted work confirmed three real defects, all fixed before commit: 24 SVG `fill`/`stroke` attributes used `var(--token)` values that do not resolve in SVG presentation attributes (replaced with theme-aware Tailwind `fill-*`/`stroke-*` classes per repo convention); the explorer's rank-proof strings hardcoded copies of the model's private authority/scope constants (the model now exports them and the proof strings derive from the exports); and both client components used inline `boardReads = []` parameter defaults whose per-render identity would tear down and reboot the Three.js scene on every re-render when the prop is omitted (now a stable module constant). Nine new tests lock the ranking math branches, the authority-times-scope rank invariant across all overview edges, the Three.js unmount dispose contract under a mocked `three` module, the confidence-weighted vertical position formula, and the empty-grains null render. Post-fix proof: 61/61 focused tests, typecheck, lint 0/0, production build, and a clean six-check browser smoke.

## 2026-06-09 - Track 54 thesis-flow reference and regression notes

**Driver:** Track 54 status was accurate but scattered across the status ledger, readiness output, Hermes bridge doc, and lessons learned. The remaining build needs a single operator map for how official source data, prices, X sentiment, artifact gates, promotion approval, daily overlays, and Friday thesis-of-record writes fit together.

**Repo shape changes:** Added `docs/reference/track54-daily-bull-bear-thesis-flow.md` as the canonical daily Bull/Bear thesis flow reference and linked it from `PROJECT_STATE.md`, `docs/plans/STATUS.md`, and `docs/reference/daily-thesis-review-prompt.md`. Updated `docs/reference/hermes-grok-x-scout-bridge.md` with the exact terminal smoke command that works from Codex: `hermes --model grok-4.3 --provider xai-oauth -z "Return exactly: HERMES_GROK_OK"`. Added lessons learned for rolling clean-day artifact math and the Hermes `xai-oauth` provider path.

**Current assessment:** This is documentation hardening only. It does not run X search, enable writes, register write-mode automations, or change the artifact gate state. The daily loop still needs clean no-write artifacts and explicit Kyle approval before write-mode automation.

## 2026-06-03 - Track 54 Grok CLI automation hardening

**Driver:** The scheduled Track 54 no-write scout jobs need to be reliable enough to collect daily artifacts without letting Grok become a thesis writer or hanging the local workstation.

**Repo shape changes:** Hardened `scripts/run-grok-x-scout.ts`, `scripts/run-track54-artifact-health-check.ts`, `scripts/build-track54-readiness-report.ts`, and focused runner/readiness tests after the 2026-06-03 automation review. The Grok CLI wrapper now writes valid disabled MCP stubs for noisy connectors, extracts scout JSON from Grok CLI's outer `text` envelope, terminates the full Windows process tree on timeout, loads `.env.local` before invoking Grok so a future `XAI_API_KEY` can be used, and fails fast when Grok requests interactive re-authentication. The readiness gate now checks whether a noninteractive credential source exists by looking for `XAI_API_KEY` or an unexpired local Grok `auth.json` expiry, without printing secrets. Added lightweight `npm run track54:grok-preflight` plus active Codex automation `track-54-grok-auth-preflight` at 3:55 PM MT Mon-Fri so expired auth is caught before the scheduled 4:10 PM daily scout. Artifact-health now reviews existing artifact state but preflights Grok before any no-write retry, returning `retry_blocked_by_grok_preflight = true` when credentials are bad instead of launching another scout. Scheduled scout/retry commands now use `--runner auto`, selecting `xai_api` when `XAI_API_KEY` is present and otherwise using the locked-down Grok CLI path. Added active late recovery automation `track-54-late-grok-auth-recovery` at 5:05 PM MT Mon-Fri so a manually refreshed Grok login can still trigger one no-write artifact-health retry before the 5:30 PM review heartbeat.

**Automation review proof:** The 2026-06-03 price/cache automation succeeded, but the daily Grok scout and artifact-health automations failed to collect a valid same-day `daily_pulse` artifact. After the wrapper fixes, the health check no longer hangs and reports the real blocker: Grok CLI authentication expired and no `XAI_API_KEY` is present. The refreshed readiness proof now reports `Grok CLI/auth preflight failed` from the credential gate, remains no-write, keeps production writes disabled, and keeps write-mode automation proposals as create-after-approval only.

**Current assessment:** This is an operator hardening pass, not production promotion. The daily-pulse gate remains at 2/5 clean artifact days with 4 accepted signals, 3 decision-grade signals, and 1 parse/auth failure; Friday-deep remains 0/1. The next safe action is to refresh Grok auth with `grok login` or add `XAI_API_KEY`, then rerun the no-write daily artifact health check. Do not register write-mode automations until the matching mode is candidate-ready and Kyle explicitly approves.

## 2026-06-03 - Grain impact mind map reference

**Driver:** The Bull/Bear board needs a grain-specific impact map before adding more data lanes, so Canola, Wheat, Durum, Barley, Oats, Corn, and Soybeans do not share one generic scoring story.

**Repo shape changes:** Added `docs/reference/grain-impact-mind-map-v1.md`, `lib/thesis/grain-impact-map.ts`, and focused coverage in `lib/__tests__/grain-impact-map.test.ts`, then linked the reference from the source registry and thesis rating model docs. The new reference and runtime contract classify impact factors as official thesis input, price context, watch-only evidence, or parked, then map each V1 grain to its main supply, demand, movement, logistics, price, positioning, quality, global-substitute, seasonal, and Viking-knowledge relationships. `/thesis?audit=1` now renders an operator-only Impact Map audit panel below the scorecard audit for each thesis card, while normal `/thesis` stays farmer-facing. `lib/thesis/grain-impact-domain-weights.ts` now applies a small grain-profile tilt to scorecard domain weights for admitted official and price-context factors without promoting watch-only or parked factors. `lib/thesis/rating-domain-mappers.ts` now also admits primary `cftc_cot_positions` rows into the deterministic positioning domain for Canada and US scorecards; proxy-only rows remain context and do not move the ranking. The same mapper now admits Canada `supply_disposition` carryout, US `usda_wasde_mapped` balance-sheet pressure, and US `usda_quarterly_stocks` inventory surprises into the deterministic supply domain, so already-pulled official baseline sources can influence scorecards without adding a new source lane. WASDE and quarterly stocks merge into one US `supply` domain instead of creating duplicate supply reads. It also admits active-season Canada `canada_crop_progress` seeding and US `usda_crop_progress` condition/planting pace into the deterministic weather domain, making seasonal timing part of the ranking model without adding direct weather or satellite sources. Fresh `grain_prices` rows now feed a bounded price-confirmation domain for Canada and US scorecards; stale price proof blocks the price contribution, and Barchart latest-only rows remain low-confidence provisional momentum. Canada `grain_monitor_snapshots` and `producer_car_allocations` now feed a bounded logistics domain for unload capacity, terminal/vessel friction, and producer-car direct-rail support; US logistics stays parked until a V1 source is admitted.

**Follow-up hardening:** Added forward migration `20260603190500_include_canada_crop_condition_in_thesis_packet.sql` so Canada thesis packets include official province-level `condition_good_excellent_pct` rows when they exist. `lib/thesis/rating-domain-mappers.ts` now scores those Canada condition rows during May-October only: <=50% good/excellent or <=-8% versus five-year average is bullish crop-stress pressure; >=75% or >=8% versus five-year average is bearish supply-cushion pressure. The Canada crop-progress importer now also parses Alberta emergence, surface-soil moisture, and pasture rows from the current official report, increasing the Alberta dry-run from 36 seeded rows to 54 total source rows without writing Supabase. The source registry, mind-map reference, and rating-model reference now state the live caveat clearly: crop-condition scoring is dormant until the collector emits admitted `condition_good_excellent_pct` rows.

**Saskatchewan seasonal context hardening:** The Canada crop-progress importer now also parses Saskatchewan's current official cropland, hayland, and pasture topsoil moisture rows as `soil_moisture_adequate_surplus_pct` source context, using surplus plus adequate percentages. The SK dry-run now parses 102 rows instead of 99, including 89% cropland, 84% hayland, and 77% pasture adequate/surplus moisture, with zero Supabase writes in dry-run mode. These rows are context-only because their `crop_name` values are not canonical grains and the thesis packet still admits only seeded and official crop-condition rows for scoring.

**Saskatchewan development timing contract:** Added forward migration `20260603192500_add_canada_crop_development_metrics.sql` to admit `development_normal_pct`, `development_ahead_pct`, and `development_behind_pct` in `canada_crop_progress`, then added Saskatchewan parser support for official crop-development group rows. The SK dry-run now parses 117 rows with zero Supabase writes: 99 seeded rows, 3 topsoil-moisture context rows, and 15 crop-development rows across Fall Cereals, Spring Cereals, Pulse Crops, Oilseeds, Perennial Forage, and Annual Forage. At this intermediate parser-contract stage, these development rows were not yet scored; the later thesis-packet/mapper pass below decided how timing should affect Bull/Bear pressure.

**Runtime development-context boundary:** `lib/thesis/grain-impact-map.ts` first mapped Saskatchewan Oilseeds development timing to Canola and Saskatchewan Spring Cereals development timing to Wheat, Durum, Barley, and Oats as watch-only weather context. Corn and Soybeans did not inherit this provincial group timing context, and deterministic Bull/Bear weights stayed unchanged until the later packet/mapper admission recorded below.

**Global competitor boundary:** The runtime impact map now gives every V1 grain an explicit global or cross-border competitor/watch factor: Canola policy/veg-oil trade, Wheat Black Sea/EU/Australia origins, Durum Mediterranean/North Africa tender context, Barley feed/malt origin competition, Oats cross-border milling/import context, Corn Brazil/Argentina competition, and Soybeans Brazil/Argentina plus China demand-policy pressure. These factors can point at raw WASDE/PSD rows (`usda_wasde_raw`) for broad world-balance context, but they are all `watch_only`; detailed policy, customs, freight, tender, palm-oil, malt, and quality feeds still need source admission before moving Bull/Bear scores.

**Market response rules:** The typed grain impact map now includes `marketResponses[]` for every V1 grain, and `/thesis?audit=1` renders those rules under the Impact Map audit panel. These rules connect source-backed facts, price confirmation, watch-only global context, parked source gaps, and Viking knowledge topics into explicit interpretation patterns such as canola flow plus soy-complex confirmation, wheat local-bull/global-origin caps, barley feed-corn caps, oats thin-market confirmation, and corn/soybean South America offsets. They are audit/explanation scaffolding only and do not add deterministic score weight.

**Roundtable response-context contract:** `lib/thesis/scorecard-llm-guardrails.ts` now publishes the same grain market-response rules as `market_response_context` in the scorecard guardrail payload consumed by the roundtable prompt pack. The payload includes market shape, market structure, seasonal windows, source classes, confidence-impact copy, and Viking topic hooks, with an explicit explanation-only scoring boundary so response context cannot become a new allowed claim, blocked claim, score, or confidence override.

**Grain data coverage matrix:** Added `lib/thesis/grain-data-coverage.ts` and an audit-only `/thesis?audit=1` Data coverage matrix. The matrix derives from the same grain impact profiles and shows each lane as pulled, packeted, scored, explanation-only, or missing. It keeps watch-only global/seasonal context out of deterministic scoring, turns parked source ideas into missing-admission rows, and gives operators a clearer checklist for the next source-admission work without changing the normal farmer-facing board.

**Source-admission priorities:** The coverage matrix now also emits audit-only `admissionPriorities[]`. It ranks existing watch-only lanes before missing-source gaps, then states the source work required and the no-score boundary for each candidate. This turns the mind map into a practical build queue without allowing unadmitted data to move the public Bull/Bear rating.

**Canada crop-development timing admission:** Added forward migration `20260603202000_include_canada_development_timing_in_thesis_packet.sql` so Canada thesis packets can carry Saskatchewan crop-development group rows for mapped public grains. `lib/thesis/rating-domain-mappers.ts` now lets `development_behind_pct` add bounded weather timing pressure only as low-confidence proxy evidence: Oilseeds map to Canola, and Spring Cereals map to Wheat, Durum, Barley, and Oats. The impact map and coverage matrix now classify those timing lanes as admitted official proxy inputs, while Corn/Soybeans still do not inherit Saskatchewan group timing and Spring/Winter Wheat remain parked.

**Live source-admission proof:** Applied the targeted Supabase SQL path for `20260603190500`, `20260603192500`, and `20260603202000`, then repaired migration history for exactly those versions after live execution. Live schema now admits the three `development_*` metrics and live `get_canada_thesis_packet()` contains both condition and mapped development rows. `python scripts/import-canada-crop-progress.py --province SK --force` wrote 117 Saskatchewan rows from the 2026-05-28 Crop Report with source run `d0ebf5dd-bacb-47bf-a5b0-a8b30acbd6f8`. `npx tsx scripts/refresh-thesis-packet-cache.ts --force` refreshed 12/12 packets at `2026-06-03T19:51:17.680412+00:00`. Direct and cached packet checks show Canola receives Oilseeds 73% behind / 27% normal development, Wheat/Amber Durum/Barley/Oats receive Spring Cereals 63% behind / 37% normal development, and Corn/Soybeans receive zero development rows.

**Canada cropland moisture proxy admission:** Added forward migration `20260603211000_include_canada_soil_moisture_in_thesis_packet.sql` so Canada thesis packets can carry broad AB All Crops and SK Cropland `soil_moisture_adequate_surplus_pct` rows for mapped public Prairie grains only. `lib/thesis/rating-domain-mappers.ts` now lets those rows add bounded low-confidence proxy weather pressure during May-October: <=55% adequate/surplus or <=-8% versus five-year/previous-year context adds +10 bullish crop-stress risk, while >=75% or >=8% lowers near-term moisture stress by -6. The impact map and coverage matrix classify these moisture lanes as admitted official proxy inputs for Canola, Wheat, Durum/Amber Durum, Barley, and Oats. Corn/Soybeans receive zero broad-moisture mapping, and broad moisture rows remain explicitly not crop-specific condition, quality, or yield evidence. At this journal checkpoint the migration is local pending validation/live application.

**Current assessment:** This is an architecture contract, audit surface, first scoring-weight adapter, and one live bounded source-admission step for Saskatchewan group-level development timing. The next implementation move is adding Canada crop-progress condition parsing from official provincial reports; new global, local-basis, quality, direct weather, or satellite sources still require source admission before moving public Bull/Bear scores. Browser smoke now treats audit mode as an operator route: parked grain gaps such as Spring/Winter Wheat can appear there, but trading/advice language remains forbidden.

## 2026-06-02 - Track 54 daily Bull/Bear proof surfaces

**Driver:** The Bull/Bear Thesis Board needed to show not only the current read, but also whether today's daily overlay, price context, X Pulse proof, and weekly source pulls are actually current.

**Repo shape changes:** `/thesis` now carries farmer-facing daily proof surfaces: `Daily Update Status`, `Today update window`, `Today update checklist`, `Daily automation gate progress`, and `Weekly Data Intake`. The intake section now explains source timing, allowed role, pressure lane, analyzed measurements, current proof, target table, and what each source can change in the bull/bear read. Normal `/thesis` shows sanitized readiness status; `/thesis?audit=1` keeps local artifact paths, hashes, and operator gate details in the audit lane.

**Operating-model hardening:** `npm run track54:readiness` now recovers Windows/npm 11 stripped wrapper flags, forwards browser-smoke target options to the smoke step, and strips those values from readiness-builder passthrough. This was verified after npm first fell back to the stale default `3110` server despite a requested `3111` base URL. Final post-build proof at `2026-06-03T00:29:53.188Z` smoked `http://127.0.0.1:3111`, found `Analyzed data:yes`, had no forbidden public copy, had `console_errors=0`, and still correctly held production writes disabled because daily-pulse is 2/5 and Friday-deep is 0/1.

**Current assessment:** The board is moving in the right direction for transparency: farmers can see whether a visible score is a weekly packet or daily overlay, what data is pulled through the week, and what each data lane signifies. The daily write loop is not yet production-ready. That is intentional: the artifact gate is still collecting dry-run evidence before any write-mode Grok or bounded daily thesis routine can be enabled.

## 2026-06-01 - Track 54 Grok/X scout guardrails

**Driver:** Track 54 shifted Grok back into a quarantined evidence-discovery role for X sentiment, with official prices and source-backed thesis packets remaining the board's authority.

**Repo shape changes:** Added the Track 54 scripts, X scout schema, daily review packet/writer, Friday X bundle, and `/thesis` X Pulse Watch path. Codex automation now owns the dry-run artifact-week review gate while production Grok write routines stay disabled.

**Latest hardening:** Accepted X signal writes now carry controlled `affected_decisions`, include the primary grain in `affected_grains`, dedupe affected regions, and reject otherwise valid signals that omit affected-region evidence. Validation also enforces the scout search windows: daily pulse is one day, Friday deep is seven days, and future-dated posts are rejected. Metadata includes the required `corroboration` block, and numeric claims reject unless Grok flags them for official verification. Friday bundles, daily trajectory writes, `/thesis?audit=1`, and CAD/US desk prompts now preserve corroboration plus allowed/blocked/official-verification evidence, so unverified X claims stay review leads rather than thesis facts; the CAD/US prompt contract is now executable and requires the accepted X bundle command, required signal metadata, X-as-untrusted-evidence wording, official-source/price precedence, audit logging, Viking knowledge, and explicit "Grok never writes/ranks/authors" boundaries. The Friday bundle builder now drops current-week rows missing the desk evidence seal: post URL, post date, source tier, affected-region context, allowed/blocked claim arrays, explicit verification flag, and valid corroboration if a corroboration block is present. Legacy rows missing only the newer corroboration block are normalized to `0` matching claims / no official match, making them visible only as uncorroborated review leads; bundle counts now expose accepted desk-ready rows, `excluded_missing_evidence`, and `legacy_missing_corroboration` separately. Friday bundle week resolution now chooses newest crop year before highest grain week, preventing prior-year week 52 rows from hiding current-year accepted signals; older accepted rows with empty `affected_decisions` get deterministic read-time tags. The xAI API fallback now loads `.env.local`, searches all trusted-handle batches, and merges/dedupes strict scout outputs before validation. Write mode resolves the latest CGC grain week automatically when `--grain-week` is omitted. The artifact-week gate now verifies raw artifact `run_date`/`mode` identity and requires decision-grade accepted evidence from `tier1`, `tier2`, or `tier3`; stale/reused artifacts or a low-grade-only accepted week cannot become a write-mode candidate. Friday-deep promotion now uses one clean Friday artifact as the weekly proof unit and holds non-Friday deep artifacts out of approval. The Grok scout and daily thesis review write scripts now also require both the Track 54 approval phrase and a live `candidate_for_enablement` artifact-week review for the intended mode before any `--write` path can run, and runtime commands cannot lower the daily five-day requirement or switch the reviewed artifact root/mode through environment overrides. The promotion brief is mode-scoped, so daily-pulse evidence cannot list or approve the Friday-deep writer. Active Codex automation now collects Friday-deep dry-run evidence on Fridays without enabling the writer. New regression tests lock the retired Grok/xAI thesis-writing boundary by proving `/api/pipeline/run` returns HTTP 410 and the shared Edge Function tombstone gate ignores the removed `ALLOW_V1_GROK` escape hatch. `npm run track54:browser-smoke` now starts local Next when needed, launches local Chrome headless, checks `/thesis`, `/thesis?audit=1`, and `/overview`, captures screenshots, and writes proof JSON. `npm run track54:readiness` now adds a single no-write operator gate over the daily-pulse review, Friday-deep review, mode-scoped promotion briefs, local Codex automation manifests, live source-freshness proof, focused acceptance tests, browser-smoke proof, and plan acceptance criteria. Both active Grok dry-run Codex automations now refresh readiness through `npm run track54:readiness -- --out scratch/track54-readiness/latest-readiness-report.json --browser-smoke-proof-out scratch/track54-browser-smoke/browser-smoke-proof.json` after their artifact review and report `acceptance_audit.browser_smoke_proof`, `browser_smoke_clean`, and projected next eligible artifact dates; the manifest audit requires exact expected `rrule` schedules, the Bushel Board repo in `cwds`, and prompts containing `npm run track54:readiness`, `--browser-smoke-proof-out`, `acceptance_audit.overall_status`, `acceptance_audit.browser_smoke_proof`, `browser_smoke_clean`, and `completion_blockers`. Current verdict is `blocked_by_artifact_gates` / `acceptance_audit.overall_status = incomplete` with production writes hard-false, focused acceptance tests passing, browser smoke proven, and daily/Friday artifact windows still incomplete. This keeps the X lane useful for farmer decisions without letting Grok invent action labels, admit stale chatter as daily evidence, self-verify numbers, silently ignore most trusted handles in fallback mode, reuse stale files, promote weak source tiers, hand incomplete rows to Friday desks, bypass Friday desk source hierarchy, reopen retired thesis writers, drift back to dry-run-only, wrong-schedule, wrong-directory, or browser-unstable automation reports, or write before the human gate.

**Write-automation proof:** Readiness now separately checks the proposed write-mode Codex manifests (`grok-x-scout-daily`, `daily-thesis-review`, and `grok-x-scout-friday-deep`) and holds manual review if any are active before matching-mode human approval.

**Focused acceptance proof:** The readiness proof now runs the broader Track 54 test slice, 18 files / 107 tests, so the JSON gate covers the readiness report itself, artifact-week review, promotion brief, scout contract, output merge, run summaries, write approval, write gate, migration/RLS contract, stale browser-proof rejection, X signal validation, Friday bundle, thesis UI, and retired-pipeline tombstone checks.

**Artifact cadence proof:** Readiness now emits `artifact_automation_coverage` and an acceptance item proving projected daily-pulse and Friday-deep artifact dates are covered by active dry-run Codex automations with expected schedules and prompt boundaries. The remaining completion blocker is artifact collection/quality over the required dates, not a missing scheduler path.

**Post-approval handoff proof:** Promotion briefs now emit `post_approval_automation_transition`; readiness proves the operator must disable the matching dry-run artifact collector before registering the matching write-mode routine, while keeping the price/cache refresh active.

**Dry-run manifest write-command proof:** The Codex automation manifest audit now reports `forbidden_prompt_boundary_present` and rejects dry-run prompts that contain explicit write-mode command fragments, even if the prompt also includes the required no-write wording.

**Migration/RLS contract proof:** Added a Track 54 migration contract test to prove `x_scout_runs` stays public-read/service-role-write only, accepted X signals carry provenance and evidence-seal fields, `signal_hash` remains upsertable, and the new trajectory scan type is only the bounded `opus_review_daily_pulse` lane.

**Browser proof freshness:** Browser-smoke proof now carries `generated_at`, and readiness rejects proof missing that timestamp or older than six hours so old screenshots cannot satisfy the final browser gate.

**Heartbeat review-kind proof:** `npm run track54:heartbeat-summary` now emits `local_review_date`, `local_review_weekday`, `local_review_kind`, and `review_modes_to_inspect` using the `America/Edmonton` automation calendar, so the thread heartbeat can distinguish weekday daily-pulse review from Friday promotion review without manually inferring the date after UTC rollover.

**Readiness operator-field proof:** `npm run track54:readiness` now mirrors `acceptance_audit.browser_smoke_proof.ok` as top-level `browser_smoke_clean` and `acceptance_audit.completion_blockers` as top-level `completion_blockers`, so scheduled dry-run automations can report the requested operator fields directly without parsing nested audit objects.

**Artifact cutoff-date proof:** Artifact reviews now default to the last completed local automation run date rather than the raw local calendar date. Before the weekday 4:10 PM MT daily-pulse cutoff, Tuesday remains a next eligible run instead of being treated as already missed; Friday-deep uses its separate 4:50 PM MT cutoff.

**Artifact health due-window proof:** Same-day artifact health now separates `artifact_due` from artifact validity. A missing artifact before the daily-pulse or Friday-deep cutoff sets `retry_blocked_until_due` instead of launching an early no-write Grok retry before the scheduled price/scout window.

**Browser forbidden-copy proof:** Browser smoke now uses a shared forbidden rendered-text list with whole-word matching for parked grains and trading/advice wording. It caught visible `/thesis` `price advice` copy and `/overview` `sell-down proxy` copy; thesis now uses `pricing instruction` wording and overview uses `farmer-movement proxy`.

**Grok CLI runner isolation proof:** The no-write X scout runner now creates a temporary Grok home/config for each CLI run, exposes only web search, denies MCP/file/shell/edit tool calls, disables known noisy connector MCP entries in the local config, raises the Grok turn budget by mode, and records these guardrails in success/failure summaries. This keeps Grok in the scout lane while reducing global plugin/connector startup noise from the local workstation.
