# August 2026

## 2026-08-24 - Canola collector recovery and audible failure alerts

- Diagnosed four unattended official-source failures from the actual Hermes collector logs rather than relying on green truth-file prose.
- Added bounded transient-network retry/backoff to CGC weekly statistics, Producer Cars, and USDA WASDE. HTTP error responses remain real failures rather than retry candidates.
- Removed four Grain Monitor parser assumptions tied to the 2025-26 crop-year label and added a 2026-27 rollover regression. The focused Grain Monitor suite passed 13/13.
- Re-ran all four failed collectors. CGC recorded a clean current run; Producer Cars advanced to 2026-27 week 4; Grain Monitor imported 2026-27 week 1; and WASDE recovered the August report.
- Refreshed `thesis_packet_cache` 12/12 with zero failures. The source-freshness watchdog returned `ok: true` with zero alerts.
- Changed the Windows-native Hermes launcher so collectors and watchdogs emit no stdout on success and emit the full `FAIL` block to stdout on failure. A controlled injected failure proved exit-code propagation, failure delivery on stdout, an empty stderr, and inclusion of the collector error tail; the controlled success path produced empty stdout/stderr.
- Synchronized the canonical launcher to all 21 baseline installed aliases and verified they compile and match byte-for-byte after line-ending normalization. The older unscheduled `bushel-collect-fx-and-prices.py` variant remains intentionally untouched.
- Changed all 14 scheduled mechanical collectors and all 3 freshness watchdogs from local-only delivery to `origin`, so non-empty failure output reaches the originating Hermes conversation while successful runs remain silent.
- Explicitly parked the pre-existing Wheat visual-overhaul working tree. No Wheat UI, test, data, audit, or sketch file was staged, reverted, or included in this recovery.
- Final scoped verification passed: JavaScript/Python syntax checks, 13/13 Grain Monitor tests, changed-file ESLint, TypeScript typecheck, production build, independent review, installed-wrapper compilation/match checks, controlled failure/setup-failure stdout checks, a real installed watchdog silent-success run, and live source freshness with zero alerts. The repository-wide `npm test` remains baseline-red outside this diff: 12 vendored `.claude/skills/baoyu-imagine` Node-test suites are incorrectly discovered by Vitest, and two unchanged server-action assertions still hardcode crop year 2025-26 (1,262 other tests passed). Global lint also traverses generated `canola-command/.next` bundles; the changed files themselves lint cleanly. These separate test-harness hygiene issues were not mixed into the Canola recovery.
