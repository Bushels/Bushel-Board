# Journal — 2026-05

Append-only history of structural changes, cleanup passes, and other repo-shape events. Day-to-day feature work goes in `docs/plans/STATUS.md`; bug post-mortems go in `docs/lessons-learned/issues.md`. This file only logs things that change the *shape* of the repo or the operating model.

---

## 2026-05-01 — MPS portfolio cleanup pass

**Driver:** First application of the MPS portfolio cleanup workflow (`MPS/docs/templates/portfolio-cleanup-prompt.md`) to bushel-board-app. Goal was to standardize the rules-vs-activity-vs-history split, anchor ignore rules, promote cross-cutting skills out of the project, and remove accumulated cruft — all so that future Claude/Codex sessions land in a more legible repo and resist drift.

**Process:** Discovery + classification by Claude → independent validation by Codex GPT-5.5 (anchoring catch + flagged 2 pre-existing tracked junk files Claude missed) → owner approval per chunk → 6 commits in order.

### Commits in this pass (HEAD-relative, oldest first)

| SHA prefix | Subject | Net |
|---|---|---|
| `757e2c5` | gitignore: anchored runtime artifacts + scout outputs | +9 / -2 |
| `ea6b7f3` | track US desk swarm + WIP design docs/scripts/migrations | +7,462 (38 files) |
| `b96f2d9` | AGENTS.md rules-only rewrite (Codex draft) | +117 / -155 |
| `45f81fc` | PROJECT_STATE.md as current-truth file | +32 (new) |
| `d7b0573` | remove tracked CGC junk + delete cruft directories | -135,317 (2 files) |
| this commit | journal scaffolding + 2026-05 entry | new |

### What moved

- **Files newly tracked (38):** 16 US Desk swarm agents + 2 CAD additions (price-analyst, desk-meta-reviewer), 7 design docs in `docs/plans/`, 1 mockup, 1 reference doc, 5 scripts (grain-desk audit, CFTC COT collector, USDA WASDE archive importer, latest-crop-progress query, collector soft-update writer), 7 SQL migrations + 1 SQL test.
- **Files removed from index:** `data/CGC Weekly/~$gsw-shg-31-en.xlsx` (Excel temp lock), `data/CGC Weekly/gsw-shg-en.csv.pre-week36-20260416.bak` (pre-week-36 backup, 135K lines).
- **Untracked cruft deleted from working tree:** `.firecrawl/`, `infographic/us-planting-progress/`, `.claude/scheduled_tasks.lock`, `data/usda-export-scout-2026-04-18.json`, `.claude/skills/baoyu-imagine/`, `.claude/skills/baoyu-infographic/`, `.baoyu-skills/`.
- **Files promoted to `~/.claude/skills/` (outside repo):** `baoyu-imagine` (35 files including the project's `EXTEND.md` that pinned `default_provider: google`, `default_quality: 2k`), `baoyu-infographic` (48 files). Verified by a fresh session-start skill scan: both skills now appear at the top of the available-skills list (no plugin namespace), and they no longer appear under the project's `.claude/skills/` block.

### What changed in rules

- **`AGENTS.md` — 170→126 lines, rules-only.** Dropped: "Current Status," "Phase: MVP + Intelligence," all dated implementation-plan bullets and task counters, the stale "Vercel Cron ingress" line. Fixed the `.Codex/agents/` typo and updated agent count from 9 to ~38. Added a Truth Files section explaining the AGENTS.md / CLAUDE.md / PROJECT_STATE.md / `docs/journal/` split, and a Cleanup And Git Guardrails section codifying lessons from this pass.
- **`PROJECT_STATE.md` (new).** Active task, known blockers, next action, recent milestones. Living file — update when state changes meaningfully.
- **`docs/journal/` (new directory).** Append-only month-rollup history. This 2026-05.md file is the first entry.
- **`.gitignore` — 3 new anchored blocks.** Folder-level rules use leading `/` so they're root-anchored and can't accidentally match a same-named directory deeper in the tree.

### Caveats and follow-ups intentionally left for later

- **`data/CGC Weekly/*.xlsx` ignore overlaps with 5 already-tracked weekly files** (weeks 29–33). Git keeps tracking those; the new rule only ignores future additions. If anyone ever runs `git rm --cached` on those, they'll silently disappear from `git status` — that's the trap Codex flagged. Decision deferred: do those tracked weekly Excel snapshots belong in git at all, or should they all become local-only like the historic CSVs already are?
- **`.baoyu-skills/baoyu-imagine/EXTEND.md` was moved globally**, not project-locally. If you ever want Bushel-Board-only baoyu defaults that differ from your global ones, recreate `.baoyu-skills/baoyu-imagine/EXTEND.md` here; the project-level `.gitignore` keeps it out of the repo.
- **`.codex/config.toml` not created.** Codex isn't in active use on this project yet (no `.codex/config.toml` existed before this pass). Add one when Codex is actually adopted here, with the goal-#5 defaults from the cleanup prompt.
- **`components/dashboard/CLAUDE.md` not touched.** It still has a "Last refreshed: 2026-04-28" line and could drift toward an activity log. Future cleanup pass should evaluate whether to keep it as a scoped landing brief or fold into the root `CLAUDE.md`.
- **24 modified tracked files left untouched.** Mid-task work on the grain-monitor parser fixes (commits `d3eaac9` and prior on this branch). Cleanup explicitly did not touch them.

### Lessons learned (folded into AGENTS.md guardrails)

- **Anchor folder-level ignore rules with a leading `/`.** Unanchored `infographic/` could match a hypothetical `app/components/infographic/` six months from now. Codex caught this; Claude missed it.
- **Verify global skill destination before deleting project copies.** The session-skill list at start said `baoyu-imagine` was "available" — but that was because the harness scanned the project's `.claude/skills/`, not because it lived globally. Promotion meant *actually copying* up first, then re-running a session-start skill scan to confirm the global pickup, *then* deleting the local source.
- **Pre-existing tracked junk is invisible until you grep for it.** `~$gsw-shg-31-en.xlsx` and a 135K-line `.bak` were tracked for weeks before Codex flagged them.
- **Rules-only AGENTS.md / CLAUDE.md is the discipline.** The single biggest source of drift in this pass was AGENTS.md slowly accumulating "Phase: MVP + Intelligence" / "(15 tasks)" / "Implementation Plans" bullets. New convention: any time CLAUDE.md is updated, check whether AGENTS.md has the same status content and prune it.

---

## 2026-05-02 — CGC weekly importer moved to Codex

**Driver:** CGC had published Week 38 (week ending 2026-04-26), but Supabase was still at Week 37. The published data was live; the failure was the app ingress path.

**Finding:** `/api/pipeline/run` returned HTTP 410 `v1_pipeline_retired` before reaching the CGC import block. The existing Claude routine contract that said to call that route was therefore stale. `/api/cron/import-cgc` is not the active routine path for this handoff.

**Change:** Added `scripts/import-cgc-weekly-codex.mjs` and `npm run import-cgc`. The script fetches the CGC weekly page/CSV from Codex, sends the raw CSV to `import-cgc-weekly` with `csv_data`, verifies `cgc_observations`, and writes 16 `collector_cgc` heartbeat rows to `score_trajectory`. Created Codex automation `cgc-weekly-grain-stats-import` for the Thursday run.

**Verification:** Trial run imported Week 38 successfully: 4,313 `cgc_observations` rows, `cgc_imports.status='success'`, validation report `pass`, all 16 CAD grains stamped with `collector_cgc` heartbeats.

**Operating note:** Codex automation owns the Thursday CGC source-table refresh. It does not trigger the V1 Grok pipeline and does not trigger the Friday grain-desk swarm.

---

## 2026-05-02 - Grok/xAI analysis workflow retired

**Driver:** The production analysis path is now Claude/Codex. Grok should not remain as a recovery writer because stale callers can silently publish old-model rows.

**Change:** Tombstoned `/api/pipeline/run` and the Grok-backed Edge Functions: `analyze-grain-market`, `search-x-intelligence`, `analyze-market-data`, `generate-intelligence`, and `generate-farm-summary`. Removed Grok debate/search/benchmark scripts and blocked `grok-*` chat adapter routing.

**UI/data read change:** Grain detail, My Farm recommendations, and advisor context no longer prefer `grain_intelligence`; live thesis surfaces read `market_analysis`. `grain_intelligence` is now a read-only legacy archive.

**Future lane:** Direct X API v2 remains the intended route for X/Twitter signal collection, but it must feed `x_market_signals` as data. It should not reintroduce Grok/xAI reasoning.

---

## 2026-05-02 - Data layer foundation plan opened

**Driver:** Product premise changed from "grain dashboard with AI analysis" to a Canada/US live grain thesis engine that assimilates trusted data, builds bull/bear views, and turns those views into farmer marketing guidance.

**Change:** Added `docs/plans/2026-05-02-data-layer-foundation-plan.md` and Track 50 in `docs/plans/STATUS.md`. The plan locks the first scope boundary: Canada and US source truth first, limited international context second, broad global feeds deferred until the source and thesis contracts are stable.

**Live finding:** Supabase source tables are ahead of the thesis tables. CGC is current to Week 38 and USDA crop progress is current to 2026-04-26, while Canada `market_analysis` is still Week 36 and `us_market_analysis` is still Grok-written legacy data.

**Next operating decision:** Build the data-layer P0 contracts before rebuilding the thesis layer: source run ledger, grain/market mapping registry, freshness view, and Canada/US thesis packet RPCs.

---

## 2026-05-03 - GitHub CLI authenticated for repo workflow

**Driver:** Data Layer Foundation work is moving toward push/PR workflow, so local GitHub CLI auth needed to be verified before continuing.

**Verification:** `gh auth status` reports account `Bushels` authenticated against `github.com` using HTTPS protocol with `repo` and `workflow` scopes. `gh repo view` resolves this checkout to `Bushels/Bushel-Board`, default branch `master`.

**Operating note:** GitHub CLI is now usable for branch pushes, PR creation, CI inspection, and GitHub-side repo checks from this workspace.

---

## 2026-05-03 - Data Layer Foundation V1 branch pushed

**Driver:** Data Layer Foundation V1 moved from audit/plan into implementation. The branch needed to be preserved before ending the session to avoid another dirty-branch handoff.

**Change:** Committed and pushed `codex/data-layer-foundation-v1` to GitHub as `18a0935` (`Add data layer foundation contracts`). The commit contains the source-run ledger, grain/market mapping registry, source freshness contract, Canada/US thesis packet RPC contracts, importer run-summary hooks, validation script, and the seeding price-query contract fix.

**Verification:** `npm run build` passed before commit, SQL migrations parsed in a linked Supabase rollback transaction, `git diff --check` was clean, and the post-push working tree was clean.

**Operating note:** This was a GitHub push only. Live Supabase migration apply is still blocked by remote-only migration version `20260429100000`; resolve that drift before applying the Data Layer Foundation migrations or opening the PR.

## 2026-05-03 - Sprint 1 source-truth pivot started

**Driver:** Bushel Board is pivoting from dashboard-first to source-truth grain intelligence. The first sprint focuses on the data spine before adding weather, cash bids, satellite, or another AI writer.

**Change:** Added the operating contracts for the pivot:

- `docs/reference/source-registry.md` - source admission, cadence, units, lag, and failure modes.
- `docs/reference/canonical-grain-fact-model.md` - identity/time/unit/source rules for thesis packets and market reads.
- `docs/plans/2026-05-03-canola-market-read-v1.md` - Canola pilot gate and output contract.
- `supabase/migrations/20260429100000_predictive_market_briefs.sql` - local mirror of the previously remote-only predictive-market migration, recovered from `supabase_migrations.schema_migrations`.

**Verification:** `npx supabase migration list --linked` showed `20260429100000` on both local and remote sides before deploy. The four Data Layer Foundation migrations were applied live, followed by `20260504021340_optimize_thesis_freshness.sql` after the first Canola validation exposed PostgREST statement timeouts. `npx tsx scripts/validate-data-layer-foundation.ts --grain Canola --market Canola` now passes.

**Operating note:** Do not add new source lanes until the deterministic Canola Market Read V1 is built on top of the live packet RPC. `source_runs` exists but has 0 rows until collectors write run summaries.

---

## 2026-05-04 - Canola source-admission handoff documented

**Driver:** After the deterministic Canola Market Read V1 and CGC hardening work, the next session needed a clean source-order handoff rather than another dashboard or thesis writer.

**Change:** Added `docs/plans/2026-05-04-canola-source-admission-handoff.md`, expanded `docs/reference/source-registry.md` with Canola Council Markets & Stats as a Canola-specific aggregator, and recorded the first public Canola crop-size facts to seed: 2025 final production, yield, seeded acres, harvested acres, and 2026 intended seeded acres.

**Operating note:** The next Canola work is Producer Cars, CFTC COT, AAFC/StatsCan crop-size baseline, price/FX context, and then Canola Council page inventory. Canola Council is useful, but not automatically source truth; scraped rows must keep upstream source label, update date, period, and units.

---

## 2026-05-05 - Canola source-admission automations live

**Driver:** The Canola Market Read V1 needed live source freshness instead of another static handoff. The ordered source pass was Producer Cars, CFTC COT, StatsCan crop baseline, price/FX, then Canola Council inventory.

**Change:** Added Canola source-admission database support and collectors. `supply_disposition` now stores seeded acres, harvested acres, yield, intended seeded acres, estimate stage, source release date, source URL, and source detail. `canola_council_market_stats_inventory` now stores inventory metadata only; it does not admit Council-displayed values into production metric tables. Public clients have SELECT-only grants on the inventory table; service-role collectors own writes. FX rates now write `source_runs`, and grain-price CAD normalization uses the latest USD/CAD rate on or before each price date.

**Automation:** Created active Codex routines for Grain Monitor weekly logistics, Producer Cars staging, CFTC COT positioning, StatsCan Canola baseline watch, Canola price/FX freshness, and Canola Council Markets & Stats inventory. The existing CGC weekly routine remains the weekly source-table anchor.

**Verification:** Live Supabase imports succeeded for Producer Cars, CFTC COT, StatsCan crop baseline, Canola Council inventory, FX rates, and grain prices. The Canola validator passes against the live packet. Recent USD-linked price rows have CAD values after the FX repair.

---

## 2026-05-05 - Barchart OnDemand intraday Canola quote path wired

**Driver:** Weekly/daily Canola price refresh is too stale for a bullish/bearish price-direction read. Intraday Canola futures need a timestamped quote lane separate from final daily settlement history.

**Change:** Added `grain_price_intraday` and `v_latest_grain_price_intraday` for timestamped market-data quotes. Added `scripts/import-barchart-canola-intraday.ts`, which calls Barchart OnDemand `getQuote` for ICE Canola, stores last price, bid/ask, net change, percent change, mode, flag, volume, open interest, raw payload, and writes `source_runs`. The importer auto-selects the ICE Canola front month across Jan/Mar/May/Jul/Nov contracts, with `BARCHART_CANOLA_SYMBOL` available as an override.

**Automation:** Created Codex automation `barchart-canola-intraday-quote-import` on an hourly cadence. It is paused until `BARCHART_ONDEMAND_API_KEY` is added locally and the first dry-run/live pull succeeds.

**Boundary:** Intraday quotes are not final settlement rows. Use this lane for current price direction and freshness; use `grain_prices` for daily settlement/EOD history.

---

## 2026-05-06 - FX CAD recalculation grant hardened

**Driver:** Dirty-repo review found that `public.recalculate_grain_prices_cad(date, date)` is a write-capable `SECURITY DEFINER` function. The importer uses it with the service-role key, so normal authenticated users do not need execute access.

**Change:** Added and applied migration `20260506180718_harden_recalculate_grain_prices_cad_execute_grants.sql`. It revokes function execute from `PUBLIC`, `anon`, and `authenticated`, then grants execute only to `service_role`.

**Verification:** Live query against `information_schema.routine_privileges` shows only `postgres` and `service_role` have `EXECUTE` on `public.recalculate_grain_prices_cad(date, date)`.

---

## 2026-05-06 - Canola packet supply-year selector fixed

**Driver:** The Canola validator passed, but the generated deterministic read still selected the older `2024-2025` supply-disposition row even though the live table had the new `2025-2026` Canola crop baseline. The packet RPC was ordering supply rows by `created_at` only.

**Change:** Added and applied migration `20260506181223_fix_canada_packet_supply_crop_year_selection.sql`. `get_canada_thesis_packet()` now prefers the requested packet crop year before falling back to older supply rows.

**Verification:** Live packet query now returns `supply.crop_year='2025-2026'`, `production_kt=21804`, and `seeded_area_acres=21617100` for Canola. `npm run canola-market-read -- markdown` now includes the 2025-2026 production, seeded acres, harvested acres, yield, and 2026 intended seeded acres in the facts section.

---

## 2026-05-08 - Bull/Bear Thesis Board V1 release candidate

**Driver:** The `/goal` for Bushel Board was to turn the data spine into a working Bull/Bear Thesis tab that uses current Canada and US packet facts, updates as source collectors land data, and avoids legacy Grok/grain-intelligence prose as current thesis text.

**Change:** Added `/thesis` as a first-class dashboard tab. The page reads `get_thesis_board_cached()` through `lib/queries/thesis-board.ts` and renders per-grain stance, confidence, bull case, bear case, freshness warnings, packet metrics, and source provenance. Desktop and mobile nav now link to Thesis.

**Data path:** Added `thesis_packet_cache` and `get_thesis_board_cached()` in migration `20260509033204_add_thesis_packet_cache.sql`. Added `scripts/refresh-thesis-packet-cache.ts` and `npm run refresh-thesis-cache`; the cache was warmed with 21 packets: 16 Canada grains and 5 US markets.

**Collector wiring:** Added `scripts/run-collector-with-thesis-cache-refresh.ts` and `npm run collect:*` wrapper commands for CGC, crop progress, Grain Monitor, export sales, Producer Cars, CFTC COT, and WASDE. The wrapper runs the mechanical importer first and refreshes the thesis cache only after a successful collector exit. Direct wrapper dry-runs skip cache refresh; npm argument forwarding was unreliable on Windows, so dry-run docs point to importer-specific commands or direct wrapper calls.

**Second opinion:** Gemini 3.1 Pro Preview audited the wrapper. Actionable fixes were applied for Windows CLI execution, `.cmd` / `.bat` handling, dry-run flag detection, failed-spawn diagnostics, and the refresh-failure retry contract. A package-level Gemini pass timed out with no usable result.

**Verification:** `npm run test -- thesis-board` passed with 8 tests. Wrapper smoke checks passed for help, direct `tsx`, direct dry-run, `.cmd`, missing-command diagnostics, and success-path refresh. `npm run build` passed after the final wrapper edits.

---

## 2026-05-09 - Predictive harness scheduler corrected to Codex Automation

**Driver:** The Canola predictive harness finished as a local, deterministic, no-write workflow, but the planning packet still left Hermes automation as a possible future runner. Kyle explicitly decided not to use Hermes automation for this harness.

**Change:** Created active Codex automation `canola-predictive-harness-no-write-review` for the weekly no-write review path. Added `docs/plans/2026-05-09-codex-automation-predictive-harness-ops.md` as the operating note and marked the earlier Hermes kickoff/results packets as superseded for automation ownership.

**Boundary:** The automation is review-only. It runs forecast tests/build guardrails and static scans, reports hashes or missing artifacts, and requires a fresh review gate before any sidecar writer, live Supabase read/write path, model/API runner, dashboard read, or production integration.
