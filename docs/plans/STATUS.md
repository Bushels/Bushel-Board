# Bushel Board - Feature Status Tracker

Last updated: 2026-07-15

Wheat farmer UX Phase 2 complete (2026-07-15 evening): Prairie mini-map, GEE stress thumbnail → `/data`, price agreement motion. Mobile stack remains vertical for reachability; swipe carousel deferred to Phase 3. Score authority unchanged. Plan: `docs/plans/2026-07-15-wheat-farmer-ux-redesign.md`.

Wheat farmer UX Phase 1 complete (2026-07-15 evening): `WheatVisualPillars` is live on farmer `/thesis` under the published-desk stance hero — Prairie MB/SK/AB package status, GEE crop-stress (watch-only → `/data`), Spring/HRW/SRW price basket. Score authority unchanged (published desk + scorecard fallback). Builders + page tests 51/51; production build; Track 54 browser smoke desktop+mobile green. Plan: `docs/plans/2026-07-15-wheat-farmer-ux-redesign.md`. Phase 2 still open (mini-maps / swipe polish).

Hermes/Grok control-plane + Wheat cleanup (2026-07-15 PM): deep audit `docs/audits/2026-07-15-wheat-centric-board-deep-audit.md` (score **74/100**). Deleted 33 orphan multi-grain dashboard/lib modules; farmer nav demoted Source Spine / Data Flow; README rewritten Wheat-centric. **Automation ownership moved to Hermes Agent** with Grok 4.5 for operator/X work: mechanical `bushel-collect-*` + watchdogs + no-write X pulse as `--no-agent` scripts; `bushel-wheat-daily-operator-brief` (17:00 MT M–F) and `bushel-wheat-friday-desk-orchestrator` (18:00 MT Fri, preflight-only default). Schedule map: `docs/reference/hermes-bushel-board-schedule.md`. Grok thesis-writing path remains tombstoned. Remaining: thesis monofile split, Wheat Data P2 re-home of kept charts, disable Claude Desktop Routines after clean Hermes week.

Published Wheat thesis authority repair (2026-07-15): `/thesis` now reads same-period `market_analysis` and `us_market_analysis` rows as the farmer-facing weekly thesis-of-record. The deterministic packet scorecard remains visible as the mechanical evidence cross-check and serves as fallback only when no matching published desk row exists; reviewed daily overlays still own current-day overrides. This repairs the production mismatch that displayed cached mechanical -6 Lean Bear and a 43% confidence-scaled rail position after the desks had published Canada +22/conf 42 and U.S. +32/conf 45. The reconciled public headline is +27 Mild Bull/conf 44, with CA/US desk values shown explicitly. Focused board and page tests lock same-period promotion, stale-period rejection, exact published tier rendering, and the separation between pressure position and confidence.

Wheat thesis source-integrity repair + complete republish (2026-07-14): repaired four interacting failures rather than editing the narrative around them. MGEX Spring Wheat now resolves Barchart continuous `MW*0` to the active contract/session date (`MWU26` at $6.58), and 21 falsely restamped post-expiry `MWK26` rows were removed. USDA crop progress now stores Winter and Spring Wheat as separate physical rows keyed by `wheat_class`; 2,502 source records were processed for 2024-2026, producing 2,488 class-safe live rows and all 528 proven-replaced mixed rows were deleted. The Prairie importer no longer pairs a July Saskatchewan staging report with an old seeding table and admits Manitoba Spring Wheat condition only when the source states it. The desk read registry now exposes `sk_cash_prices`, `canada_crop_progress`, and `gee_crop_stress`; the U.S. export-sales SLA is 14 days because the Thursday release describes the prior Thursday's week ending. A missed CGC week 48 was recovered live (4,415 rows; all validation checks; 16/16 collector heartbeats). Full Opus desk reruns published U.S. Wheat +32/conf 45, then Canadian Wheat +22/conf 42 using the same-run U.S. cross-read; `desk:postcheck` refreshed 12/12 thesis packets with 0 failures. Scheduled Claude task skills and swarm docs now carry the continuous-contract, class-boundary, and current Prairie-read rules.

Desk catch-up run published + wheat-only hardened (2026-07-12): diagnosed and closed the June-14→July-12 stale-thesis outage — the Friday desks had been firing but aborting at preflight on stale-source SLAs (grain_prices had a 4-day SLA but NO scheduled collector; holiday-weekend machine-off gap; delayed Routine runs silently no-oping with no source_runs row), and us-desk-weekly was missing from the runner entirely. Fixes: collect-prices Routine (Fri, before COT + desk), one-shot grain_prices self-heal at desk preflight, us-desk-weekly re-created + ENABLED (Kyle-approved), collect-producer-cars Routine created, delayed-run rules added to collector/desk prompts, CLAUDE.md monitoring queries fixed for live schema (source_runs has started_at/finished_at not source_date/completed_at; pipeline_runs has no created_at). Both desks then ran manually end-to-end under Fable 5 (model gates verified as a floor live): CAD Wheat +32/conf 60 and US Wheat +35/conf 35 published, desk:postcheck OK, thesis cache 12/12. Kyle reaffirmed WHEAT-ONLY firmly (memory feedback_wheat_only; the 2026-07-12 run's non-wheat spend is what prompted it — Routine prompts, both swarm docs, and CLAUDE.md now all carry the wheat-only scope). Data traps found live: grain_prices.change_pct is 1-day not weekly; MGEX MWK26 feed frozen since 2026-05-14 and ICE RSK26 since 2026-05-15 (frozen-feed importer detection spawned as follow-up); CBOT ZO thin-market bad tick; posted_prices empty (no operator bids) so Rules 12/13/15 ran degraded; the hardcoded /thesis crop-progress card was replaced with live usda_crop_progress data (lib/queries/wheat-crop-progress*.ts).

Desk swarm headless runner wired + live-verified (2026-07-02): the parked June-16 service-role data plane (`scripts/desk/desk-cli.ts` + contracts/schemas/row-builders/reads/freshness/dates, 35 unit tests) is now fully wired. `package.json` gained `desk:cad` / `desk:us` / `desk:postcheck`. Both swarm prompt docs (`grain-desk-swarm-prompt.md`, `us-desk-swarm-prompt.md`) converted from Supabase-MCP raw SQL to CLI commands (preflight/read/knowledge/write/fail/postcheck), which retired live schema bugs at the source: the broken `pipeline_runs (source, metadata)` insert, NULLs into NOT NULL `pipeline_runs` columns on the US side, market-year resolution off the deprecated `usda_wasde_estimates`, a `us_score_trajectory.week_ending` SELECT (column doesn't exist — order by `recorded_at`), top-level Track-46 fields the zod write contract would strip (now nested `llm_metadata.track_46`, `tier` renamed `stance_tier`), and a prose `historical_context` example (column is jsonb). All desk scout/analyst/meta-reviewer agent defs converted to the CLI read surface (meta-reviewer WRITE paths to `desk_performance_reviews` remain a documented headless gap — run Saturday reviews interactively until a review-writer CLI lands). Live proof 2026-07-02: `desk:cad` + `desk:us` preflight green against production (CAD week 46; US market_year 2025 / week_ending 2026-06-26; all SLAs pass), read + knowledge smokes verified, both write envelopes validated in dry-run (zero writes). Real writes stay approval-gated (`--write` + `DESK_WRITE_APPROVAL`). Scheduled tasks `grain-desk-weekly` / `us-desk-weekly` SKILL.md rewritten to the CLI contract — still DISABLED pending Kyle's explicit enable + `DESK_WRITE_APPROVAL` in `.env.local` (superseded 2026-07-02/2026-07-12: both now ENABLED). Ops finds from the same session's audit: `collect:prices` had no firing schedule (prices stale at 2026-06-26; manually refreshed — 65 rows upserted + 12/12 thesis-cache refresh) and `gee_crop_stress` last wrote 2026-06-15 despite Jun 19/26 task runs (suspect Earth Engine token refresh; see the task SKILL's auth note).

Production UI revert incident + wheat-first merge + dead-code deletion (2026-07-11 PM): merging PR #18 (docs-only) to master triggered a fresh Vercel production build from master, which replaced the deployed-but-never-merged codex/wheat-first-thesis UI — production visually reverted from the Wheat cockpit to the old all-grain board. Root cause: production was pinned to a side-branch deployment; ANY push to master was going to clobber it. Fix: codex/wheat-first-thesis is now merged into this branch (conflict-resolved STATUS.md), restoring the Wheat-only /thesis cockpit on master's lineage. Dead code deleted per operator direction (Kyle: "I want that all gone, I just want the wheat analysis"): components/overview/* (8 components, hidden since 2026-06-10, grep-verified unimported) + their 2 orphaned test files; the /overview redirect stays for old links; recover from git history before ce86d7c if ever needed. Lesson: never leave a production deployment pinned to an unmerged branch — merge the branch or expect the next master push to revert the site.

Desk scoped to WHEAT ONLY (2026-07-11 PM, Kyle decision): the Friday grain-desk-weekly swarm now dispatches scouts/specialists/chief for Wheat alone — Step 0.2 active list = ['Wheat'], Wave B specialist dispatch parked, pipeline_runs arrays scoped, in-run meta-review checks + Saturday desk-meta-reviewer given Wheat-only forms (batch /16 forms retained as the re-enable contract). Grain detail + My Farm for the 15 parked grains keep serving their last desk rows behind assessDeskThesisStaleness(). The 16-grain engine is parked, not deleted — the re-enable list and tier budgets stay documented in the prompt. Model matrix from the same session: desk chiefs + Saturday reviewers = Opus-class OR HIGHER (Step 0.0 is now an explicit floor — the old Opus-only wording would abort under a Claude-5-family model); parsing collectors = Sonnet; script-wrapper collectors + watchdogs = Haiku. ⚠ Live CCR trigger collect-wasde-archive is pinned to outdated claude-sonnet-4-6 and its Supabase connector currently lacks the Bushel Board project — re-pin + re-auth before the monthly fire on the 13th.

Wheat-desk audit + swarm wheat-first refit (2026-07-11): audited the weekly data + the CAD desk swarm against the Wheat-first pivot (branch codex/wheat-first-thesis, still unmerged to master). Data audit: repo reference CSV verified clean through week 36 (no gaps, no dupes, monotonic cumulative wheat series), but LIVE verification of weeks 37-48 was blocked from the cloud session (Supabase MCP account lacks the project; network policy 403s supabase.co + grainscanada.gc.ca) — runnable live-audit SQL shipped in docs/plans/2026-07-11-wheat-desk-audit.md. Critical schema finding: all 8 pipeline_runs INSERTs in both swarm prompts still violated NOT NULL constraints after the June 9 repair (grains_requested missing everywhere; US side passed NULL crop_year/grain_week) — every fail-loud row silently died; all rewritten schema-legal plus a status='running' ledger row (Phase 0.5) so mid-run chief death leaves a tombstone. Wheat-first refit: new FLAGSHIP effort tier (Wheat only — mandatory Phase 4.5 deep pass weekly, 4-6 reasoning items, deeper L2 budget, never demotable), US desk cross-read operationalizing R-CA-WHT-01 (Friday desk ORDER SWAPPED: us-desk-weekly now fires 6:47 PM MT before grain-desk-weekly 7:45 PM MT — Kyle must re-register both Routines), wheat-class lens (CWRS/CWAD/CPS/winter SQL aggregation, R-CA-WHT-05), llm_metadata.wheat_cockpit handoff block for /thesis, and freshness guardrail degrades (only stale CGC aborts; stale COT/prices degrade with confidence caps — the April outage was a stale-price full-desk abort). Scout rewiring: basis-scout + sk_cash_prices (CWRS cash tape, R-CA-WHT-07), supply-scout + statcan_wds_raw farm stocks (R-CA-WHT-06) + canada_crop_progress new-crop lens, sentiment-scout marked paused-voting/retired-view lanes dead, price-analyst SQL fixed (5 nonexistent columns), stale MGEX-availability claims corrected (Barchart fallback exists). Both meta-reviewers: fixed nonexistent-column SQL, recency-anchored audit query (rollover-safe), missed-run detection, Wheat Pass-0 with dedicated wheat_accuracy line. Proposed Saturday desk-output-watchdog (npm run check:desk-freshness) documented in collector-task-configs.md. Full audit report: docs/plans/2026-07-11-wheat-desk-audit.md.

Wheat thesis closeout handoff (2026-06-24; headline-authority rule superseded 2026-07-15): the next session should start from `docs/plans/2026-06-24-wheat-thesis-goal-handoff.md`. Current verified state is a visually redesigned Wheat-first `/thesis`, a first metric-relationship loop, a first Wheat pressure-map model/panel, repaired deterministic Wheat price scoring across Spring Wheat/HRW/SRW when multiple Wheat-class futures rows are present, a reconciliation judge with deciding-datum freshness and counterweight language, a USDA Wheat source-sweep panel for Crop Progress, WASDE balance, Export Sales, and Quarterly Stocks, a node-and-edge relationship spiderweb where side/distance/rank/edge thickness communicate score impact, a visible three-contract price-basket proof strip with packet-window trend context plus 60-day historical futures sparklines from `get_wheat_price_history(60)`, and a bounded historical export context panel from `get_wheat_export_history(16)`. The June scorecard-owned headline rule is historical: same-period published desk rows now own the weekly public headline, while the scorecard remains its mechanical audit and fallback. The CGC Wheat export formula was repaired: Terminal Disposition export rows are cross-checks, not additive exports; cached week-45 Canada Wheat now shows 193.9 kt current-week exports and a 30.2% export/delivery ratio. Final price/cache refresh completed 12/12 with source watermark 2026-06-25T00:43:11Z, and the source-freshness watchdog reports 0 alerts. Hindsight memory was rechecked for Wheat/USDA/Rule 21/Hermes context and returned no Wheat-specific documents, memories, tags, or mental models, so repo docs remain the operating truth. Remaining work: deepen the USDA Wheat sweep with live-packet verification, make judge explanations source-specific, decide whether spiderweb distance should include source authority/freshness/replay accuracy beyond weighted score impact, and continue no-write treatment for Hermes/Grok pulse evidence unless tied back to official or admitted market data. An unfinished Wheat X-sentiment prototype was parked as a design note only because its draft scoring math failed directional tests; do not wire X pulse into the scorecard. Skill/agent cleanup also added `.agents/skills/_eval-core`, tightened the existing `grain-report` Wheat loop guidance, marked retired Grok pipeline skill usage as archive-only, and corrected CFTC wording so positioning stays a timing/crowding input.

Wheat Bull/Bear thesis board redesign (2026-06-23): `/thesis` remains Wheat-first, but the farmer-facing hero is now one Wheat decision read instead of a Canada-vs-USA frame. CA and US packets are presented as evidence-geography chips inside the Wheat read. The first screen now uses a large confidence-scaled Wheat score, compact bull/bear evidence cards, source-geography chips, a source-to-pressure-to-read mini-flow, and a latest USDA Crop Progress card for the 2026-06-22 release covering the 2026-06-21 week ending. The USDA card records winter crop harvest at 40% versus 25% last week, 18% last year, and 24% five-year average; winter crop good/excellent at 26% versus 27% last week and 49% last year; spring crop headed at 16% versus 6% last week, 15% last year, and 16% five-year average; and spring crop good/excellent at 54% versus 55% last week and 54% last year. Pressure lanes now render as sorted diverging bars. The source-lane swarm/reconciliation contract is documented in `docs/plans/2026-06-23-wheat-bull-bear-thesis-swarm-redesign.md`: lane agents trace source rows to bull/bear pressure and a judge agent reconciles those packets into one consistent Wheat read while preserving public wording guards and the no-advice boundary.

Flood / Excess Moisture Watch V0/V0.1 (2026-06-22): GEE spike proved enough signal to ship a watch-only `/environmental/flood-watch` surface under the new Environmental tab; legacy `/data/flood-watch` redirects there. The reproducible script is `scripts/gee/flood_excess_moisture_watch.py` via `npm run gee:flood-watch`; the first event artifact covers 2026-06-14 through 2026-06-21 and is published as `public/data/flood-watch-v0.json`. Totals are 1,298 observed surface-water acres, 8,772 excess-moisture acres, 3,245 forecast-risk acres, and 44,850 watch acres. Source freshness was recorded in `source_runs` as `gee_flood_excess_moisture_watch`. V0.1 adds official soil-moisture anomaly visual context under acreage hotspots: AAFC weekly SMOS anomaly for Canada and Crop-CASMA SMAP hybrid weekly anomaly for the contiguous U.S. The original 2.5-degree rectangles remain the reproducible aggregation method but are no longer shown as public map squares. Public copy is explicitly watch-only: acres at risk, not confirmed damaged acres; the official underlay is regional moisture context, not crop-loss proof. The page includes public wet/dry field reports into `feedback_log` through `/api/environmental/field-check` for calibration/review only; reports include normal, too wet, standing water, access blocked, dry topsoil, and drought stress. Mapbox/react-map-gl remains the fastest v0 stack because the existing dashboard already supports token wiring, raster underlays, and the choropleth pattern. See `docs/plans/2026-06-22-flood-excess-moisture-watch-v0.md`.

Wheat-first Bull/Bear `/thesis` surface (2026-06-16): normal farmer-facing `/thesis` now shows one active grain, Wheat, through the centralized active farmer display allowlist (`lib/thesis/active-grain-display.ts`). Corn, Soybeans, Durum, Canola, Barley, and Oats remain in the shared V1 harness/profiles and audit/model code, but they are not visible as active farmer reads. The page now leads with a Wheat stance meter, top bull/bear evidence, source-to-pressure-to-read data flow, pressure-lane breakdown, "What feeds this read", Wheat-only local area context, source health, Wheat pressure map, and Wheat packet details. `/thesis?audit=1` keeps operator proof including the full-grain graph context. Browser smoke now checks desktop and 375px mobile `/thesis` plus `/thesis?audit=1`, with normal route forbidden text covering the old all-grain board headings and non-Wheat active cards. Proof passed: `npm run typecheck`, `npm run lint` (0 errors; 3 unrelated existing warnings), focused thesis/data-universe/grain-impact/browser-smoke Vitest slice (133/133), `npm run build`, and `npx tsx scripts/run-track54-browser-smoke.ts --settle-ms 2000`. No Track 54 write-mode path, Supabase write-mode change, thesis-cache write, production Grok write, or retired Grok writer revival was run.

Note: earlier June 9 `/thesis` entries below describe the prior all-grain farmer-first board. They are retained as history; the current normal farmer display is Wheat only.

WHEAT-FIRST direction + GEE crop-stress lane (2026-06-15): set the strategic direction to **wheat-first, depth before breadth** — perfect/validate the bull/bear engine on wheat before deepening the other 15 (desk still serves all 16). Shipped this session: (1) **Stance Model V2** design (`docs/plans/2026-06-12-stance-model-v2-design.md`) — relation-overlay rating, bounded international/FX/on-farm-stocks contexts, per-grain profiles, reserves lane, debate **Rules 20–21** (stance-change ledger + reconciliation), and the mapper-level **Rule-9 positioning fix** (±20→±10, COT is timing not direction). (2) **GEE satellite crop-stress lane** (`gee_crop_stress` table + `scripts/gee/*`, scoping `docs/plans/2026-06-13-gee-crop-stress-lane-scoping.md`): MODIS NDVI + SMAP soil moisture z-scored vs 5yr baseline for **US HRW (per-state + belt), Russia winter, Russia spring**; **validated vs NASS r=0.93/0.98**; collector `npm run collect:gee-crop-stress` (Fri 11am MT ahead of the desk swarm); **watch-only** until multi-season validation. EE access via personal token on project `monette-494717` (org policy blocks SA keys — see `docs/reference/gee/service-account-setup.md`). (3) Wheat thesis-of-record persisted: Canada +9 (HOLD) / US +11. (4) `collect-crop-progress` `UnboundLocalError` fixed. (5) **Wheat-first site redesign direction** (`docs/plans/2026-06-15-wheat-first-data-viz-redesign.md`) — data-rich viz + mapping + a dedicated data page; the GEE crop-stress map is the hero; next session builds it (start from that doc's §10 handoff). [2026-07-03 note: this entry arrived via the codex/data-layer-foundation-v1 merge that restored the GEE collector to the working branch.]

Friday bushel desk outage + repair (2026-06-09 PM): discovered the Friday desk swarms stopped writing after 2026-04-25 — `market_analysis` last wrote week 36 (2026-04-18, CAD) and `us_market_analysis` 2026-04-20, with `pipeline_runs` showing 2026-04-24/25 failures (root: phase 0.3 freshness guardrail on then-stale grain prices) and zero rows of any status afterward, meaning the Claude Desktop Routines stopped firing. Repo-side fixes landed in both swarm prompt docs: `pipeline_runs` inserts now use the CHECK-legal `triggered_by='cron'` with the routine name inside the JSON (was `grain-desk-weekly`/`us-desk-weekly`/`claude-agent-us-desk`, all CHECK violations), the US prompt no longer references nonexistent `source`/`metadata` columns, and Step 0.0 now accepts any Opus-class model instead of hard-pinning `claude-opus-4-7` (which would abort the desk under newer Opus with a zero-trace death because the abort row itself violated the CHECK). Surfaces guarded: grain detail and My Farm now run `assessDeskThesisStaleness()` (`lib/utils/thesis-staleness.ts`) — a desk thesis 2+ weeks behind the display data week shows a "Last desk read: week N - N weeks old" chip instead of the stance badge, a stale notice with a Thesis-board pointer replaces the current-read framing, and the stale stance no longer drives haul/hold recommendation inputs. Operator action: Kyle must re-enable/recreate the `grain-desk-weekly` and `us-desk-weekly` Claude Desktop Routines from the updated prompt docs before Friday 2026-06-12.

Farmer-facing read linkage (2026-06-09 PM): normal `/thesis` grain cards now render a compact "What feeds this read" strip (`components/dashboard/grain-read-linkage.tsx` + `lib/thesis/grain-read-linkage.ts`) derived from the same deterministic grain impact graph as the audit surface: official-input/price-context/watch-lead counts, the top-ranked lanes as chips, and a plain-English boundary line that watch leads never move the score. Display only — no new scoring authority; full graph detail stays in `/thesis?audit=1`. Focused tests cover per-grain summaries, exact coverage-count mirroring, parked-gap exclusion, and official-first ordering.

Overview hidden (2026-06-10): /overview now redirects to /thesis (Kyle: no current utility); nav links removed (desktop + mobile), root redirect and post-auth destination repointed to /thesis, grain-page back links updated, and the Track 54 smoke + readiness route contracts trimmed to /thesis + /thesis?audit=1 in lockstep (readiness fixtures repointed). Overview page components retained under components/overview/ per the retire pattern; restore by reverting app/(dashboard)/overview/page.tsx.

Five-source admission sweep (2026-06-10): adversarially-verified web research on all five next data lanes, then two importers shipped and LIVE: (1) scripts/import-statcan-wds.py - StatsCan WDS REST for principal field crops (seeded area + production, 32-10-0359; tri-annual stocks, 32-10-0007; Canada + AB/SK/MB) AND monthly canola crush (32-10-0352 pinned vectors) into new statcan_wds_raw (1,164 rows backfilled; April 2026 crush 1,052,245 t verified against the research spot-check); (2) scripts/import-sk-cash-prices.py - Saskatchewan Dashboard weekly provincial average cash prices, 10 crops into new sk_cash_prices (7,464 rows since 2020; latest canola $746.89/t Jun 3). Migration 20260610190000 applied live; collect:statcan + collect:sk-prices wrappers added; registry entries written including the honest cash-bids boundary (no clean public per-elevator basis source exists; PDQ ToS forbids automation; posted_prices stays the local-bid lane). Parked with exact activation intel in the registry: per-crop condition parser (AB table debuts ~Jun 19 report; coordinate-based PDF parsing required) and CN rail performance (deterministic weekly PDF URL verified; adds order/fulfillment metrics the Grain Monitor lacks). Scoring/mapper admission for the new lanes is deliberately NOT included - data flows first, bounded scoring follows as its own gated step.

"In their area" layer V1 (2026-06-09 PM): normal `/thesis` now has a farmer-facing `Your area` card directly under the major matrix. A farmer enters a postal code (no account needed; FSA stored in a `bushel_area_fsa` cookie on-device via server actions in `app/(dashboard)/thesis/area-actions.ts`) and the board localizes: (1) province-level CGC Primary elevator deliveries per V1 grain — this week, season cumulative, and same-week season-over-season pace ("X% ahead/behind last season") from `lib/queries/area-read.ts` + pure builders in `area-read-utils.ts`; (2) local operator bids via the existing `get_area_prices(fsa)` RPC with price and "local price gap" (basis), with an honest empty state while `posted_prices` has zero rows (doubles as the operator onboarding hook). FSA→province mapping covers AB/SK/MB/BC (`lib/utils/area.ts`); out-of-coverage FSAs say so plainly. Browser-smoke now requires the `Your area` marker on both `/thesis` and `/thesis?audit=1`. Verified live end-to-end in production build: postal entry → cookie → "Local context for S0K (Saskatchewan)" with real CGC week-42 flows and live pace reads. Note for review: `get_area_prices` is executable by anon (PUBLIC execute never revoked despite an authenticated-only GRANT) — currently treated as intended since posted bids are operator advertisements, but it is a deliberate-decision flag. Follow-ups: logged-in prefill from `profiles.postal_code`, basis-vs-futures comparison once bids exist, and area context on grain detail pages.

Farmer-first /thesis split (2026-06-09 PM): normal `/thesis` now leads with the market read — Top Takeaway, the all-grains quick scan, and the major matrix come first, followed by the source-health banner and the compact current snapshot, then the Wheat Pressure Map and grain cards. ALL operator telemetry moved behind `?audit=1`: Daily Update Status (including the daily decision path, today update window/checklist, automation gate progress, and readiness/browser-proof freshness), the X Pulse Watch panel, Weekly Data Intake, and the Source Packets KPI grid. The Track 54 browser-smoke contract moved in lockstep: normal `/thesis` now requires only farmer markers (takeaway, quick scan, source health, board update mode, score source, "What feeds this read", pressure-map markers) and explicitly FORBIDS operator sentinels ("Daily automation gate progress", "Weekly Data Intake", "Readiness proof", "Track 54 production gate", "Watch-only social evidence"); the audit route keeps the full superset. Gotcha caught by the smoke itself: the panel title "X Pulse Watch" cannot be a forbidden sentinel because case-insensitive whole-word matching collides with the legitimate farmer-facing "X Pulse watch leads" copy in the pressure map — the moved panel's unique badge phrase is used instead. Page tests updated: 18 moved-panel tests now render in audit mode, plus new farmer-first ordering and operator-absence tests (38/38 passing). Live production proof: farmer order verified, all six smoke checks green with zero console errors and zero forbidden hits.

Data Universe — farmer-facing R3F system visualization (2026-06-09 PM): new public route `/data-universe` ("Data Map" in desktop + mobile nav) renders the entire data system as an interactive React Three Fiber scene: 17 source lanes on the outer ring (13 official, 2 price context, 2 watch) flowing through 6 pressure lanes into the 7 public V1 grains and the central Bull/Bear board, with 58 weighted connections, animated flow particles, hover/click node selection with plain-English detail, drag-to-orbit with autorotate, reduced-motion respect, and a jsdom/WebGL fallback. Model: `lib/thesis/data-universe.ts` (pure, derived from the deterministic grain impact graphs — adds no scoring authority; confidence-weighted board reads color the grain nodes). Components: `components/dashboard/data-universe-scene.tsx` + `data-universe-client.tsx` (next/dynamic ssr:false). Page: `app/(dashboard)/data-universe/page.tsx`. Two real bugs found and fixed during build: (1) the WebGL support probe leaked one context per mount — browsers cap live contexts and the leak starved the real canvas into `THREE.WebGLRenderer: Context Lost` (probe now releases via `WEBGL_lose_context.loseContext()` and caches per page load; same release added to the audit constellation's probe); (2) React StrictMode's dev double-mount permanently lost the R3F canvas context (a canvas can never get a second context of the same type), fixed with a self-healing remount: `webglcontextlost` bumps a Canvas key to mount a fresh canvas element, capped at 3 recoveries before falling back — which also covers real-world GPU resets. Dependencies added: `@react-three/fiber@^9`, `@react-three/drei@^10`.

WASDE veg-oil data gap (2026-06-09 PM): the coverage matrix's top Canola admission priority (China policy / palm oil / global veg-oil demand context) points at `usda_wasde_raw`, but the live table only holds the five US commodities the importer pulls (Corn, Soybeans, Barley, Oats, Wheat — `scripts/import-usda-wasde.py` PSD codes 0440000/2222000/0430000/0452000/0410000). No rapeseed, no vegetable-oil, no world-level rows exist. Promoting that watch lane first requires extending the WASDE importer with world-level rapeseed/palm/soy-oil PSD pulls as its own source-admission task (source identity, cadence, freshness proof, mapper, tests).

## Feature Tracks

Current active track note (2026-06-01): Track 54 is the active implementation lane for the supervised Grok/X scout, daily prices, bounded daily thesis review, and Friday desk-swarm X bundle. It builds on Track 51's source-backed `/thesis` board and must not revive the retired Grok thesis-writing chain. March Grok pipeline plans remain historical only; `docs/plans/2026-05-27-x-signal-valuation-guardrail-framework.md` is conceptually accepted but superseded by `docs/plans/2026-06-01-grok-x-price-daily-thesis-completion-plan.md`.

Grain relationship visualization audit (2026-06-09): audit-only V1 is implemented locally on `/thesis?audit=1`. It adds a Grain Impact Graph, cross-grain relationship map, native Three.js relationship constellation, influence board, conversation matrix, selected-grain summary, rank-proof cards, current driver stack, and per-grain ranked network. The feature is an operator proof surface only: it does not change score weights and does not render on normal `/thesis`. React Three Fiber remains deferred; native Three.js is enough for the audit canvas until the model proves it needs a fuller 3D component layer. Latest local proof passed focused tests, typecheck, lint, production build, Playwright desktop/mobile screenshots, current-server route smoke, and desktop/mobile Three.js canvas pixel checks proving nonblank render, animation, clickable node selection, and zero browser console errors.

Grain relationship visualization review hardening (2026-06-09 PM): a pre-checkpoint adversarial review pass fixed 24 SVG `var(--token)` fill/stroke attribute violations (now theme-aware Tailwind `fill-*`/`stroke-*` classes), made the explorer's rank-proof strings derive from newly exported model constants (`sourceAuthorityWeight`, scope rank multipliers) instead of hardcoded copies, and replaced inline `boardReads = []` client-component defaults with a stable module constant so an omitted prop cannot reboot the Three.js scene on every re-render. Nine new tests pin the ranking math branches and the authority-x-scope rank invariant, the Three.js mount/unmount dispose contract, the confidence-weighted vertical position formula, and the empty-grains null render. Post-fix proof: 61/61 focused tests, typecheck clean, lint 0/0, production build passed, fresh six-check browser smoke clean. A single non-reproducing React #418 hydration exception was observed once on first desktop `/thesis` load against the pre-fix build and is logged as an intermittent to watch.

Track 54 operator check (2026-06-09 early MT): current no-write proof is still a hold, but the active blocker shifted from "no usable scout runner" to "artifact window not complete yet." Latest heartbeat/readiness proof generated from `scratch/track54-readiness/latest-readiness-report.json` reports `overall_status = hold_manual_review`, `production_writes_enabled = false`, `browser_smoke_clean = true`, and fresh browser/readiness proof. `daily_pulse` is now `3/5` artifact days found, `2` clean artifact days, `3` clean artifact days still missing, with `1` accepted signal and `1` decision-grade accepted signal in the current reviewed window. Because the daily gate uses a rolling seven-day review window, readiness now projects the next eligible `daily_pulse` artifact dates as 2026-06-09, 2026-06-10, 2026-06-11, and 2026-06-12, with earliest candidate date 2026-06-12 after the June 2 clean day falls out. The selected 2026-06-08 artifact is the Hermes terminal no-write artifact `data\X Scout Runs\2026-06-08\daily_pulse-hermes-terminal-20260609T020001Z-raw.json`, SHA-256 `57009259510318769b7191b23d1ff1373cc08979bdae0cdd71306c603f045339`, with fresh price proof and zero accepted signals. Friday-deep remains `0/1` found, `0` clean, and `1` clean Friday artifact still missing, with earliest candidate date 2026-06-12. Original Grok runner credentials still report `credential_issue = missing_credential`, but `hermes_terminal.ok = true` with xAI OAuth, `model_smoke_ok = true`, `grok-4.3`, and `x_search` listed. Direct terminal proof must use `hermes --model grok-4.3 --provider xai-oauth`; the plain `xai` provider path expects `XAI_API_KEY`. The active primary no-write collectors fall through to deterministic artifact-health Hermes fallback when original Grok preflight fails, while readiness still blocks write-mode approval until original Grok auth or `XAI_API_KEY` is restored. The automation-run summary marks the old 2026-06-08 primary collector and daily health-check blocks as superseded by later manifest updates, so `automations_with_failed_or_blocked_latest_run = []` until the repaired prompts get their next scheduled proof. Heartbeat and automation-run summaries expose `clean_artifact_days_found`, rolling projected dates, and readiness manifest-audits that the active `track-54-promotion-review` heartbeat asks for the clean-day field. The bounded daily thesis review dry-run still builds and writes nothing; it found the earlier Wheat X Pulse decision and skipped it as already applied (`writes_attempted = 0`, `writes_skipped_existing = 1`). Canonical flow is now documented at `docs/reference/track54-daily-bull-bear-thesis-flow.md`: official source packets remain the thesis base, prices are context, Grok/Hermes only produce local X scout artifacts, promotion requires clean artifact gates plus Kyle approval, daily writes are bounded trajectory ticks, and Friday desk swarms remain the thesis of record. Next safe action is to let the June 9 through June 12 no-write windows run, using Hermes fallback only through the scheduled/deterministic health path if the same-day artifact is missing or invalid, and refresh original Grok credentials before any write-mode approval.

Track 54 write-approval hardening (2026-06-08 late MT): the write gate now requires the exact reviewed artifact window from the mode-scoped promotion brief before any future write-mode command can run. The Track 54 approval phrase must be paired with `--approval-review-from YYYY-MM-DD` and `--approval-review-to YYYY-MM-DD`; missing, malformed, or reversed windows fail before the artifact reviewer reaches write mode. Proposed write-mode Codex automations now also carry structured `approval_review_window` evidence with mode, from/to dates, artifact-day counts, accepted-signal counts, and decision-grade counts, and readiness rejects proposal drift if that structured window no longer matches the gate. The Grok scout CLI help now labels those approval-window flags as required. Focused proof passed with 21 tests across write approval, write gate, daily thesis packet, and daily update idempotency plus 80 tests across promotion brief, readiness report, and write gate; the live daily thesis review dry-run remained no-write with `writes_attempted = 0` and `writes_skipped_existing = 1`.

Track 54 operator check (2026-06-07): latest no-write proof is still a hold, but the blocker is now precise. `npm run track54:grok-preflight` proves Grok CLI `0.2.14` is installed while credentials are absent: `credential_ok = false`, `credential_source = none`, `credential_issue = missing_credential`, no CLI auth file, and no `XAI_API_KEY`. `npm run track54:recover-after-grok-login` now delegates to the fixed `track54:artifact-health:both-retry` alias, avoiding Windows/npm 11 wrapper flag noise while preserving the same dry-run recovery path. Live recovery proof on Sunday 2026-06-07 launched no Grok scout because neither daily_pulse nor friday_deep was due, refreshed readiness through the top-level wrapper, and returned `ok = true`. Latest readiness proof after build is `overall_status = hold_manual_review`, `production_writes_enabled = false`, and `browser_smoke_clean = true`; daily_pulse is still 2/5 clean artifact days in the reviewed readiness window with 4 accepted and 3 decision-grade accepted signals, 2026-06-03 and 2026-06-04 are explicit auth/parse failures, Friday-deep is 0/1, and all write-mode Grok routines remain disabled.

Track 54 readiness proof-surface polish (2026-06-07): the sanitized local readiness snapshot now preserves non-secret Grok credential fields while stripping raw command output, artifact paths, and artifact hashes. Normal `/thesis` now names the exact X Pulse scout credential issue (`missing credential; source none`) in `Daily automation gate progress`, and `/thesis?audit=1` adds the safe recovery action after credential refresh. This is display-only operator clarity; it does not run Grok search, launch the scout, write Supabase rows, enable production Grok routines, or revive retired Grok endpoints.

Track 54 heartbeat safe action (2026-06-07): `npm --silent run track54:heartbeat-summary` now emits `next_safe_operator_action`, using the same shared helper as `npm run track54:automation-runs`. When Grok credential proof is held, both summaries tell the operator to refresh with `grok login` or `XAI_API_KEY`, then run `npm run track54:recover-after-grok-login`, and not to run the scout or any write command directly.

Repo type/test gate cleanup (2026-06-07): `npm run typecheck` now wraps the broad TypeScript gate, and `npm run verify` runs typecheck, lint, full bounded Vitest, and production build in order. Stale fixture/mock type repairs landed across seeding progress, Track 54 env helpers, overview supply-disposition fixtures, public-copy guardrails, forecast artifacts, Bushy/weather tests, and knowledge helpers. `npm run test` is pinned to the proven bounded two-worker Vitest runner. The repo lint backlog is now clean at 0 errors / 0 warnings. Latest full proof: `npm run verify` passed on 2026-06-07 MT with 148 Vitest files / 1126 tests passing and production build passing.

Track 54 browser proof hardening (2026-06-07): `npm run track54:browser-smoke` now checks desktop and mobile viewports for `/thesis`, `/thesis?audit=1`, and `/overview`, captures screenshots for every route/viewport pair, rejects short/blank rendered bodies, validates PNG screenshot bytes/dimensions/visual-content stats, exercises the `Toggle theme` control to prove a real UI interaction changes state and restores it, and scroll-probes every required marker to prove it is visibly reachable and not covered by another rendered element. The proof builder is import-safe and covered by `lib/__tests__/track54-browser-smoke.test.ts`, so desktop/mobile/interaction/screenshot/visible-marker output is locked by fast unit coverage as well as live browser proof. Readiness now also rejects legacy route-only smoke proof that lacks desktop/mobile coverage, screenshot files inside the browser-proof directory, matching screenshot byte counts, valid PNG signatures, matching screenshot dimensions, screenshot visual-content stats, console, forbidden-copy, marker, visible-marker, and theme-toggle evidence for every route. Fresh no-write proof generated at `2026-06-08T04:07:27.597Z` passed with all markers present, visible, and unobscured; no forbidden public copy; zero console errors; successful interaction proof across all six checks; screenshot byte counts from 48,049 to 201,232 bytes; screenshot dimensions of 1024x768 desktop / 390x844 mobile; and visual stats with at least 37 distinct sampled colors, 0.5933 non-white ratio, and 159.2 luma range. Readiness refreshed from that proof at `2026-06-08T04:27:26.127Z`: `overall_status = hold_manual_review`, `browser_smoke_clean = true`, `production_writes_enabled = false`, 3 completion blockers, daily_pulse remains 2/5 clean artifact days with 4 accepted and 3 decision-grade accepted signals, Friday-deep remains 0/1, and remaining blockers are still Grok credentials/artifact gates, not app runtime.

Track 54 loaded-route, origin, screenshot-identity, and actual-visual proof hardening (2026-06-07): readiness rebuilt from the fresh six-check browser proof at `2026-06-08T04:27:26.127Z` and now rejects any browser-smoke proof that lacks a command `--base-url`, any loaded line that is not a local app URL, any loaded URL whose origin differs from the command base URL, any loaded URL whose path plus query does not exactly match the expected route, any duplicate screenshot path claimed by multiple route/viewport checks, any screenshot filename that does not match the route/viewport contract, or any referenced PNG whose decoded pixels fail the visual-content thresholds. The live no-write rebuild had zero loaded-URL, origin, screenshot-reuse, screenshot-filename, or actual-visual failures, `browser_smoke_clean = true`, `overall_status = hold_manual_review`, and `production_writes_enabled = false`.

Track 54 readiness/browser-proof freshness display (2026-06-07): the sanitized local readiness snapshot now includes readiness-report age plus browser-smoke proof generated time, age, and six-hour freshness status while still stripping raw proof output, commands, screenshot paths, artifact paths, and artifact hashes. Normal `/thesis` and audit `/thesis?audit=1` now distinguish fresh, stale, future-dated, missing browser proof, and present-but-missing-timestamp readiness/browser proof; stale or future-dated readiness gate counts are labeled as last-known proof instead of current proof; and stale ready-for-approval mode badges stay lagged instead of green. The thread heartbeat summary now also emits `report_freshness_status` and `browser_smoke_proof_freshness_status`, and the live heartbeat prompt is manifest-audited for those fields, so automation JSON can tell stale, future-dated, missing-proof, and timestamp-missing proof apart while preserving the existing freshness booleans.

Track 54 update (2026-06-01): Codex now has two active dry-run/no-write evidence jobs: `grok-x-scout-artifact-week-review` for the five-day `daily_pulse` gate and `grok-x-scout-friday-deep-artifact-review` for the one-Friday `friday_deep` gate. Two active no-write monitors cover artifact drift through deterministic script execution: `track-54-daily-artifact-health-check` runs Mon-Thu after the daily-pulse dry run and starts with `npx tsx scripts/run-track54-artifact-health-check.ts --mode daily_pulse --retry-missing --refresh-readiness=after-retry`; `track-54-friday-artifact-health-check` runs Friday before the promotion heartbeat and starts with `npx tsx scripts/run-track54-artifact-health-check.ts --mode both --retry-missing --refresh-readiness=always`. The script retries only missing or structurally invalid dry-run/no-write artifacts and leaves an incomplete five-day promotion window to the artifact-week gate. Both evidence jobs now run `npm run track54:readiness -- --out scratch/track54-readiness/latest-readiness-report.json --browser-smoke-proof-out scratch/track54-browser-smoke/browser-smoke-proof.json` after their mode-specific artifact review, so browser proof and the persisted readiness JSON are refreshed through the same wrapper. `npm run track54:readiness` is now the self-contained top-level no-write operator check: it runs browser smoke, feeds the proof to the readiness builder, persists `scratch/track54-readiness/latest-readiness-report.json`, and checks daily-pulse, Friday-deep, promotion-brief, local Codex automation-manifest status, artifact automation coverage, Grok CLI preflight status, weekday and Friday artifact health-check status, weekday evidence/promotion heartbeat status, write-mode automation negative proof, post-approval dry-run/write-mode handoff proof, live source-freshness proof, focused acceptance tests, browser-smoke completion proof, database/RLS migration contract proof, and plan acceptance criteria. `npm run track54:readiness:build` rebuilds from the default existing proof; custom proof paths should call `npx tsx scripts/build-track54-readiness-report.ts --browser-smoke-proof <path> --out <path>` directly. Browser-smoke proof now carries `generated_at`, rejects rendered forbidden terms with whole-word matching for parked grains and trading/advice wording, and readiness rejects missing or older-than-six-hour proof instead of accepting stale screenshots. The stricter browser proof caught and removed visible `/thesis` `price advice` copy and `/overview` `sell-down proxy` copy. The focused acceptance proof now covers Track 54 readiness, Grok CLI preflight hold, artifact-week, promotion-brief, scout-contract, output-merge, write-approval, write-gate, migration contract, stale browser-proof rejection, and retired-pipeline tombstone tests. The promotion brief, readiness report, and heartbeat summary now expose selected artifact identity (`artifact_path` and `artifact_sha256`) for each reviewed day, plus machine-readable proposed Codex write-mode automation specs for `grok-x-scout-daily`, `daily-thesis-review`, and `grok-x-scout-friday-deep`; the heartbeat summary also carries `local_review_date`, `local_review_weekday`, `local_review_kind`, `review_modes_to_inspect`, `write_mode_proposal_ids`, and `post_approval_transition`, and readiness checks their exact commands, approval windows, workspace `cwds`, approval-only status, and dry-run-disable prerequisites. They remain create-after-approval specs only and do not enable routines. The manifest audit now requires exact Codex `rrule` schedules, the Bushel Board repo in `cwds` for workspace jobs, Grok dry-run prompts that include `npm run track54:readiness`, `--browser-smoke-proof-out`, `--out`, `acceptance_audit.overall_status`, `acceptance_audit.browser_smoke_proof`, `browser_smoke_clean`, `persisted_report_path`, and `completion_blockers`, both health-check prompts' deterministic artifact-health commands plus persisted-readiness handoff, and a thread-targeted no-write evidence/promotion heartbeat that starts with `npm --silent run track54:heartbeat-summary`, reports local review date/type, review modes to inspect, selected artifact hash/path, post-approval transition, and write-mode proposal IDs when present, and still requires explicit Kyle approval; it also rejects dry-run or heartbeat prompts containing explicit write-mode command fragments. Dry-run-only prompt drift, health-check retry-boundary drift, heartbeat target/prompt drift, schedule drift, wrong working directory, uncovered projected artifact date, broken Grok CLI preflight, unsafe post-approval handoff, explicit write command inside a dry-run prompt, stale browser proof, forbidden rendered page text, or active pre-approval write-mode Codex manifest is a manual hold. Current verdict is `blocked_by_artifact_gates` / `acceptance_audit.overall_status = incomplete`; production write routines remain disabled until the matching-mode gate passes and receives human approval.

Track 54 operator check (2026-06-02): daily-pulse now has 2/5 clean no-write artifact days. The 2026-06-01 selected Grok CLI daily-pulse artifact hash is `e440fc9cf89096f134f3a8d7e0b3a0f23c65e3eeb57a62fd4da05bdbf419e9e9` with 3 raw signals, 3 accepted signals, 2 decision-grade accepted signals, 0 rejected, and fresh price context; a later same-day quiet dry-run produced hash `79eba090f1a0b34ed9457eb515d0d5c0f87ccbf7c34568d5ac44d24589b55684` with 0 signals, and the reviewer keeps the better valid no-write artifact for the gate instead of blindly following the latest pointer. The 2026-06-02 scheduled daily-pulse dry-run produced artifact hash `04a6005cc972fefec32f8c12f52480037fb26f9ca00f3c327e697c769a5b6012` with 1 raw/accepted/decision-grade signal and fresh price context. Promotion, readiness, and heartbeat proof carry selected artifact hash/path, post-approval transition, and write-mode proposal IDs so trusted same-day runs and gated handoff are explicit in the operator packet and the daily thread wakeup. Grok CLI preflight is proven with `grok 0.2.14`; the scout path now uses temporary Grok config, web-search-only tool exposure, MCP/file/shell/edit deny rules, and a larger mode-aware turn budget to reduce connector/plugin noise while staying no-write. Final post-build browser smoke proof generated by readiness at `2026-06-03T00:29:53.188Z` (June 2, 6:29 PM MT) passed for `/thesis`, `/thesis?audit=1`, and `/overview` against `http://127.0.0.1:3111`, including the new `Analyzed data` marker; readiness still reports `blocked_by_artifact_gates`: daily-pulse is 2/5 clean artifact days, Friday-deep is 0/1, write-mode manifests are missing/safe, weekday and Friday artifact health-check manifests are active/proven, the Mon-Fri evidence/promotion heartbeat is active/proven, and production writes remain disabled. Codex heartbeat `track-54-promotion-review` is active Mon-Fri at 5:30 PM MT to wake this thread after the dry-run evidence jobs: Mon-Thu for daily_pulse evidence review using the persisted readiness report, and Friday for the no-write promotion review before any human approval decision. Workspace cron `track-54-daily-artifact-health-check` is active Mon-Thu at 4:45 PM MT to inspect the same-day daily-pulse dry-run artifact and retry only the no-write scout if the scheduled artifact is missing or invalid; after a retry it refreshes browser smoke and the persisted readiness report. Workspace cron `track-54-friday-artifact-health-check` is active Fridays at 5:15 PM MT to inspect both final-day `daily_pulse` and `friday_deep` artifacts and retry only no-write scout runs before promotion review. Readiness audits both health-check schedules/workspaces/retry commands/no-write boundaries and verifies the evidence/promotion heartbeat is active, scheduled, thread-targeted, no-write, and approval-gated. If a future write-mode automation is pre-staged as paused, readiness now requires the expected schedule, Bushel Board workspace, approval phrase, dry-run-disable prerequisite, and no-retired-pipeline prompt guardrails before treating it as safe.

Track 54 heartbeat status (2026-06-02): `npm run track54:heartbeat-summary` generated `2026-06-03T00:32:29.172Z` for local Tuesday `weekday_daily_pulse_review`. The persisted readiness report path matched, report age was 0.04 hours, browser-smoke proof age was 0.05 hours, `browser_smoke_proof_ok = true`, `heartbeat_blockers = []`, `status = ready_for_review`, and `safe_to_promote_without_human_approval = false`.

Track 54 operator check (2026-06-03): inspected the active Codex automation run history and local run memory. `canola-price-and-fx-freshness-import` succeeded at 3:45 PM MT: FX upserted 16 rows through `2026-06-03`, grain prices upserted 65 rows across 9 tracked contracts, and thesis cache refreshed 12/12. The 4:10 PM dry-run scout and 4:45 PM artifact-health runs failed to produce a valid 2026-06-03 `daily_pulse` artifact. The first failure was Grok CLI rejecting the disabled `mcp_servers.gemini-cli` config, then the runner had to extract scout JSON from Grok CLI's outer `text` envelope, and the remaining blocker is expired Grok CLI auth with no `XAI_API_KEY` present in `.env.local`. `scripts/run-grok-x-scout.ts` now writes valid disabled MCP stubs, parses the Grok CLI JSON envelope, kills the Windows process tree on timeout, loads `.env.local` before Grok execution, and fails fast on interactive re-auth prompts. `scripts/build-track54-readiness-report.ts` now also treats Grok runner proof as CLI/version plus credential proof: either `XAI_API_KEY` exists or the local Grok `auth.json` has an unexpired `expires_at`, with no secret output. Refreshed readiness/heartbeat proof on 2026-06-03 remains no-write and fresh: local review kind is `weekday_daily_pulse_review`, daily-pulse is 2/5 clean artifact days with 4 accepted and 3 decision-grade accepted signals, Friday-deep is 0/1, selected daily artifacts are still the 2026-06-01 hash `e440fc9cf89096f134f3a8d7e0b3a0f23c65e3eeb57a62fd4da05bdbf419e9e9` and 2026-06-02 hash `04a6005cc972fefec32f8c12f52480037fb26f9ca00f3c327e697c769a5b6012`, browser-smoke proof is fresh/clean, write-mode proposals remain `grok-x-scout-daily`, `daily-thesis-review`, and `grok-x-scout-friday-deep` only, production writes are false, and the next safe action is `grok login` or `XAI_API_KEY` followed by a no-write artifact-health retry.

Track 54 Grok auth preflight automation (2026-06-03): added `npm run track54:grok-preflight` and active Codex automation `track-54-grok-auth-preflight` at 3:55 PM MT Mon-Fri. It runs only the no-write Grok version/credential preflight, reports `grok_version_ok`, `credential_ok`, `credential_source`, `credential_issue`, `cli_auth_expired`, `ok`, non-secret evidence, and next actions, and explicitly does not run Grok search, the X scout, daily thesis review, write commands, Supabase writes, production Grok routines, or retired Grok endpoints. Readiness now manifest-audits this cron before treating the daily X scout lane as operational.

Track 54 retry preflight guard (2026-06-03): the daily-pulse and Friday-deep dry-run automation prompts now run `npm run track54:grok-preflight` before launching Grok, and both artifact-health automations report `grok_preflight_ok` plus `retry_blocked_by_grok_preflight`. The deterministic health script still reviews existing artifacts when auth is bad, but blocks no-write retries before scout launch if credentials are expired or missing. Scheduled scout/retry commands now use `--runner auto`, selecting `xai_api` when `XAI_API_KEY` is present and otherwise using the locked-down Grok CLI path, so the preflight's accepted credential sources match the actual run path. The current verified blocked retry state is clean: no scout retry ran, production writes stayed false, and the artifact gate remains held by 2/5 daily-pulse days, 0/1 Friday-deep days, and expired Grok credentials.

Track 54 late auth recovery automation (2026-06-03): added active Codex automation `track-54-late-grok-auth-recovery` at 5:05 PM MT Mon-Fri. It runs `npm run track54:grok-preflight`, then the deterministic no-write `npx tsx scripts/run-track54-artifact-health-check.ts --mode both --retry-missing --refresh-readiness=always` command. This gives the day one late recovery slot if Grok auth is refreshed after the normal daily health window; readiness manifest-audits the prompt, schedule, workspace, no-write boundary, preflight fields, retry-blocked fields, and persisted-readiness reporting. It remains a dry-run evidence guard only.

Track 54 morning auth warning branch (2026-06-04): updated active thread heartbeat `track-54-promotion-review` to run at 9:30 AM and 5:30 PM MT Mon-Fri. Before noon it runs only `npm run track54:grok-preflight`, notifies this thread if `credential_ok` is false, and stays quiet when credentials are ready; after noon it keeps the existing no-write evidence/promotion review. Readiness manifest-audits both branches so expired Grok auth is surfaced before the 4:10 PM X scout window. Added `npm run track54:recover-after-grok-login` as the no-write operator shortcut after credentials are refreshed; it runs both-mode artifact health with missing-artifact retry and readiness refresh. It does not run Grok search, the X scout, daily thesis review, write commands, Supabase writes, production Grok routines, or retired Grok endpoints unless the deterministic artifact-health script first proves credentials are ready and the same-day artifact is due/missing/invalid.

Track 54 structured auth diagnostics (2026-06-04): `npm run track54:grok-preflight` now emits machine-readable non-secret credential fields (`credential_source`, `credential_issue`, `xai_api_key_present`, `cli_auth_file_present`, `cli_auth_expires_at`, `cli_auth_expired`) while preserving the existing human-readable `output` array. Readiness now requires the auth preflight, late recovery, and thread heartbeat prompts to inspect the structured fields so the safe operator action can be tied to `expired_auth`, missing credentials, or an API-key/CLI-auth source without launching Grok.

Track 54 heartbeat auth summary (2026-06-04): `npm run track54:heartbeat-summary` now exposes the persisted readiness Grok preflight as a compact `grok_runner` object with `ok`, `credential_source`, `credential_issue`, and CLI/API-key presence fields. The thread heartbeat must inspect and report `grok_runner.credential_issue` during afternoon reviews, so the daily evidence packet names the auth blocker without opening the full readiness report.

Track 54 Hermes shadow scout bridge (2026-06-08): added local bridge script `scripts/build-hermes-x-scout-artifact.ts`, reference doc `docs/reference/hermes-grok-x-scout-bridge.md`, focused tests, active Codex automation `track-54-hermes-x-scout-prompt-bridge`, and readiness manifest auditing for that automation's schedule/workspace/prompt/no-write boundaries. The automation now runs as `Track 54 Hermes terminal X scout shadow recovery` after the normal no-write scout window: it runs `npm --silent run track54:hermes-preflight`, reviews today's daily_pulse artifact, skips Hermes when the same-day artifact is already structurally valid/no-write/date-matched/mode-matched/price-fresh, and runs the terminal Hermes shadow scout only when the same-day artifact is missing, invalid, parse-failed, identity-mismatched, or lacks fresh price proof. It does not run Hermes for low signal count alone, does not run daily thesis review, does not run `--write`, does not write Supabase rows, does not enable production Grok routines, does not register write-mode automations, and does not call retired pipeline paths; Hermes remains a shadow X evidence scout subject to the existing Track 54 artifact reviewer.

Track 54 Hermes terminal scout runner (2026-06-08): verified `hermes` is callable from the Codex terminal and xAI OAuth can run `grok-4.3` when the model is addressed as `grok-4.3` rather than catalog id `x-ai/grok-4.3`. Added `scripts/check-track54-hermes-preflight.ts`, `npm run track54:hermes-preflight`, `scripts/run-hermes-x-scout-terminal.ts`, and `npm run track54:hermes-x-scout:terminal`. The preflight checks Hermes CLI availability, xAI OAuth login, no-search model smoke response, and `x_search` toolset visibility without running X search; readiness and heartbeat summaries now carry a separate `hermes_terminal` proof so missing original Grok CLI/API credentials do not hide Hermes shadow-scout readiness. The runner uses Hermes one-shot mode with `--provider xai-oauth`, `--model grok-4.3`, and `--toolsets x_search`, saves the raw response under `scratch/hermes-x-scout/`, performs the same read-only `grain_prices.price_date` freshness check as the normal scout runner, and imports it into the same no-write Track 54 local artifact contract with a shared prompt/raw/summary artifact stem. The runner remains local evidence only: no Supabase writes, no daily thesis review, no production Grok routines, and no retired pipeline calls.

Track 54 Hermes fallback recovery (2026-06-08): `scripts/run-track54-artifact-health-check.ts` now accepts `--fallback hermes_terminal`. The daily, Friday, and late-recovery retry aliases pass that fallback explicitly. If a same-day artifact is due and structurally missing/invalid, the health command still tries the original Grok preflight first; when that fails, it runs `npm run track54:hermes-preflight` and may run `npm run track54:hermes-x-scout:terminal -- --mode <mode> --date <local-run-date>` only after Hermes preflight passes. It reports `hermes_preflight_ok`, `hermes_preflight_model_smoke_ok`, `retry_used_fallback`, and `retry_blocked_by_hermes_preflight`, and still never retries for low signal count alone or enables writes.

Track 54 auth expiry-window guard (2026-06-04): `npm run track54:grok-preflight` now treats CLI auth that expires before the next scheduled no-write scout window as a failed preflight with `credential_issue = auth_expires_before_next_scout`. This closes the false-green case where morning auth was technically unexpired but would lapse before the 4:10 PM MT daily scout could collect the artifact.

Track 54 Grok Build model selector (2026-06-04): `scripts/run-grok-x-scout.ts` now accepts a Grok CLI model override via `--grok-cli-model`, `GROK_CLI_MODEL`, or the npm-safe positional form after runner. The local Grok model cache identifies Cursor/Composer 2.5 as `grok-composer-2.5-fast`. `npm run track54:recover-after-grok-login`, the active daily/friday no-write scout and artifact-health automations, and the approval-only future Grok scout write-mode proposals now pass that model to Grok CLI retries while preserving web-search-only tool exposure, MCP/file/shell/edit deny rules, and production writes disabled until the approval gate; live artifact collection still requires refreshed Grok auth or `XAI_API_KEY`.

Track 54 data-intake visibility (2026-06-02): `/thesis` now renders `Daily Update Status` plus a `Weekly Data Intake` section near the top of the board. The daily strip shows board packet/cache freshness, grain-price context, latest X Pulse accepted/rejected proof, and the daily soft-update gate boundary. The intake section lists the weekly/daily/monthly source pulls, target tables, current board freshness/run status, and what each source signifies for bull/bear pressure, including CGC weekly flow, Prairie crop-progress packages, Export Sales, CFTC COT, WASDE, daily prices, and supervised X Pulse dry-run evidence. It now leads with a plain week-flow strip covering daily price base, early crop read, Thursday flow check, Friday thesis handoff, and monthly WASDE reset so farmers can understand the sequence before reading collector rows. Official-source status is aggregated from rendered packet freshness rows; X Pulse status comes from the latest scout run accepted/rejected counts and price-snapshot status. Intake rows now also query latest `source_runs` proof, so data lanes not present in the rendered packet can still show latest collector status, finished time, source period, and changed-row count. This is a transparency surface only; it does not widen source admission or enable Grok writes. Browser smoke now requires `Daily Update Status`, `Weekly Data Intake`, `Current board status`, and `Latest collector` on `/thesis` and `/thesis?audit=1`.

Track 54 daily-trajectory visibility (2026-06-02): `/thesis` now reads the latest bounded daily thesis-review rows from `score_trajectory` and `us_score_trajectory` where `scan_type = opus_review_daily_pulse`, normalizes CAD text evidence and US JSON evidence into one shape, and applies matching-period rows as the visible current-score overlay across Top Takeaway, quick scan, matrix, and grain cards. The `Daily Update Status` section shows `Latest Daily Trajectory Moves` with stance delta, confidence delta, current trajectory stance, signal note, reasoning, and watch-evidence provenance. The overlay updates stance/confidence only; weekly thesis text, official-source drivers, source freshness, and Friday thesis-of-record boundaries remain packet anchored. This remains read-only while production write-mode Grok routines are still blocked by artifact gates.

Track 54 daily-overlay coverage (2026-06-02): `Daily Update Status` now also shows `Daily overlay coverage`, counting the visible board rows with matching current-period daily trajectory overlays and the rows still using weekly packet scores. It names the specific daily-overlaid rows and the weekly-packet rows, closing the partial-coverage ambiguity for the Bull/Bear board: a single bounded daily row can visibly update one grain without implying that every visible grain received a same-day trajectory row.

Track 54 inline score-source labels (2026-06-02): visible Bull/Bear scores now carry `Score source: Daily overlay` or `Score source: Weekly packet` directly in the matrix, quick-scan rows, and grain cards. The coverage panel still summarizes row counts, but the score surface itself now tells farmers whether they are reading today's bounded trajectory overlay or the weekly thesis packet.

Track 54 daily-overlay recency clarity (2026-06-02): `Daily Update Status` now distinguishes current-period overlay coverage from same-day update coverage. Prior-day bounded trajectory rows can still overlay visible Bull/Bear scores, but the soft-update tile labels them as `prior-day overlay` until a current local-day row exists, score-source badges carry the overlay date, and the coverage panel counts how many rows are stamped today.

Track 54 intake role/pressure visibility (2026-06-02): `Weekly Data Intake` now labels every pull with its allowed board role and pressure lane. Official source rows are marked as `Official thesis input`; prices/FX are marked as `Price context`; X Pulse remains `Watch-only evidence`. Pressure lanes distinguish supply/weather, demand/flow, logistics, positioning, price/FX, social/evidence risk, and supply/demand baseline, so the weekly data list explains both what is pulled and how it is allowed to affect the Bull/Bear read.

Track 54 analyzed-data visibility (2026-06-02): `Weekly Data Intake` now includes `Analyzed data` for every source row and mobile card. It names the specific measurements watched before Bull/Bear interpretation, including USDA planting/condition/harvest percentages, Prairie crop condition/moisture/stress notes, CGC deliveries/stocks/receipts/exports, Export Sales commitments and admitted projection pace, Producer Cars allocation/shipments, CFTC position changes, futures/FX/CAD-normalized prices, X Pulse source-tier/claim/decision metadata, and WASDE supply-demand revisions.

Track 54 mobile intake readability (2026-06-02): `Weekly Data Intake` now renders mobile `Quick source cards` from the same proof objects as the desktop audit table. Each card carries the source timing, allowed role, pressure lane, current board/run status, target table, and `What it changes` significance so the weekly data path is readable at phone width without horizontal table scrolling.

Track 54 current-week intake tracker (2026-06-02): `Weekly Data Intake` now shows a `Current week pull tracker` before the detailed cadence cards. It compares the 12 configured intake lanes to the current America/Edmonton weekday, then shows today's scheduled pulls, current proof coverage for those lanes, the next official source pull after today, and the Friday source-of-record handoff inputs. The tracker is informational only and does not widen source admission, promote price context into thesis fact, or change the X Pulse watch-only boundary.

Track 54 weekly pull calendar (2026-06-02): `Weekly Data Intake` now groups the configured intake lanes into a `Weekly pull calendar` for Monday-Friday. Each day shows the expected official sources, daily prices, supervised X Pulse proof, pressure lanes, and proof-backed count using the same source objects as the detailed intake table; it is a readability surface only and does not add new source authority.

Track 54 board update-mode visibility (2026-06-02): the top `Current snapshot` card now renders `Board update mode` from visible score-source flags. It reports same-day Bull/Bear overlay coverage, prior daily-overlay activity, and weekly-packet rows before the lower daily-status section, preserving the Friday thesis source-of-record boundary and keeping write-mode routines disabled.

Track 54 audit production-gate visibility (2026-06-02): `/thesis?audit=1` now renders `Track 54 production gate` from the sanitized local readiness report. It shows production-write status, browser proof, completion blockers, daily-pulse 5-day progress, Friday-deep Friday progress, and next operator actions, while normal `/thesis` keeps this operator gate hidden and no write-mode Grok or daily-thesis routines are enabled.

Track 54 Bull/Bear pressure translation (2026-06-02): `Weekly Data Intake` now includes `How data becomes Bull/Bear pressure`, a compact rule strip that maps each pressure lane to bullish and bearish interpretation. It covers supply/weather, demand/flow, logistics, positioning, price/FX, social/evidence risk, and WASDE baseline lanes, while explicitly keeping X Pulse watch-only until official-source or desk review corroborates a lead.

Track 54 daily decision path visibility (2026-06-02): `Daily Update Status` now shows the allowed daily movement sequence directly on `/thesis`: Price/FX refresh, X Pulse dry-run proof, bounded daily review, and Friday thesis source-of-record handoff. This clarifies how a daily Bull/Bear score can move while preserving official-source precedence and keeping weekly thesis text anchored until Friday desk review.

Track 54 today update window visibility (2026-06-02): `Daily Update Status` now shows `Today update window`, a current local-day card that reports whether the board is still price-pending, waiting for the 4:10 PM MT X Pulse proof, inside the post-scout artifact-health window, weekend-held, or already backed by same-day X proof. The status checks current-day price freshness before using clock cutoffs, so an already-refreshed price/FX packet moves the board to the X-proof gate without implying write-mode Grok routines are enabled.

Track 54 today update checklist (2026-06-02): `Daily Update Status` now includes `Today update checklist`, a four-gate current-day proof board for `Price/FX context`, `X Pulse proof`, `Bounded daily row`, and `Daily automation gate`. It counts completed gates and explains why today's Bull/Bear status is current, pending, or proof-gated without enabling automatic write mode.

Track 54 price-context gate clarity (2026-06-02): the top `Daily Update Status` price-context tile now keys off the required `grain_prices` futures/FX source only. Optional `posted_prices` empties still appear in grain-card source freshness where they belong, but they no longer make the daily update gate read `empty + strong` when the required price packet is fresh.

Track 54 X Pulse proof wording clarity (2026-06-02): `Daily Update Status` now labels the scout tile as `Latest X Pulse proof` and states when the stored proof is from a prior local day. Current-day proof status stays in `Today update window`, preventing a green latest-proof badge from implying that today's 4:10 PM MT dry-run artifact is already complete.

Track 54 normal-view gate progress (2026-06-02): normal `/thesis` now shows sanitized `Daily automation gate progress` inside `Daily Update Status` when the local readiness report is present. It shows daily-pulse and Friday-deep clean-artifact counts, accepted/decision-grade evidence counts, next proof dates, and the write-disabled boundary without exposing local file paths, artifact hashes, or the audit-only `Track 54 production gate` panel.

Track 54 normal-view proof freshness (2026-06-02): the normal `Daily automation gate progress` card now includes sanitized readiness-proof freshness: generated time, browser-smoke status, overall gate status, and the first blocker. This keeps the daily Bull/Bear board from implying stale gate counts are current while preserving the normal/audit split.

Track 54 readiness wrapper smoke-target fix (2026-06-02): `scripts/run-track54-readiness.ts` now forwards browser-smoke-only options (`--base-url`, `--chrome-path`, `--settle-ms`, `--no-start-server`) to `scripts/run-track54-browser-smoke.ts` and strips them from readiness-builder passthrough. It also recovers the Windows/npm 11 behavior where npm strips unknown script flags into `npm_config_* = true` and bare positional values, including the inverted `--no-start-server` form. This keeps operator checks pointed at the intended fresh local server instead of silently falling back to an older process on the default port.

Track 54 artifact-health cutoff fix (2026-06-02): `run-track54-artifact-health-check.ts --date <today>` now respects the same America/Edmonton completed-run cutoff as implicit checks. Before the 4:10 PM MT daily-pulse window, an explicit same-day review date reports `artifact_due = false`, `retry_needed = false`, and `retry_blocked_until_due = true`; prior completed dates still count as due. This prevents a manual health check from front-running the scheduled daily scout.

Track 54 failed-artifact and CLI-runner hardening (2026-06-02): `scripts/run-grok-x-scout.ts` now applies a mode-aware scout timeout, writes failure summaries when Grok times out, exits, or returns unparsable output, and constrains the Grok CLI path with temporary local config, web-search-only tool exposure, MCP/file/shell/edit deny rules, and a larger mode-aware turn budget. `scripts/review-grok-x-scout-artifact-week.ts` treats summary-only failures as auditable failed attempts, not clean artifact days, while preserving dry-run/no-write proof. Latest readiness at `2026-06-03T00:29:53.188Z` remains `blocked_by_artifact_gates`: browser smoke clean with daily-status/intake/current-status/readiness-proof/Analyzed-data markers present, daily-pulse 2/5, Friday-deep 0/1, production writes disabled.

Track 54 heartbeat hardening (2026-06-01): `npm run track54:heartbeat-summary` now reports browser-smoke proof age and freshness at heartbeat time, so a delayed daily review cannot rely on a readiness report whose UI smoke proof has aged past the six-hour browser-proof rule.

Track 54 automation-date hardening (2026-06-01): `scripts/run-grok-x-scout.ts` and `scripts/review-grok-x-scout-artifact-week.ts` now default to the `America/Edmonton` automation calendar instead of UTC, and artifact reviews use the last completed local automation cutoff rather than the raw local calendar date. Late-evening Mountain-time reviews keep the current local day in the review window, while early-morning reviews keep that day's scheduled dry-run eligible until the 4:10 PM MT daily-pulse cutoff or the 4:50 PM MT Friday-deep cutoff.

Track 54 artifact-health hardening (2026-06-01): `npm run track54:artifact-health` provides the repo-side no-write health check used by the live Codex health-check automations. It reports same-day artifact validity, selected artifact hash/path, accepted and decision-grade counts, fresh-price status, retry reasons, whether the mode's artifact is actually due, and whether readiness was refreshed; when configured to refresh readiness it now calls the top-level `npm run track54:readiness` wrapper so health checks and operators use the same browser-smoke/readiness path. Focused tests prove structural/safety failures trigger retries after the scheduled artifact window, a clean artifact with low or zero signal count does not rerun purely to fill the promotion gate, and an early missing artifact defers retry until after the 4:10 PM MT daily-pulse or 4:50 PM MT Friday-deep cutoff.

Current Track 51 source-admission note (2026-06-03): Canada crop progress is still direct-mapped for seeded/condition lanes only where official crop definitions exist, but Saskatchewan development group rows are now admitted live as low-confidence proxy timing evidence in Canada thesis packets and deterministic weather scoring: Oilseeds -> Canola, Spring Cereals -> Wheat/Amber Durum/Barley/Oats. This does not admit Spring/Winter Wheat, Corn/Soybeans, or crop-specific condition/quality claims. Production proof: targeted Supabase migrations `20260603190500`, `20260603192500`, and `20260603202000` are applied/recorded; SK crop-progress import source run `d0ebf5dd-bacb-47bf-a5b0-a8b30acbd6f8` wrote 117 rows from the 2026-05-28 report; `thesis-packet-cache` refreshed 12/12 at `2026-06-03T19:51:17.680412+00:00`; cached packets show Canola Oilseeds 73% behind, Wheat/Amber Durum/Barley/Oats Spring Cereals 63% behind, and zero development rows for Corn/Soybeans.

Previous active track note (2026-05-31): Track 51 remains the source-backed board foundation. Use `docs/plans/2026-05-31-bushel-board-source-quality-handoff.md` first for source-quality context, but do not reopen the transparent rating-model plan as unfinished: Tasks 1-9 are already implemented in this checkout. Current verified state: `/thesis` and `/overview` are hard-gated to source-backed V1 public rows only (Corn, Soybeans, Wheat, Durum/Amber Durum, Canola, Barley, Oats); Spring/Winter Wheat are parked off public board surfaces until class-safe source mapping is admitted; live `thesis_packet_cache` is pruned/hard-gated to 12 packets; live `prediction_scorecard` RLS is public read-only with service-role writes retained; Canada crop progress is visible in `v_source_freshness` and mapped only to admitted V1 Canada lanes (Barley, Canola, Corn, Durum, Oats, Soybeans). Transparent Thesis Rating Model V1 now supplies deterministic scorecards, audit-only `/thesis?audit=1` display, and LLM payload guardrails as parallel evidence; it does not replace visible farmer-facing stance fields yet. `/thesis` source-health appears before Top Takeaway, Top Takeaway appears before the compact current snapshot, and both appear before KPI summary cards so the farmer read is not buried below snapshot metadata or packet counts on mobile. `/thesis` Top Takeaway now compares aggregate confidence-weighted reads against split-market country-side bull/bear pressure, labels one-country reads (`CA only` / `US only`) instead of implying full Canada-US confirmation, avoids singular/plural headline grammar traps for crop names such as Soybeans and Oats, shows the lead evidence/source behind the named bull or bear read, and names the first rows to inspect with reasons such as `country split` or `Canada-only read` instead of repeating the row action label. Prominent `/thesis` evidence, driver, and freshness labels now use farmer-readable source names while provenance rows retain raw source IDs for audit/debugging. `/thesis` quick scan and major matrix now add row action cues (`Open first`, confirmed read, one-country read, source gap) so farmers know what to inspect first without widening V1 scope, while visible copy avoids prediction-market/advice wording such as `trade signal`, `pricing plan`, and `price-advice certainty`. The quick-scan legend is derived from visible row statuses, so the public board explains active one-country rows and does not show absent mapping-gap copy. Scan and matrix stance bars are confidence-scaled so visual weight tracks source confidence instead of raw score alone. `/thesis` and the source-freshness watchdog now select the best same-period Prairie crop-progress package, so a later province-only rerun cannot downgrade a complete MB+SK+AB package; tests also lock that a newer partial period outranks an older complete package so a new incomplete week is not hidden. Partial Prairie context is shown only when the selected package is actually partial. The Watch Source Groups KPI counts stale/empty/lagged/broken freshness rows separately from province-completeness status. Verification on 2026-05-31: Canada crop-progress importer unittests passed, 44 focused thesis tests passed, `npm run lint` passed with 0 errors / 71 warnings, `npm run build` passed, source-freshness watchdog summary returned OK with `cache_items=12` and latest `prairie_week_status=complete_mb_sk_ab`, normal `/thesis` browser smoke showed clean source health, farmer-read-first hierarchy, precise Watch Source Groups KPI, friendly lead-evidence labels, grammar-safe Top Takeaway copy (`The strongest split-market bull pressure is Soybeans...`), reason-backed queue copy (`Start with: Corn (country split), Soybeans (country split), Canola (Canada-only read).`), no trading/advice wording, status-aware legend, seven allowed rows, no forbidden rows, and no console errors, `/thesis?audit=1` browser smoke confirmed scorecard audit details still render with no forbidden rows and no console errors, and live Supabase freshness returned `canada_crop_progress` as strong with 627 rows/latest AB period `2026-05-26`. Earlier browser DOM probes for `/thesis?verify=1` and `/overview?verify=1` showed no forbidden smaller crops and clean console. Full Vitest all-files run still hangs as test-runner/open-handle debt; isolate that separately before treating it as product regression.
| # | Feature | Status | Date | Key Files |
|---|---------|--------|------|-----------|
| 1 | CGC Data Pipeline (import, backfill, Vercel cron ingress) | Complete | 2026-03-04 | `supabase/functions/import-cgc-weekly/`, `app/api/cron/import-cgc/route.ts`, `scripts/backfill.ts` |
| 2 | Database Schema & Migrations | Complete | 2026-03-04 | `supabase/migrations/` |
| 3 | Grain Dashboard (overview, detail, all grains) | Complete | 2026-03-05 | `app/(dashboard)/overview/`, `app/(dashboard)/grain/[slug]/` |
| 4 | Email/Password Auth + Middleware | Complete | 2026-03-11 | `app/(auth)/`, `components/auth/`, `lib/auth/auth-scene.ts`, `lib/supabase/`, `proxy.ts` |
| 5 | Supply/Disposition Balance Sheets | Complete | 2026-03-06 | `scripts/seed-supply-disposition.ts`, `lib/queries/intelligence.ts`, `lib/queries/supply-disposition.ts` |
| 6 | AI Grain Intelligence (Grok + x_search) | **Retired 2026-05-02** | 2026-03-07 | `supabase/functions/generate-intelligence/` is tombstoned; live thesis reads use `market_analysis` |
| 7 | Week-over-Week Comparison (composite metrics) | Complete | 2026-03-08 | `components/dashboard/wow-comparison.tsx`, `lib/queries/observations.ts` |
| 8 | X/Twitter Signal Feed + Relevance Voting | **Retired 2026-05-02** | 2026-03-09 | Grok-backed `search-x-intelligence` is tombstoned; future work is direct X API v2 |
| 9 | Farm Summary (per-user AI narratives + percentiles) | Needs Claude/Codex writer | 2026-03-11 | `supabase/functions/generate-farm-summary/` is tombstoned; `components/dashboard/farm-summary-card.tsx` still displays stored summaries |
| 10 | My Farm (crop plans, delivery tracking, inventory percentages) | Complete | 2026-03-11 | `app/(dashboard)/my-farm/`, `lib/queries/crop-plans.ts`, `supabase/migrations/20260312110000_crop_inventory_marketing_tracking.sql` |
| 11 | Pipeline Velocity Chart (RPC-based) | Complete | 2026-03-10 | `components/dashboard/gamified-grain-chart.tsx`, `supabase/migrations/20260310200000_pipeline_velocity_rpc.sql` |
| 12 | Import Validation (post-import anomaly detection) | Complete | 2026-03-07 | `supabase/functions/validate-import/` |
| 13 | Prairie Landing Page | **Retired 2026-04-28** | 2026-03-11 | Deleted with the public-first auth pivot (Track 48). `app/page.tsx` is now a one-line redirect to `/overview`; `components/landing/` directory removed. See Track 48 below. |
| 14 | Farmer Engagement & Input System | Complete | 2026-03-11 | `lib/auth/role-guard.ts`, `components/dashboard/sentiment-banner.tsx`, `components/dashboard/delivery-pace-card.tsx` |
| 15 | Farmer-First Onboarding, Unlock UX, and Nav Polish | Complete | 2026-03-11 | `lib/auth/post-auth-destination.ts`, `app/(dashboard)/my-farm/`, `components/layout/`, `components/auth/`, `components/dashboard/x-signal-feed.tsx` |
| 16 | UX Layout & Hierarchy Redesign (P1) | Complete | 2026-03-11 | `components/dashboard/section-header.tsx`, `components/dashboard/compact-signal-strip.tsx`, `components/dashboard/supply-pipeline.tsx`, `app/(dashboard)/overview/page.tsx`, `app/(dashboard)/grain/[slug]/page.tsx` |
| 17 | Dual-LLM Intelligence Pipeline (Step 3.5 Flash + Grok debate) | Complete | 2026-03-12 | `supabase/functions/analyze-market-data/`, `supabase/functions/_shared/commodity-knowledge.ts`, `components/dashboard/bull-bear-cards.tsx`, `lib/queries/intelligence.ts` |
| 18 | Supplementary Data Pipeline (Grain Monitor & Producer Cars) | Complete | 2026-03-13 | `supabase/migrations/20260313120000_create_grain_monitor_and_producer_cars.sql`, `supabase/functions/analyze-market-data/`, `supabase/functions/generate-intelligence/`, `docs/reference/agent-debate-rules.md` |
| 19 | AI Thesis Debate Moderation & Agent Improvement | Complete | 2026-03-13 | `docs/lessons-learned/canola-week31-debate-moderation.md`, `docs/reference/agent-debate-rules.md` |
| 20 | CFTC COT Positioning Integration | Complete | 2026-03-13 | `supabase/migrations/20260313140000_create_cftc_cot_positions.sql`, `supabase/functions/import-cftc-cot/`, `app/api/cron/import-cftc-cot/route.ts`, `.claude/skills/cftc-cot/SKILL.md`, `docs/plans/2026-03-13-cftc-cot-integration-design.md` |
| 21 | Dashboard Overhaul â€” Farmer Decision Architecture | Complete | 2026-03-13 | `components/ui/glass-card.tsx`, `components/dashboard/cot-positioning-card.tsx`, `components/dashboard/logistics-card.tsx`, `components/dashboard/flow-donut-chart.tsx`, `components/dashboard/recommendation-card.tsx`, `components/dashboard/multi-grain-sentiment.tsx` |
| 22 | Farmer Advisor Chat & Memory | Proposed | 2026-03-13 | `docs/plans/2026-03-13-farmer-advisor-chat-design.md`, `docs/plans/2026-03-13-farmer-advisor-chat-implementation.md` |
| 23 | Dashboard Redesign V2 â€” Wave 1: Data Foundation | Complete | 2026-03-14 | `scripts/seed-supply-disposition.ts`, `app/(dashboard)/my-farm/client.tsx`, `app/(dashboard)/grain/[slug]/page.tsx`, `lib/utils/crop-year.ts`, `components/auth/auth-shell.tsx`, `components/dashboard/crop-summary-card.tsx` |
| 24 | Dashboard Redesign V2 â€” Wave 2: Grain Page Redesign | Complete | 2026-03-14 | `components/dashboard/key-metrics-cards.tsx`, `components/dashboard/net-balance-chart.tsx`, `components/dashboard/delivery-breakdown-chart.tsx`, `components/dashboard/grain-quality-donut.tsx`, `app/(dashboard)/grain/[slug]/page.tsx` |

| 25 | Dashboard Redesign V2 â€” Wave 3: Engagement & My Farm | Complete | 2026-03-14 | `components/dashboard/metric-sentiment-vote.tsx`, `components/dashboard/percentile-graph.tsx`, `components/ui/grain-icon.tsx`, `app/(dashboard)/overview/signal-strip-with-voting.tsx`, `components/dashboard/farmer-cot-card.tsx` |
| 26 | Dashboard Redesign V2 â€” Wave 4: Advanced Intelligence | Complete | 2026-03-14 | `supabase/migrations/20260314500000_*`, `components/dashboard/crush-utilization-gauge.tsx`, `components/dashboard/price-sparkline.tsx`, `lib/queries/processor-capacity.ts`, `lib/queries/grain-prices.ts` |
| 27 | Delivery Pace Chart (YoY Cumulative Gap, Dual Y-Axis) | Complete | 2026-03-15 | `components/dashboard/delivery-gap-chart.tsx`, `lib/utils/delivery-gap.ts`, `tests/lib/utils/delivery-gap.test.ts`, `app/(dashboard)/grain/[slug]/page.tsx` |
| 28 | Terminal Net Flow Visualization | Complete | 2026-03-16 | `components/dashboard/terminal-flow-chart.tsx`, `components/dashboard/logistics-banner.tsx`, `components/dashboard/logistics-stat-pill.tsx`, `lib/queries/logistics-utils.ts`, `lib/queries/logistics.ts`, `supabase/migrations/20260316120000_weekly_terminal_flow_rpc.sql` |
| 29 | Unified Grok 4.1 Fast Migration | **Retired 2026-05-02** | 2026-03-16 | Runtime Grok/xAI writers and advisor fallback tombstoned |
| 30 | Stance Spectrum Meter & Confidence Gauge | Complete | 2026-03-16 | `components/dashboard/bull-bear-cards.tsx`, `components/dashboard/recommendation-card.tsx`, `lib/utils/recommendations.ts`, `supabase/migrations/20260316130000_add_stance_score.sql` |
| 31 | Pipeline v2: Senior Analyst (single-pass) | **Retired 2026-05-02** | 2026-03-17 | `supabase/functions/analyze-grain-market/` is tombstoned |
| 32 | Live Grain Futures Prices (Phase A) | Complete | 2026-03-18 | `scripts/import-grain-prices.ts`, `lib/queries/grain-prices.ts`, `supabase/migrations/20260318120000_add_grain_prices_unit.sql` |
| 33 | Viking Knowledge System (L0/L1/L2) | Complete | 2026-03-19 | `lib/knowledge/viking-l0.ts`, `lib/knowledge/viking-l1.ts`, `lib/knowledge/viking-retrieval.ts`, `supabase/functions/_shared/viking-knowledge.ts` |
| 34 | Grok 4.20 Upgrade + Parallel Debate Architecture | **Retired 2026-05-02** | 2026-03-21 | Debate scripts deleted; use Claude/Codex review |
| 35 | Hermes Pipeline Design + US Thesis Lane | Complete | 2026-04-11 | `docs/hermes/SOUL.md`, `supabase/migrations/20260412100*`, `lib/us-market-context.ts`, `docs/plans/2026-03-28-hermes-pipeline-design.md` |
| 36 | Chat-First iOS Pivot + Bushy Persona | Complete | 2026-04-14 | `docs/plans/2026-04-13-chat-first-ios-design.md`, `BushelBoard/`, `.claude/agents/chat-architect.md`, `supabase/functions/chat-completion/` |
| 37 | Web Alpha â€” Bushy Chat | Complete | 2026-04-14 | `app/(dashboard)/chat/page.tsx`, `components/bushy/`, `supabase/functions/_shared/chat-tools.ts`, `supabase/functions/_shared/chat-context-builder.ts` |
| 38 | Operational Feedback Loop | Design Only | 2026-04-14 | Design doc committed, not yet implemented |
| 39 | Unified Pricing Board | Complete | 2026-04-14 | `supabase/migrations/20260418100100_unified_pricing_board.sql`, `supabase/functions/_shared/chat-tools.ts`, `components/auth/signup-form.tsx` |
| 40 | Parallel Pipeline Orchestrator | Design Only | 2026-04-15 | `docs/plans/2026-04-15-parallel-pipeline-orchestrator-design.md` |
| 41 | Claude Agent Desk | Complete | 2026-04-15 | `.claude/agents/{supply,demand,basis,sentiment,logistics,macro}-scout.md`, `.claude/agents/{export,domestic,risk}-analyst.md`, `docs/reference/grain-desk-swarm-prompt.md`, `docs/reference/collector-task-configs.md` |
| 42 | Hermes Chat Agent â€” Tiered Memory | 2026-04-15 | Design + skeleton: 6 tables, classification engine, supersession engine, X API v2 client, compression engine, Hermes server, Vercel proxy, 4 RPCs |
| 43 | Grain Detail Simplification | 2026-04-15 | Strip grain detail page to 3 sections: Market Thesis (two-column reasoning), Ask Bushy (embedded chat), My Farm (progress + recommendation). Fix stale week display. |
| 44 | Overview Bull/Bear Unification | 2026-04-16 | Single `UnifiedMarketStanceChart` grouped by region (CA 10 grains + US 4 markets), accordion rows expand to two-column bull/bear bullet panels with recommendation + detail link. CGC snapshot grid, Logistics Banner, and Community Pulse removed from Overview. `lib/queries/us-market-stance.ts`, `lib/queries/market-stance.ts` extended with `bullPoints`/`bearPoints`/`region`/`detailHref`. |
| 45 | Bio Trial Signup (landing-page lead capture + email notification) | **Retired 2026-04-28** | 2026-04-18 | Bundled with Track 13's retirement. All `components/landing/trial-*` files and `app/api/trial-notify/route.ts` deleted. Supabase RPCs `public.submit_bio_trial_signup` / `public.get_bio_trial_acres` left in DB (no app-side caller; can be dropped in a future cleanup). Reference doc `docs/reference/bio-trial-signup.md` carries a deprecation header. |
| 46 | Seeding Progress Map (`/seeding` Crop Pulse Seismograph) | Complete | 2026-05-08 | `app/(dashboard)/seeding/page.tsx`, `app/(dashboard)/seeding/spring-wheat/page.tsx`, `app/(dashboard)/seeding/[grain]/page.tsx`, `components/dashboard/seeding-{map,seismograph-glyph,scrubber,legend,canada-progress,table-fallback}.tsx`, `components/dashboard/spring-wheat-pulse-{map,page}.tsx`, `lib/queries/seeding-progress{,-utils}.ts`, `lib/queries/seeding-drill{,-utils}.ts`, `lib/queries/canada-seeding-progress.ts`, `lib/queries/spring-wheat-pulse.ts`, `supabase/migrations/20260428000000_us_state_centroids.sql`, `supabase/migrations/20260428000100_get_seeding_seismograph.sql`, `supabase/migrations/20260507145552_add_canada_seeding_progress.sql`, `supabase/migrations/20260508110159_create_canada_crop_progress.sql`, `supabase/migrations/20260507172242_add_spring_wheat_state_centroids.sql`, `supabase/migrations/20260507184844_add_crop_progress_previous_year_metrics.sql`, `supabase/migrations/20260508161414_add_sorghum_state_centroids.sql`, `scripts/import-usda-crop-progress.py` (per-state ingest), `scripts/import-usda-acreage.py` (planted-acre badges), `scripts/import-canada-crop-progress.py` (MB/SK provincial importer), `docs/plans/2026-04-27-seeding-progress-map-design.md`, `docs/plans/2026-04-27-seeding-progress-map-plan.md`, `docs/plans/2026-04-27-bushel-board-cohesion-audit.md`. v1: US grain belt (six commodities after Sorghum), Manitoba provincial seeding progress seeded from the 2026-05-05 crop report, Saskatchewan crop-specific progress imported from the 2026-05-08 crop report, and a premium pulse grain switcher with visible map progress bars, USDA Progress/Condition mode, source-backed last-year hover comparisons, and AB release watchlist. v2/v3: Northbound Crop Wave + Supply Stress Halos deferred. |
| 47 | Public-First Auth + Nav Reorder + Landing Retirement | Complete | 2026-04-28 | `lib/supabase/middleware.ts` (allowlist â†’ denylist; only `/my-farm` gated), `app/page.tsx` (one-line `redirect("/overview")`), `app/(dashboard)/my-farm/page.tsx` (explicit `redirect("/login")` defense-in-depth), `components/layout/desktop-nav-links.tsx` (My Farm pulled out of central pill), `components/layout/my-farm-nav-link.tsx` (new â€” anchored to right cluster), `components/layout/nav.tsx` (wires the new link), `components/layout/mobile-nav.tsx` (My Farm moved to bottom of primary nav block). **Deletions:** `components/landing/` directory, `app/api/trial-notify/route.ts`. Verified: `/`, `/overview`, `/grain/[slug]`, `/us`, `/seeding` all 200 OK without auth; `/my-farm` 307 â†’ `/login`. |
| 48 | My Farm Storage Tracker â€” "Grain in your bin" | Complete | 2026-04-28 | Headline `/my-farm` surface. Two inputs (total tonnes + remaining tonnes) per tracked grain, peer comparison "X% of farmers have more in the bin than you". Reuses existing `crop_plans.starting_grain_kt` + `volume_left_to_sell_kt` columns (no schema migration). New: `components/dashboard/grain-storage-card.tsx`, `lib/queries/grain-storage-comparison.ts`, server action `updateGrainStorage()` in `app/(dashboard)/my-farm/actions.ts`, RPC migration `supabase/migrations/20260428100000_grain_storage_comparison.sql` (â‰¥5-farmer privacy gate, `SECURITY INVOKER`, derives caller from `auth.uid()`). **Sentiment voting paused as part of this pivot** â€” `multi-grain-sentiment.tsx` and `sentiment-banner.tsx` no longer rendered on `/my-farm` but retained in repo + DB for future restoration. |
| 49 | Overview Hybrid Editorial+Terminal Redesign | Complete | 2026-04-28 | Full `/overview` redesign: Direction A wheat/canola palette + Fraunces typography + Direction B chart density + trajectory graphs + tabular-nums stance scores. New: `components/overview/spot-price-rail.tsx` (CBOT futures ticker), `components/overview/hero-thesis.tsx` (strongest-move editorial card + trajectory chart), `components/overview/trajectory-chart.tsx` (client SVG, Friday-reset weekly drift), `components/overview/grain-stance-grid.tsx` (CA + US grouped), `components/overview/grain-stance-row.tsx` (per-row stance bar + score + WoW delta), `components/overview/seeding-strip.tsx` (US choropleth from `usda_crop_progress`, Canada placeholder â†’ /seeding), `components/overview/marketplace-strip.tsx` (Kalshi mock cards + real spot price tiles), `lib/queries/overview-data.ts` (parallel fetch orchestrator + `pickStrongestMove()` + `computeWow()` helpers). Tests: `lib/__tests__/overview-utils.test.ts`, `components/__tests__/grain-stance-row.test.tsx`, `components/__tests__/spot-price-rail.test.tsx` (32 tests total). Kalshi API wiring deferred â€” cards are mock stubs with explicit TODO comment. |
| 50 | Data Layer Foundation V1 | Complete for thesis V1 | 2026-05-22 | Branch `codex/data-layer-foundation-v1`, latest baseline commit `4287293`. Live Supabase migrations are applied for `source_runs`, grain/market mappings, source freshness, Canada/US thesis packet RPCs, freshness optimization, Canola source-admission automation support, intraday Canola quote storage, FX CAD recalculation grant hardening, Canola packet supply-year selection, Oats CFTC portfolio mapping, cached thesis packets, USDA quarterly grain stocks (`public.usda_quarterly_stocks`), USDA quarterly-stocks packet/freshness integration, USDA acreage freshness/thesis context from `crop_acreage_estimates`, and WASDE month-over-month revision fields in US thesis packets. Sprint-1 docs cover source registry, canonical fact model, and Canola Market Read V1. Deterministic Canola read generator renders from `get_canada_thesis_packet('Canola', ...)`; `/source-spine` leads with the core weekly spine; `source_runs` has successful live rows for Producer Cars, CFTC COT, StatsCan crop baseline, Canola Council inventory, FX rates, grain prices, and `usda_quarterly_stocks`; `/thesis` now reads cached Canada/US packet facts, with US packets carrying `supply.quarterly_stocks`, `supply.acreage`, and WASDE previous-report revision deltas. |
| 51 | Bullish/Bearish Major-Grains Board V1 | Active next product lane; source-quality checkpoint verified | 2026-05-31 | `app/(dashboard)/thesis/page.tsx`, `lib/queries/thesis-board.ts`, `lib/queries/us-acreage.ts`, `lib/__tests__/thesis-board.test.ts`, `lib/__tests__/us-acreage.test.ts`, `supabase/migrations/20260509033204_add_thesis_packet_cache.sql`, `supabase/migrations/20260522123500_add_crop_acreage_to_thesis_freshness.sql`, `supabase/migrations/20260522151000_add_wasde_revision_context_to_us_thesis_packet.sql`, `scripts/refresh-thesis-packet-cache.ts`, `scripts/run-collector-with-thesis-cache-refresh.ts`, `components/layout/desktop-nav-links.tsx`, `components/layout/mobile-nav.tsx`, `docs/reference/collector-task-configs.md`, `docs/plans/2026-05-16-bullish-bearish-major-grains-next-session.md`. Current public V1 scope is source-backed rows only: Corn, Soybeans, Wheat, Durum/Amber Durum, Canola, Barley, and Oats; Spring Wheat and Winter Wheat are parked off public board surfaces until class-safe source mapping is admitted. The `/thesis` board is source-backed by cached Canada/US thesis packets, includes quarterly-stocks-driven US supply drivers, acreage-aware planting-progress drivers where available, live deterministic WASDE revision drivers for material ending-stocks/export/domestic-demand changes, and guarded Export Sales + WASDE projection compound drivers only when importer-admitted `export_pace_pct` is present. It shows source-health/provisional-use copy before the farmer read, surfaces partial Prairie crop-progress package status from `source_runs.metadata.prairie_week_status`, puts `Top takeaway` before the all-grains quick scan, includes a status-aware quick-scan legend, and distinguishes true source-mapping gaps from intentional Canada-only V1 lanes such as Durum/Canola US. Pulses, flax, smaller CGC labels, US rice/cotton, global lanes, and Kalshi are excluded from this product pass. 2026-05-31 source-quality checkpoint: Saskatchewan/Alberta crop-progress parser drift is regression-tested, Canada crop progress appears in `v_source_freshness` with 10/17-day thresholds, and `grain_market_mappings` admits only Barley, Canola, Corn, Durum, Oats, and Soybeans for the Canada crop-progress lane; no Spring/Winter Wheat or generic Wheat proxy mapping was added. 2026-05-24 verification: May WASDE refresh and Canada crop-progress admission are live; `get_canada_thesis_packet().supply.canada_crop_progress` is populated for directly mapped Canada grains, `get_thesis_data_freshness()` includes strong `canada_crop_progress`, and thesis cache has 21 rows after force refresh. 2026-05-25 update: USDA ESR commodity codes in `scripts/import-usda-export-sales.py` were corrected and live Corn/Soybeans/Barley/Oats rows were re-imported through 2026-05-14; cached US packets now admit guarded projection pace for Corn and Soybeans while Barley/Oats remain null-guarded. 2026-05-22 verification: live acreage freshness/cache checks, live WASDE revision cache checks for US Corn/Soybeans/Wheat, focused `us-acreage` + `thesis-board` Vitest suites, scoped ESLint, and `npm run build` passed; USDA export sales are current through `2026-05-07`. 2026-05-23 verification: guarded Export Sales projection admission/backfill is live through `2026-05-14`; `/thesis?audit=1` shows Wheat export-sales/WASDE projection pace at 102.3% (`102.315%` vs 24.494 MMT WASDE exports), shows no projection-pace claims for Corn/Soybeans/Barley/Oats while guarded fields are null, and normalizes cached latest-period freshness so WASDE/quarterly-stocks rows do not display false empty-source seed warnings. Focused `thesis-board` Vitest with `--maxWorkers=1 --no-file-parallelism`, scoped ESLint, `npm run validate-data-layer`, browser console check, and `npm run build` passed. 2026-05-25 verification: Spring/Winter Wheat mapping decision is resolved for V1 as explicit `Mapping needed` placeholders, generic Wheat is not used as a proxy, preview deployment for commit `ccbf32d` is Ready and `/thesis?audit=1` contains the mapping-copy strings; production `https://bushel-board-app.vercel.app/thesis` is still old `master` commit `4398413`. 2026-05-31 verification/fix pass: V1 public scope is now source-backed rows only (`Corn`, `Soybeans`, `Wheat`, `Durum`/`Amber Durum`, `Canola`, `Barley`, `Oats`); Spring/Winter Wheat are parked off public board surfaces until class-safe source mapping is admitted; smaller Canada crops and US rice/cotton remain excluded. Live migration `hard_gate_thesis_packet_cache_v1` hard-gates cache refresh/read paths and pruned `thesis_packet_cache` to 12 source-backed packets (7 Canada + 5 US). Live migration `enable_prediction_scorecard_rls` enables RLS with anon/authenticated read-only and service-role writes. `/overview` now uses an explicit V1 allowlist instead of raw `ALL_GRAINS`. Verification: focused thesis tests passed, lint 0 errors / 68 warnings, production build passed, source-freshness watchdog reported `cache_items=12` and `prairie_week_status=partial_prairie_week`, browser smoke showed the partial-Prairie banner with no console errors, and browser DOM probes for `/thesis?verify=1` and `/overview?verify=1` showed no forbidden smaller crops and clean console. Full Vitest suite still hangs as open-handle/test-runner debt. |
| 54 | Grok X Scout, Prices, and Daily Thesis Completion | Artifact-week automation gate active; write-mode Grok still gated | 2026-06-01 | `docs/plans/2026-06-01-grok-x-price-daily-thesis-completion-plan.md`, `scripts/import-grain-prices.ts`, `scripts/run-collector-with-thesis-cache-refresh.ts`, `scripts/check-bushel-source-freshness.ts`, `scripts/run-grok-x-scout.ts`, `scripts/review-grok-x-scout-artifact-week.ts`, `scripts/build-grok-x-scout-promotion-brief.ts`, `scripts/build-daily-thesis-review-packet.ts`, `scripts/run-daily-thesis-review.ts`, `scripts/build-friday-x-signal-bundle.ts`, `scripts/run-track54-browser-smoke.ts`, `app/(dashboard)/thesis/page.tsx`, `lib/x-api/*`, `lib/queries/x-scout-runs.ts`, `x_market_signals`, `thesis_packet_cache`, `score_trajectory`, `us_score_trajectory`, Friday CAD/US desk prompts. Goal: add a quarantined Grok/X scout lane, daily price collection, deterministic X signal validation, bounded weekday soft updates, Friday accepted-X-signal bundles, and an `/thesis` X Pulse Watch panel. Repo-side flow is implemented and live schema is applied: `x_scout_runs`, extended `x_market_signals`, `opus_review_daily_pulse`, and upsertable `signal_hash`. Grok CLI `0.2.14` headless JSON is proven; live daily pulse run `f74c7b7c-fb69-4db1-aaa6-5b36ebe2bde8` wrote two X API-verified accepted signals with fresh price status; daily review wrote CAD Wheat trajectory row `482` with bounded deltas; retry dry-run skipped the already-applied daily pulse; same-day scout reruns preserve old raw artifacts, and artifact-week review now chooses the best valid no-write same-day artifact instead of letting a later quiet run erase earlier useful evidence; thesis cache force-refresh returned 12/12 packets. Current local proof covers focused tests, lint 0 errors, production build, source-freshness watchdog, price dry-run, Grok CLI dry-run, Friday X bundle, whitespace check, and browser smoke; `npm run track54:browser-smoke` starts local Next when needed, emits timestamped browser proof, and readiness consumes it with `--browser-smoke-proof <path>` plus persists the operator report with `--out scratch/track54-readiness/latest-readiness-report.json`. Codex automation `canola-price-and-fx-freshness-import` is active for Mon-Fri 3:45 PM MT official price/cache refresh; `grok-x-scout-artifact-week-review` is active Mon-Fri at 4:10 PM MT for daily-pulse dry-run/no-write artifact review; `track-54-daily-artifact-health-check` is active Mon-Thu at 4:45 PM MT for no-write same-day artifact retry; `grok-x-scout-friday-deep-artifact-review` is active Fridays at 4:50 PM MT for the separate Friday-deep dry-run gate; `track-54-friday-artifact-health-check` is active Fridays at 5:15 PM MT for no-write final-day daily-pulse/friday-deep artifact health before promotion review. Both Grok dry-run prompts now refresh readiness through `npm run track54:readiness -- --out scratch/track54-readiness/latest-readiness-report.json --browser-smoke-proof-out scratch/track54-browser-smoke/browser-smoke-proof.json`, and report decision-grade accepted counts, artifact identity mismatch days, write-mode evidence days, missing no-write-evidence days, summary-count mismatch days, browser-smoke proof, `browser_smoke_clean`, and projected next eligible artifact dates. The artifact-week gate requires at least 5 clean artifact days and at least 1 decision-grade accepted signal (`tier1`, `tier2`, or `tier3`) before it can return `candidate_for_enablement`; it verifies `dry_run = true`, `write = false`, `scout_run_id = null`, parsed artifact `run_date`/`mode`, and parsed artifact validation counts before claiming no Supabase writes or evidence quality. Summary-count mismatches, artifact identity mismatches, write-mode evidence, missing no-write evidence, unlisted accepted handles, stale price context, and low-grade-only accepted evidence hold the gate. The promotion brief builder creates a no-write, mode-scoped human approval packet and leaves all write-mode routines disabled: `daily_pulse` reviews list only weekday daily commands, `friday_deep` reviews list only the Friday-deep scout command, and `manual_test` reviews cannot authorize automation. The write scripts now also reject `--write` unless the Track 54 approval phrase is supplied by `--approval-phrase` or `TRACK54_WRITE_APPROVAL` and the artifact-week reviewer returns `candidate_for_enablement` for the intended mode. `/thesis?audit=1` exposes accepted-signal allowed/blocked claims, corroboration status, and future rejected-signal reason summaries from `x_scout_runs.metadata.rejection_reasons`, closing the audit-mode accepted/rejected evidence gap. Accepted X writes now persist controlled `affected_decisions`, include the primary grain in `affected_grains`, dedupe affected regions, and reject signals missing affected-region evidence. X validation enforces daily one-day and Friday seven-day search windows, rejects future-dated posts, includes required corroboration metadata, and rejects numeric claims that Grok does not flag for official verification. Friday bundles, daily review trajectory evidence, and CAD/US desk prompts now preserve the corroboration/allowed/blocked/official-verification evidence seal so unverified X claims stay review leads, not thesis facts. Friday bundle current-week resolution picks the newest CGC crop year before comparing grain-week numbers, so current-year accepted signals are not hidden by prior-year week 52 rows; older accepted rows with empty `affected_decisions` receive deterministic derived tags at read time. The xAI API fallback loads `.env.local`, searches all trusted-handle batches instead of only the first 20-handle group, and merges/dedupes strict scout JSON outputs before validation. Write-mode scout runs resolve the latest CGC grain week automatically when `--grain-week` is omitted, while still allowing an explicit override. Current daily-pulse evidence is clean but incomplete with 2/5 artifact days, 4 accepted signals, 3 decision-grade accepted signals, 0 rejected, fresh price context, 0 accepted unlisted handles after adding `SaskWheat` as tier3, 0 write-mode artifact days, 0 missing no-write-evidence days, 0 summary-count mismatch days, and 0 artifact identity mismatch days; the 2026-06-02 no-write artifact hash is `04a6005cc972fefec32f8c12f52480037fb26f9ca00f3c327e697c769a5b6012`, and the 2026-06-01 quiet run with 0 signals is retained for audit but no longer downgrades the gate. Production `grok-x-scout-daily --write`, `grok-x-scout-friday-deep --write`, and `daily-thesis-review --write` remain disabled and code-gated until the matching-mode artifact week proves signal quality and gets human approval; daily-pulse evidence cannot approve Friday-deep writes. Boundaries: Grok is evidence discovery only; it cannot write `market_analysis`, `us_market_analysis`, or refresh `thesis_packet_cache`; accepted X posts remain visually subordinate to official source health and the weekly thesis. The March Grok/xAI writer chain remains retired and tombstoned. |
| 55 | World Veg-Oil Balance - Bounded Canola Demand Context | Complete; lane live, currently neutral | 2026-06-09 | Admits the world vegetable-oil complex as a bounded Canola demand-context lane, closing the top Canola admission priority ("China policy, palm oil, and global vegetable-oil trade" citing `usda_wasde_raw`). `scripts/import-usda-wasde.py` now imports world-level PSD rows for Oilseed Rapeseed `2226000`, Oil Rapeseed `4239100`, Oil Palm `4243000`, and Oil Soybean `4232000` via the PSD `/world/` endpoint (`country_code '00'`; codes and endpoint verified against the live PSD catalog 2026-06-09 — `/country/00/` returns an empty list, so the dedicated world path is required) with `desk_heartbeat=False` so world markets never tick `us_score_trajectory`; the ESMIS archive importer is documented as not applicable (US-page XLS parser; world per-oil history accrues forward from monthly live runs because the PSD API is latest-snapshot-only). Migration `20260609170000_admit_world_veg_oil_canola_demand_context` adds a Canola-only `demand.global_veg_oil` block to `get_canada_thesis_packet` (latest report month per commodity with prior-marketing-year stocks/use at the same report month, so YoY comparison works from the first snapshot) and registers four `grain_market_mappings` context rows, which scope the `usda_wasde_raw` freshness row to Canola packets while US lanes keep `usda_wasde_mapped`. New `bounded_context` impact source class (admitted + deterministic + weight-neutral) across `grain-impact-map`, `grain-data-coverage` (scored), `grain-impact-domain-weights` (boost 0 — an `official_thesis_input` classification would have silently inflated the demand weight share, caught by regression test), `grain-impact-graph` (bounded role, authority 0.7), `/thesis` audit badges, and the relationship explorer. Mapper lane `mapCanolaGlobalVegOilDemandContext` in `lib/thesis/rating-domain-mappers.ts` is quadruple-gated: Canola only, strong `usda_wasde_raw` freshness (anything weaker drops the lane instead of degrading the CGC primary), at least 2 commodities with current+prior stocks/use, average YoY shift at least +/-0.5 pp; max +/-6 tilt inside an already-active CGC demand domain (vs +/-35 CGC range), never creates a demand domain, never carries a scorecard. Canola impact factor split: `canola-global-veg-oil-balance` (bounded_context, scored) + `canola-china-policy-watch` (watch_only); `palm oil collector` removed from parked gaps. Verification 2026-06-09: scoped dry-run (104 rows, 0 heartbeat previews), live import (104 rows, `source_runs` success), migration applied after history repair (7 remote-MCP version stamps reconciled), live packet shows 4-commodity `global_veg_oil` with strong freshness, forced thesis cache refresh 12/12, `/thesis` + `/thesis?audit=1` render the Bounded context badge and new factors with no error boundary; 8 mapper-lane Vitest cases plus coverage/impact-map/weights/page suites pass; `npm run build` passes. Current May-2026 read: average YoY world veg-oil stocks/use shift is -0.13 pp — inside the +/-0.5 dead zone, so the lane is live but neutral this month. Boundary: this lane is bounded low-confidence demand context for Canola only; it must never become a primary score driver, and China policy/customs detail remains watch-only. |
| 52 | Predictive Market Harness - Canola to Multi-Grain | Phase 3A complete; background only | 2026-05-16 | Canola no-write harness is committed through Patch 2H. Local Phase 3A refactor work adds shared grain profiles and a grain-agnostic harness path while preserving Canola compatibility, but no sidecar writer, production/dashboard write path, training claim, or Hermes automation was added. Keep this as background infrastructure; current product focus is Track 51 Bullish/Bearish Major-Grains Board V1, not harness expansion. |
| 53 | Kalshi Prediction Board - Calibration V1 | Ongoing; parked until grain markets return | 2026-05-11 | Read-only calibration layer for Kalshi Corn, Soybeans, and Wheat weekly/monthly commodity series. New: `lib/kalshi/commodity-markets.ts`, `lib/queries/kalshi-commodities.ts`, `scripts/capture-kalshi-commodity-snapshot.ts`, `lib/__tests__/kalshi-commodity-markets.test.ts`; `/kalshi` renders equal-weight Kalshi Prediction Board cards. The cards compare Kalshi YES against a deterministic Bushel Board Implied Line built from existing thesis direction, confidence, and ranked evidence, with delta, bull/bear reasons, spread/liquidity warnings, and no line when the market threshold or thesis evidence is insufficient. 2026-05-11 update: public API wiring is proven, but current Kalshi grain commodity availability is `0` open markets and latest returned finalized markets only; `/overview` and dashboard nav no longer promote the lane while parked. Closed/finalized markets are labeled as wiring proof only and are not used for the implied line. No Supabase writes, no trading path, no thesis-prompt feedback, no scorecard/training writes, and no rice/cotton or smaller-grain expansion. |

### 2026-04-18 â€” Bio Trial Signup (Track 45)

**What was delivered:**
- Landing-page trial section (`<TrialDeskSection>`) with agronomist-desk aesthetic â€” hero, sticky-note benefits, clipboard signup form, brass odometer showing running enrolled-acres total.
- Client-side Supabase RPC wrappers (`components/landing/trial-client.ts`) â€” deliberately bypass Next.js server actions so submit-to-APPROVED latency stays under ~500 ms (server actions triggered RSC revalidation of the dynamic landing route).
- Fire-and-forget notification email pipeline: browser POSTs to `/api/trial-notify` (Node runtime), which Zod-validates and ships a kraft-paper-styled HTML + plain-text email via the Resend SDK. Email failures never affect the user's APPROVED state.
- Middleware exemption for `/api/trial-notify` in `lib/supabase/middleware.ts` so the unauthenticated public landing page can hit the notification endpoint without a 307 to `/login`.
- End-to-end verified 2026-04-18: odometer rolled 003000 â†’ 003025, row persisted with `status: new`, Resend delivery confirmed.

**Critical invariants** (full list in the reference doc): don't re-introduce a server action for the submit path; keep the notification POST fire-and-forget; keep `/api/trial-notify` on the middleware exemption list; Resend is in sandbox mode so `TRIAL_NOTIFY_TO` must be the Resend account owner until a domain is verified.

**Plans:** `docs/plans/2026-04-18-bio-trial-integration-design.md` (phases 1â€“3 shipped; 4â€“7 still ahead).
**Reference:** `docs/reference/bio-trial-signup.md`.
**Consolidated status + Mermaid flow diagram + next steps:** `docs/handovers/2026-04-18-bio-trial-feature-status.md` (2026-04-18 evening).

**2026-04-18 evening update:** Work on the trial was moved to its own long-lived branch `feature/bio-trial-integration` (seasonal feature â€” merge into master when a trial season opens). A standalone SixRing vendor console was shipped on the `bio_trial/` static site this session (`vendor.html` + `vendor.js`) â€” it covers Phase 4's functional scope but lives outside the Bushel Board app. Phase 4 proper (porting that console to `/admin`) is still ahead, along with Phase 5 (chat gating), Phase 6 (delivery â†’ magic-link invite), and the new Phase 7 (Trial tab with chat-extracted trial events). A RPC-naming reconcile is needed â€” two sets of vendor RPCs exist in the DB right now; only one should survive.

### 2026-04-16 â€” Overview Bull/Bear Unification (Track 44)

**What was delivered:**
- New `components/dashboard/unified-market-stance-chart.tsx` â€” single chart grouped by region (ðŸ‡¨ðŸ‡¦ CA + ðŸ‡ºðŸ‡¸ US) with expand-on-click accordion rows revealing a two-column bull/bear bullet panel plus recommendation + detail link.
- New `lib/queries/us-market-stance.ts` with `getUsMarketStancesForOverview(marketYear)` and pure `normalizeUsKeySignals(unknown)` helper that splits `us_market_analysis.key_signals` into `bullPoints`/`bearPoints`. Markets with no analysis yet are omitted (flatMap pattern) rather than rendered as stubs.
- Extended `lib/queries/market-stance.ts` CA query: SELECT adds `bull_reasoning`, `bear_reasoning` JSONB, output shape now carries `region: "CA"`, `bullPoints`, `bearPoints`, `detailHref`. Defensive `coerceBullets(unknown)` parser.
- Extended shared types in `components/dashboard/market-stance-chart.tsx`: new `BulletPoint` interface, `region`, `bullPoints`, `bearPoints`, `recommendation?`, `detailHref` on `GrainStanceData`.
- `app/(dashboard)/overview/page.tsx` shrunk from 167 â†’ 53 lines. The CGC market snapshot grid, Logistics Banner, and Community Pulse (sentiment banner + signal strip) are removed from this page.
- Unit test `tests/lib/us-market-stance-normalize.test.ts` â€” 3 cases (split bull/bear, null/empty, malformed entries).

**Orphans noted:** `MarketSnapshotGrid`, `LogisticsBanner`, `SignalStripWithVoting`, `MarketStanceChart` (the React component; its type exports are still live), and queries `getMarketOverviewSnapshot`, `getLogisticsSnapshotRaw`, `getAggregateTerminalFlow`, `getLatestXSignals` â€” documented in `docs/lessons-learned/issues.md` for a separate cleanup PR after one-week soak. `SentimentBanner` is still imported by My Farm so it stays.

**Data notes:**
- Only 4 of 5 US markets currently have analysis (Barley missing at time of ship). `flatMap` approach means the US group shows just the rows that have data.
- US `priorScore` is `null` for now â€” the `us_market_analysis` table doesn't store a weekly stance anchor; trajectory lives in `us_score_trajectory` and is out of scope. TODO left in the query.
- US prices query explicitly filters `.in("grain", futuresGrains)` and dedupes latest-per-grain rather than relying on `.limit(N)`, so a busy CA price table can never starve the US group.

**Defensive parsing:** Both CA and US normalizers take `unknown` JSONB and do structural narrowing with `typeof === "string"` guards, matching the project convention for untrusted DB shapes.

**Plans:** `docs/plans/2026-04-16-overview-bullbear-unification-design.md`, `docs/plans/2026-04-16-overview-bullbear-unification-implementation.md`.

### 2026-04-15 â€” Claude Agent Desk (Track 41)

**What was delivered:**
- 6 scout agent definitions: supply-scout, demand-scout, basis-scout, sentiment-scout, logistics-scout, macro-scout (5 Haiku + 1 Sonnet)
- 3 specialist agent definitions: export-analyst, domestic-analyst, risk-analyst (all Sonnet)
- Friday swarm orchestration prompt (`grain-desk-weekly` scheduled task, Fri 6:47 PM ET): 7-phase desk chief that dispatches scouts in parallel, compiles briefs, dispatches specialists, resolves divergence using all 15 debate rules + Viking L0/L1/L2, writes to `market_analysis`
- 6 daily data collector scheduled tasks: crop-progress (Mon), grain-monitor (Wed), export-sales (Thu AM), cgc (Thu PM), cftc-cot (Fri PM), wasde (Fri monthly)
- Claude-native macro search guidance; the old `scripts/xai-search.ts` helper is retired
- Reference docs: `docs/reference/grain-desk-swarm-prompt.md`, `docs/reference/collector-task-configs.md`
- Integration verified: build passes, 214 tests pass, all RPCs return data for Wheat Week 35
- Grok pipeline (`analyze-grain-market`) retired; not a fallback

### 2026-04-14 â€” Unified Pricing Board (Track 39)

**What was delivered:**
- Unified `posted_prices` table replacing separate `elevator_prices` + `provider_listings` (both had 0 rows â€” clean swap)
- `operator_products` table for operator product catalogs, seeded at signup
- `price_query_log` table for demand analytics â€” logs every farmer price query for operator feedback
- `get_area_prices` RPC (farmer price lookup), `get_operator_analytics` RPC (demand trends), `get_operator_reach` RPC (farmer count in FSA)
- 5 unified chat tools: `post_daily_prices`, `get_area_prices`, `manage_products`, `get_demand_analytics`, `update_facility_status`
- Signup form supports all operator types (elevator/crusher/mill/terminal/seed/fertilizer/chemical) with product line capture
- `facility_status` on profiles for operational notes ("Taking canola until Wed")
- `user_role` on `feedback_log` for filtering farmer vs operator feedback
- 24h price expiry drives daily posting rhythm
- Design doc: `docs/plans/2026-04-14-unified-pricing-board-design.md`

### 2026-04-14 â€” Web Alpha: Bushy Chat (Track 37)

**What was delivered:**
- Full Bushy chat at `/chat` route with auth guard
- SSE streaming from Supabase `chat-completion` Edge Function
- 10 chat tools: save_local_intel, update_farmer_memory, get_area_stance, search_market, create_crop_plan, post_daily_prices, get_area_prices, manage_products, get_demand_analytics, save_feedback
- Context builder with parallel data loading (farmer profile, crop plans, national stances, area intel, posted prices)
- Trust footer with data freshness (CGC, futures, local reports, posted prices)
- Quick chips for conversation starters
- Verification prompts for farmer-reported data (basis, prices, yields)
- Source tags: [posted pricing], [local reports], [sponsored], [national market], [your history]

### 2026-04-14 â€” Chat-First iOS Pivot + Bushy Persona (Track 36)

**What was delivered:**
- Complete iOS design doc: Swift/SwiftUI chat app with Apple Intelligence, Siri intents, widgets
- Xcode project skeleton with chat UI, auth, SSE client
- Bushy persona: grain analyst buddy, prairie farmer vocabulary, conversational tone
- chat-architect agent definition for prompt engineering and tool design
- LLM adapter layer for model-agnostic architecture; Grok primary path retired
- Rebrand: iOS app is "Bushels", agent is "Bushy", gamified data exchange

### 2026-03-17 â€” Pipeline v2: Senior Analyst Single-Pass Architecture (Track 31)

**What was delivered:**
- New `analyze-grain-market` Edge Function replacing the dual-LLM chain (`analyze-market-data` + `generate-intelligence`) with a single-pass Senior Analyst. Retired 2026-05-02.
- **Dynamic Shipping Calendar** (`lib/shipping-calendar.ts`) â€” temporal context module computing data lag, seasonal context, and week-aware framing for LLM injection (6 seasonal buckets, RangeError validation)
- **Pre-computed Analyst Ratios** (`lib/data-brief.ts`) â€” server-side arithmetic (export pace, stocks-to-use, crush utilization, delivery/export vs 5yr avg, managed money positioning) so LLM interprets rather than calculates
- **Analyst Prompt Builder** (`lib/analyst-prompt.ts`) â€” research-tier system (major 4+4 queries, mid 2+2, minor 1+1) with commodity knowledge injection, data hygiene rules, and structured output schema
- Historical self-batching with BATCH_SIZE=1. The chain to `generate-farm-summary` is retired.
- 27 new unit tests across 3 modules (shipping-calendar: 10, data-brief: 8, analyst-prompt: 9)
- Deno-compatible copies in `supabase/functions/_shared/` for Edge Function runtime

**Benchmark results (litmus tests):**
- Barley: v1 stance=-45 â†’ v2 stance=+35 (PASS â€” correctly identified +78% export pace)
- Flaxseed: v1 stance=+25 â†’ v2 stance=+20 (Partial â€” web search found offsetting factors: firm $16-17/bu cash bids, low commercial stocks tempering the bearish 17% export pace signal)
- Canola: v1 stance=-45 â†’ v2 stance=+45 (Bonus â€” web search enriched thesis with real-time context)

**Historical architecture change:** Single xAI API call with tool use replaced 2 sequential LLM calls + separate X signal search. Retired 2026-05-02.

**Design doc:** `docs/plans/2026-03-17-pipeline-v2-senior-analyst-design.md`
**Implementation plan:** `docs/plans/2026-03-17-pipeline-v2-senior-analyst-implementation.md` (7 tasks)

**New files:** `supabase/functions/analyze-grain-market/index.ts`, `lib/shipping-calendar.ts`, `lib/data-brief.ts`, `lib/analyst-prompt.ts`, `lib/commodity-knowledge-text.ts`, `lib/__tests__/shipping-calendar.test.ts`, `lib/__tests__/data-brief.test.ts`, `lib/__tests__/analyst-prompt.test.ts`, `supabase/functions/_shared/shipping-calendar.ts`, `supabase/functions/_shared/data-brief.ts`, `supabase/functions/_shared/analyst-prompt.ts`
**Modified files:** `supabase/config.toml`, `supabase/functions/_shared/market-intelligence-config.ts`

### 2026-03-16 â€” Stance Spectrum Meter & Confidence Gauge (Track 30)

**What was delivered:**
- Stance spectrum meter in `BullBearCards` â€” horizontal gradient bar (bullish green â†’ neutral gray â†’ bearish amber) with positioned marker driven by `stance_score` (-100 to +100)
- Semicircle confidence gauge in `RecommendationCard` â€” replaces text badge with SVG arc showing numeric confidence (0-100%)
- `stance_score smallint` column added to `market_analysis` table with CHECK constraint
- `computeConfidenceScore()` in `lib/utils/recommendations.ts` â€” blends stance magnitude (60%) with pace alignment (40%)
- Edge Function schema updated to request `stance_score` from Grok 4.1 Fast structured output
- Hero `MarketStanceBadge` now derives stance from `stance_score` instead of keyword matching

### 2026-03-16 â€” Unified Grok 4.1 Fast Migration (Track 29)

**What was delivered:**
- Historical migration from mixed OpenRouter/xAI to unified Grok. Retired 2026-05-02.
- `analyze-market-data` is now a tombstone and must not write analysis.
- Advisor chat Grok fallback is removed; it now requires the Claude/Hermes path.
- X market signals added to advisor context for richer market awareness
- Full pipeline re-run completed â€” all 16 grains regenerated with Grok 4.1 Fast

### 2026-03-16 â€” Terminal Net Flow Visualization (Track 28)

**What was delivered:**
- `TerminalFlowChart` component on grain detail page â€” diverging bar + line chart showing weekly terminal receipts vs exports with net flow
- `LogisticsBanner` component on overview page â€” narrative headline with sparkline summarizing terminal flow across grains
- `LogisticsStatPill` component â€” compact stat display used within the logistics banner
- Two new RPC functions: `get_weekly_terminal_flow` (per-grain weekly receipts/exports/net) and `get_aggregate_terminal_flow` (cross-grain summary)
- Client-safe utility module `lib/queries/logistics-utils.ts` for headline generation logic
- Server query module `lib/queries/logistics.ts` for Supabase RPC calls
- 5 unit tests for the logistics headline generator
- Wired into `app/(dashboard)/grain/[slug]/page.tsx` (TerminalFlowChart) and `app/(dashboard)/overview/page.tsx` (LogisticsBanner)

**Design doc:** `docs/plans/2026-03-16-terminal-net-flow-design.md`
**Implementation plan:** `docs/plans/2026-03-16-terminal-net-flow-implementation.md` (9 tasks, all complete)

**New files:** `components/dashboard/terminal-flow-chart.tsx`, `components/dashboard/logistics-banner.tsx`, `components/dashboard/logistics-stat-pill.tsx`, `lib/queries/logistics-utils.ts`, `lib/queries/logistics.ts`, `supabase/migrations/20260316120000_weekly_terminal_flow_rpc.sql`
**Modified files:** `app/(dashboard)/grain/[slug]/page.tsx`, `app/(dashboard)/overview/page.tsx`

### 2026-03-15 â€” Delivery Pace Chart: YoY Cumulative Gap with Dual Y-Axis (Track 27)

**What was delivered:**
- `DeliveryGapChart` component with dual Y-axes: left axis for cumulative deliveries (Kt), right axis for YoY gap (Kt) with green ticks
- 3 datasets on 2 axes: current year solid line + prior year dashed line (left), gap line + shaded fill area (right)
- Pure utility function `computeDeliveryGap()` with 5 passing tests
- Canola-gated Delivery Pace section on grain detail page between Key Metrics and Net Balance
- SectionHeader with dynamic pills: YoY % (red/green) and gap Kt ("X kt withheld"/"X kt ahead")
- Prototype fidelity lesson documented â€” design doc initially simplified the user's Chart.js prototype, dropping the right Y-axis

**Process improvements from this track:**
- Updated gemini-collab skill with Prototype Fidelity Check (Pattern 4), Design Doc Deviation Check (Pattern 5), and Prototype Fidelity Review workflow (Workflow 6)
- Documented full retrospective in `docs/lessons-learned/issues.md`
- Rule: when user provides source code, default to faithful reproduction first, improvements second

**New files:** `components/dashboard/delivery-gap-chart.tsx`, `lib/utils/delivery-gap.ts`, `tests/lib/utils/delivery-gap.test.ts`
**Modified files:** `app/(dashboard)/grain/[slug]/page.tsx`, `components/dashboard/CLAUDE.md`

### 2026-03-14 â€” Dashboard Redesign V2: Wave 4 Advanced Intelligence (Track 26)

**What was delivered:**
- Processor self-sufficiency RPC (`get_processor_self_sufficiency`) â€” computes producer vs non-producer delivery ratio from Process worksheet
- Self-sufficiency signal injected into `analyze-market-data` Edge Function for AI thesis generation
- Processor capacity reference table with 12 grains seeded from AAFC/industry data
- YoY toggle on Pipeline Velocity chart â€” "Last Year" and "5yr Avg" overlay lines with toggle pills
- Historical pipeline average RPC (`get_pipeline_velocity_avg`) â€” N-year average cumulative metrics per grain week
- Crush utilization gauge â€” semicircular SVG arc showing annualized processing vs capacity with bullish/moderate/low signals
- Grain prices table (`grain_prices`) with sample futures data for Canola, Wheat, Barley, Oats
- Price sparkline in grain detail hero â€” compact SVG trend line with latest settlement price and daily change
- New migrations: `20260314500000` (self-sufficiency RPC), `20260314510000` (processor capacity table), `20260314520000` (historical pipeline avg RPC), `20260314530000` (grain prices table)

### 2026-03-14 â€” Dashboard Redesign V2: Wave 3 Engagement & My Farm (Track 25)

**What was delivered:**
- Per-card metric sentiment voting (bullish/bearish) on Key Metrics cards with optimistic UI
- X signal voting on overview page CompactSignalStrip (thumbs up/down with vote state management)
- Farmer-friendly COT positioning card replacing trader-focused chart (mood gauge, plain-English insights)
- Bull/Bear confidence bar made prominent, model attribution removed
- Prairie Chatter removed from grain detail page (overview only)
- Key Metrics cards moved above Net Balance chart (both full-width)
- Custom grain SVG icons component
- Percentile distribution bell curve graph for delivery pace comparison
- Delivery logging default unit changed to kg, destination helper text added
- Edge Function prompt updated for shorter bullets, `confidence_score` (0-100), and `final_assessment`
- New migration: `metric_sentiment_votes` table + `confidence_score`/`final_assessment` columns on `market_analysis`

**New components:** `farmer-cot-card.tsx`, `metric-sentiment-vote.tsx`, `percentile-graph.tsx`, `grain-icon.tsx`, `key-metrics-with-voting.tsx`, `signal-strip-with-voting.tsx`
**New queries:** `lib/queries/metric-sentiment.ts`
**New actions:** `app/(dashboard)/overview/actions.ts`, `app/(dashboard)/grain/[slug]/metric-actions.ts`

### 2026-03-14 â€” Dashboard Redesign V2: Wave 2 Grain Page Redesign (Track 24)

**What was delivered:**
- Grain detail page completely restructured from reporting dashboard to signal-generating decision tool
- New components: Key Metrics Cards (4 vertical cards with WoW + insights), Net Balance Chart (surplus/deficit bars + cumulative line), Delivery Breakdown Chart (stacked area: elevators/processors/cars), Grain Quality Donut (Terminal Receipts by grade)
- Enhanced components: StorageBreakdown redesigned with CSS horizontal bars, BullBearCards with confidence gauge + final assessment, LogisticsCard with grain week labels on every KPI
- Bull & Bear Cases promoted from collapsed `<details>` to visible section
- Removed redundant sections: Flow Donut, Supply Pipeline, IntelligenceKpis, expandable Market Signals
- New queries: `getGradeDistribution()` (Terminal Receipts by grade), `getDeliveryChannelBreakdown()` (3 delivery channels)

**Deleted components:** `flow-donut-chart.tsx`, `supply-pipeline.tsx`, `intelligence-kpis.tsx`, `x-signal-feed.tsx`

### 2026-03-14 â€” Dashboard Redesign V2: Wave 1 Data Foundation (Track 23)

**What was delivered:**
- AAFC supply baseline updated to Feb 2026 figures (16 grains, `is_approximate` flag for 5 estimated grains)
- "% Left in Bin vs Market" calculation corrected: Total Opening Supply - CYTD Producer Deliveries (live CGC data)
- Data freshness badge on grain detail page hero section
- Fraunces font rendering fix (variable font axes), estimated yield alignment, hover arrows on crop summary cards, flow donut overflow fix

### 2026-03-13 â€” Dashboard Overhaul: Farmer Decision Architecture (Track 21)

**What was delivered:**
- Grain detail page restructured with 2-column layout, hero BULLISH/BEARISH badge, bullet-point thesis format
- New components: COT Positioning card, Logistics Snapshot card, "Where Grain Went" donut chart, Recommendation cards (HAUL/HOLD/PRICE/WATCH)
- My Farm page restructured with multi-grain sentiment voting and recommendation badges
- Supply Pipeline redesigned with "Still in Bins" hero metric and corrected labels (Processing, Carry Forward, Shrink & Waste)
- Glassmorphism design system: GlassCard, GlassTooltip, MarketStanceBadge, ActionBadge components with 3D elevation shadows and button underglow
- Overview page upgraded with glass treatment and market stance badges
- AI prompts updated for bullet-point format and `market_stance` field in `generate-intelligence` and `generate-farm-summary`

**New query modules:** `lib/queries/cot.ts`, `lib/queries/logistics.ts`, `lib/queries/flow-breakdown.ts`

## Performance Fixes

### 2026-03-12 â€” v_grain_overview 945x Query Speedup

- The Overview page silently timed out because `v_grain_overview`'s `latest_week` CTE did a `GROUP BY` + `MAX()` full-table scan on 1M+ rows in `cgc_observations` (5.2s, exceeding PostgREST statement timeout).
- Added composite index `idx_cgc_obs_crop_year_grain_week (crop_year DESC, grain_week DESC)` and rewrote the CTE to `ORDER BY ... LIMIT 1`, enabling an Index Only Scan with 0 heap fetches.
- Query time: 5,200ms -> 5.5ms.

**Migration:** `supabase/migrations/20260312180000_optimize_v_grain_overview.sql`

## Recent UI Polish

### 2026-03-12 â€” Overview Community Pulse Rail Refresh

- Reworked the overview X/community preview from a plain horizontal strip into a designed signal rail with larger cards, stronger metadata hierarchy, edge fades, left/right controls, and a custom draggable scrubber.
- Replaced reliance on an undefined `scrollbar-hide` utility with an explicit cross-browser scrollbar-hiding utility in `app/globals.css`, then gave the overview rail its own visible scroll affordance.
- Verified the redesigned rail visually in-browser after implementation.

**Files modified:** `components/dashboard/compact-signal-strip.tsx`, `app/globals.css`

### 2026-03-12 â€” Chart Upgrades: Dual-Axis Deliveries (#1) & Storage WoW (#8)

- **#1 â€” Dual-axis delivery chart:** Added separate Y-axes for pipeline metrics (kt, left) and farmer deliveries (tonnes, right) so farmer-scale data is no longer invisible against pipeline volumes. Applied to `gamified-grain-chart.tsx` and `pace-chart.tsx`.
- **#8 â€” Storage chart total + WoW:** Added a total summary line with WoW badge and per-bar delta badges using prairie green/amber color coding.

**Files modified:** `components/dashboard/gamified-grain-chart.tsx`, `components/dashboard/pace-chart.tsx`, storage chart component

### 2026-03-12 â€” Data Enrichment: Backlog Items #5, #6, #9

- **#5 â€” % left in bin vs market:** Dual progress bars on My Farm crop cards comparing farmer remaining vs AAFC carry-out, using existing supply disposition query.
- **#6 â€” Net deliveries vs disappearance KPI:** New NetBalanceKpi component on grain detail showing producer deliveries vs domestic disappearance with bullish/bearish signal, reusing existing WoW data.
- **#9 â€” Customize X feed grain selection:** Grain filter pill toggles on CompactSignalStrip on Overview, pre-selects unlocked grains, client-side filtering.

### 2026-03-12 â€” X Thread Backlog Items #10, #11, #12, #13 (Analytics Foundation)

- **#11 â€” Sentiment shipping week fix:** Sentiment poll now uses `getCurrentGrainWeek()` (shipping week N+1) instead of `latestGrainWeek` (CGC data week N). Added clarifying subtitle.
- **#13 â€” AI temporal awareness:** All Edge Function intelligence prompts now include `TEMPORAL_AWARENESS` preamble distinguishing CGC data week N from farmer shipping week N+1. Prompt versions bumped to v3.
- **#10 â€” Save Community Pulse history:** New `sentiment_history` table + `snapshot_weekly_sentiment()` RPC. Archives weekly per-grain sentiment aggregates. RLS + SECURITY DEFINER hardened.
- **#12 â€” Daily sentiment rollups:** New `sentiment_daily_rollup` table + `snapshot_daily_sentiment()` RPC. Tracks intra-week sentiment trajectory with delta tracking.

**All 13 X Thread backlog items now complete.**

**Files created:** `supabase/migrations/20260312190000_sentiment_history.sql`, `20260312190100_sentiment_daily_rollup.sql`, `20260312190200_harden_sentiment_rpcs.sql`, `lib/queries/sentiment-history.ts`, `lib/queries/sentiment-daily.ts`
**Files modified:** `app/(dashboard)/grain/[slug]/page.tsx`, `components/dashboard/sentiment-poll.tsx`, `supabase/functions/_shared/market-intelligence-config.ts`, `supabase/functions/analyze-market-data/index.ts`, `supabase/functions/generate-intelligence/prompt-template.ts`, `supabase/functions/generate-farm-summary/index.ts`

### 2026-03-12 â€” X Thread Backlog Items #4 & #7

- **#4 â€” Overview section reorder:** Moved Market Intelligence above the Community Pulse X feed on the Overview page. Section order is now Snapshot â†’ Intelligence â†’ Community Pulse.
- **#7 â€” Province map labels:** Province map labels now show both metric tons and percentage of total (e.g., "AB\n4,200.0 kt (38.2%)").

**Files modified:** `app/(dashboard)/overview/page.tsx`, `components/dashboard/province-map.tsx`

### 2026-03-12 â€” Daylight Auth Contrast & Spacing Fix

- Fixed the top-third readability issue on the daylight auth shell by switching the hero copy from low-contrast white text to dark wheat text on the daytime variant.
- Strengthened the daylight badge, logo chip, and proof cards, added a subtle glass panel behind the hero copy, and increased top spacing so the badge no longer crowds the top-left brand chip on narrower viewports.
- Verified the updated `/signup` page visually in-browser after implementation.

**Files modified:** `components/auth/auth-shell.tsx`

### 2026-03-13 â€” Supplementary Data Pipeline: Grain Monitor & Producer Cars (Track 18)

**Status:** Complete for the production rollout â€” data layer, AI integration, Edge Function deployments, and debate moderation all done. UI display, automated scraping, and scripted repo seeding remain future work.

**What was implemented:**
- **New tables:**
  - `grain_monitor_snapshots` â€” Government Grain Monitor PDFs (port throughput, grain-in-storage, carryover trends). Week 30 sample data was inserted in production (lagged 1 week for Week 31 analysis).
  - `producer_car_allocations` â€” CGC Producer Car reports (forward-looking rail allocations). Week 33 sample data was inserted in production (2-week forward for Week 31 analysis).

- **New RPC:** `get_logistics_snapshot(crop_year, grain_week)` returns both tables as structured JSON.

- **AI integration:** Embedded logistics context into commodity knowledge (2 new sections: "Marketing Strategy & Contract Guidance" + "Logistics & Transport Awareness", ~1.5K tokens). Injected into Step 3.5 Flash + Grok prompts via updated `market-intelligence-config.ts` version bumps (v4 analyze/generate, v3 knowledge).

- **Data insertion:** Week 30 Grain Monitor + Week 33 Producer Car allocations for 2025-2026 were manually loaded in the live project. The repo currently ships schema plus source files, not an automated sample-data seed for those rows.

**Known issues (resolved):**
- ~~Grain name mismatch between `producer_car_allocations` ("Durum") and `grains` table ("Amber Durum").~~ Fixed via SQL UPDATE: "Durum" â†’ "Amber Durum", "Chickpeas" â†’ "Chick Peas". Buckwheat remains unmatched (minor grain, not in tracked 16).
- Producer car data is cumulative forward-looking, not weekly. RPC returns latest available week â‰¤ grain_week + 3 to prevent allocations from aging out mid-analysis.

**Edge Function deployments (2026-03-13):**
- `analyze-market-data` - RETIRED/TOMBSTONED 2026-05-02
- `generate-intelligence` - RETIRED/TOMBSTONED 2026-05-02
- `generate-farm-summary` - RETIRED/TOMBSTONED 2026-05-02

**What remains (future work):**
- Automated PDF scraping from Government Grain Monitor and CGC Producer Car reports
- Historical backfill of pre-2026 grain monitor and producer car data
- UI display: logistics tiles, supply-chain context cards, port/rail status summaries on Overview and Grain Detail pages
- Automated scheduler integration (daily/weekly pulls depending on source update cadence)

**Files modified:**
- `supabase/migrations/20260313120000_create_grain_monitor_and_producer_cars.sql` (new)
- `supabase/functions/_shared/commodity-knowledge.ts`
- `supabase/functions/_shared/market-intelligence-config.ts`
- `supabase/functions/analyze-market-data/index.ts`
- `supabase/functions/generate-intelligence/index.ts`
- `supabase/functions/generate-intelligence/prompt-template.ts`

### 2026-03-13 â€” AI Thesis Debate Moderation & Agent Improvement Reference (Track 19)

**What:** Claude moderated the Canola Week 31 disagreement between Step 3.5 Flash (bearish) and Grok (bullish), identifying 3 specific logical errors in Step 3.5's analysis. Created reusable reference docs for continuous agent improvement.

**Key findings:**
- Step 3.5 conflated YTD exports (-28% YoY) with current-week flow â€” stocks drew -175.6 Kt while 455.6 Kt of deliveries arrived, meaning 631 Kt absorbed in one week
- The export lag reflects Vancouver port congestion (107% capacity, 26 vessels vs avg 20), not demand weakness
- Grok's bullish thesis was directionally correct but needed sharper specifics (timeline, triggers, risk)

**New reference docs:**
- `docs/lessons-learned/canola-week31-debate-moderation.md` â€” full evidence-based moderation with corrected thesis
- `docs/reference/agent-debate-rules.md` â€” 8 reusable rules (flow coherence, thesis quality) + grain-specific rules + validation checklist

## Intelligence Pipeline

### Current
```text
Codex CGC importer -> cgc_observations -> Claude/Codex desk workflow -> market_analysis / farm_summaries
```

- `npm run import-cgc` is the active CGC import path.
- `market_analysis` is the live thesis table.
- Supervised Grok/X scouting is a quarantined evidence-discovery lane under Track 54. It may feed validated `x_market_signals`, but it must not publish thesis rows or revive the old Grok writer chain.
- Direct X API v2 remains a future deterministic ingestion option; the old Grok `search-x-intelligence` workflow remains retired.

### Retired Grok Chains
```text
search-x-intelligence -> analyze-market-data/analyze-grain-market -> generate-intelligence -> generate-farm-summary
```

- Runtime entrypoints return tombstones.
- Do not use `XAI_API_KEY` or Grok CLI for analysis recovery or thesis publishing.
- Historical March Grok plans remain reference-only. New X sentiment work must use the Track 54 scout/validation boundary.

## Database Tables

| Table | Rows (approx) | Purpose |
|-------|---------------|---------|
| `cgc_observations` | ~1.1M | Weekly grain statistics in long format (6 crop years: 2020-2026) |
| `grains` | 16 | Canadian grain types with slugs |
| `supply_disposition` | ~200 | AAFC balance sheet data per grain/year/source |
| `crop_plans` | varies | User crop plans with starting grain, live remaining inventory, and contract splits |
| `crop_plan_deliveries` | varies | Append-only farmer delivery ledger with idempotency keys and sale classification |
| `profiles` | varies | User profiles with farm metadata and role |
| `market_analysis` | ~16/week | Published Claude/Codex desk thesis, bull/bear, historical context |
| `grain_intelligence` | historical | Retired Grok narrative archive; read-only |
| `farm_summaries` | varies | Per-user weekly summaries; needs Claude/Codex writer after Grok retirement |
| `x_market_signals` | ~80/week | X/Twitter posts scored per grain/week |
| `validation_reports` | 1/import | Post-import anomaly detection results |
| `signal_feedback` | varies | Farmer relevance votes on X signals |
| `grain_sentiment_votes` | varies | Weekly haul/hold/neutral votes by grain |
| `cgc_imports` | 1/import | Audit log of data loads |
| `grain_monitor_snapshots` | 1/week | Government Grain Monitor: port throughput, vessel queues, OCT, storage capacity |
| `producer_car_allocations` | ~11/week | CGC Producer Cars: forward-looking rail allocations by grain/province/destination |
| `signal_scan_log` | ~3/day | Scan observability: mode, grains scanned, signals found, duration |
| `sentiment_history` | ~16/week | Archived weekly per-grain sentiment aggregates |
| `sentiment_daily_rollup` | ~16/day | Intra-week daily sentiment trajectory with delta tracking |
| `health_checks` | 1/pipeline | Post-pipeline site health validation results |
| `cftc_cot_positions` | ~9/week | CFTC Disaggregated COT: trader positioning per commodity per week, mapped to CGC grains |

## Key Views & RPC Functions

| Name | Type | Purpose |
|------|------|---------|
| `v_grain_yoy_comparison` | View | YoY metrics built from `v_country_producer_deliveries` plus terminal receipts/exports/stocks |
| `v_supply_pipeline` | View | Canonical AAFC balance sheet for the supply pipeline component |
| `v_supply_disposition_current` | View | Canonical latest supply-disposition row per grain/year |
| `v_signal_relevance_scores` | View | Blended relevance: 50% recency-adjusted AI + 40% farmer + 10% bonuses |
| `v_latest_import` | View | Data freshness check |
| `get_pipeline_velocity()` | RPC | Five pipeline metrics server-side, bypassing PostgREST row limits |
| `get_signals_with_feedback()` | RPC | X signal feed with current-user vote state |
| `get_signals_for_intelligence()` | RPC | Service-only X signals for intelligence generation |
| `calculate_delivery_percentiles()` | RPC | Percentile ranking over priced-progress pace by grain |
| `get_sentiment_overview()` | RPC | Per-grain sentiment aggregates for the overview banner |
| `get_delivery_analytics()` | RPC | Anonymized delivery + marketing-position stats with privacy threshold (>=5 farmers) |
| `get_historical_average()` | RPC | 5-year historical average/min/max/stddev for any grain/metric/worksheet |
| `get_seasonal_pattern()` | RPC | Weekly seasonal aggregates across multiple crop years |
| `get_week_percentile()` | RPC | Where current value sits in 5-year historical range |
| `get_logistics_snapshot()` | RPC | Grain Monitor + Producer Car data as structured JSON for Edge Functions |
| `snapshot_weekly_sentiment()` | RPC | Archives weekly per-grain sentiment aggregates to `sentiment_history` |
| `snapshot_daily_sentiment()` | RPC | Snapshots daily sentiment with delta tracking to `sentiment_daily_rollup` |
| `get_cot_positioning()` | RPC | Managed money and commercial net positions with spec/commercial divergence flag |
| `get_weekly_terminal_flow()` | RPC | Per-grain weekly terminal receipts, exports, and net flow |
| `get_aggregate_terminal_flow()` | RPC | Cross-grain aggregate terminal flow summary |
